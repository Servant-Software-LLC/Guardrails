# Auto-generated by the bump-tap workflow — do not edit by hand.
# Source template: .github/templates/guardrails.rb.tmpl in Servant-Software-LLC/Guardrails.
# Placeholders (@VERSION@, @SHA_*@) are substituted per release from the release checksums.
class Guardrails < Formula
  desc "Ship AI-built software with confidence — verify agentic work against your standards"
  homepage "https://github.com/Servant-Software-LLC/Guardrails"
  version "@VERSION@"
  license "MIT"

  on_macos do
    on_arm do
      url "https://github.com/Servant-Software-LLC/Guardrails/releases/download/v#{version}/guardrails-#{version}-osx-arm64.tar.gz"
      sha256 "@SHA_OSX_ARM64@"
    end
    on_intel do
      url "https://github.com/Servant-Software-LLC/Guardrails/releases/download/v#{version}/guardrails-#{version}-osx-x64.tar.gz"
      sha256 "@SHA_OSX_X64@"
    end
  end

  on_linux do
    on_arm do
      url "https://github.com/Servant-Software-LLC/Guardrails/releases/download/v#{version}/guardrails-#{version}-linux-arm64.tar.gz"
      sha256 "@SHA_LINUX_ARM64@"
    end
    on_intel do
      url "https://github.com/Servant-Software-LLC/Guardrails/releases/download/v#{version}/guardrails-#{version}-linux-x64.tar.gz"
      sha256 "@SHA_LINUX_X64@"
    end
  end

  def install
    libexec.install Dir["*"]
    (bin/"guardrails").write <<~SH
      #!/bin/bash
      exec "#{libexec}/guardrails" "$@"
    SH
    chmod 0755, bin/"guardrails"
  end

  def caveats
    <<~EOS
      Guardrails ships agent-side skills. To deploy them for Claude Code, run:
        guardrails skills install --force
      then restart Claude Code so it picks them up.
    EOS
  end

  test do
    assert_match "guardrails", shell_output("#{bin}/guardrails --version")
  end
end
