REPO: C:\DevAI\Guardrails
TOPIC: Guardrails/Model-Tiering-Epic

CONTEXT: What we were working on
The #201 model-tiering arc. This session (2026-08-10) opened the arc's next front: the user now has
a MacBook running Qwen-32B locally and wants to complete the local-inference path for DAG tasks. The
driving question: does the plan include dials for choosing the model/provider for BOTH the task
ACTORS and the AGENTS THAT REVIEW the task work (pass/fail) at end-of-run — and the reviewer, because
it depends on the models that did the work, should be an equal-or-higher thinker than the actor. The
answer after evaluation: per-task ACTOR tiering is already designed (#201 / #224–#231); the VERIFIER/
JUDGE dial + a "judge ≥ actor" invariant is the GAP. I authored a Charter living-document plan for
that gap (docs/plans/model-tiering-verifier.charter.md) and served it via `charter review` for the
maintainer's decisions. Prior threads on this topic (the static-v1 DoR PR #342 and the #349 pilot-seat
dogfood) remain open and are carried forward below.

COMPLETED:
--- Prior (2026-07-07 and before, carried forward) ---
- #201 extended with the "Resolution timing" design (static difficulty TAG vs DYNAMIC attempt-launch
  resolution of (provider,model,effort)); escalation ladder promoted to a core mechanism; renamed to
  "Epic: Model tiering for token/cost conservation…".
- #201 split into 8 sub-issues #223–#231 (registry #224, tier tagging #225, runtime resolution #226,
  budget probes #227, escalation ladder #228, guardrails-review appropriateness check #229,
  cost-by-tier #230, interactive steering #231; local-inference IPromptRunner #223 out of the wave
  sequence). Wrote the 3 stage briefs model-tiering-{foundation,consumers,dynamic-behavior}.md
  (Sonnet 5, commit aca1ea9) + the diagram-live-status-and-search.md brief (#219/#220).
- OSS-extraction verdict (agent-team, 3 independent perspectives): build in-repo behind a narrow
  IModelRouter-style seam; do NOT stand up a separate repo. Posted on #201. Devil's-advocate also
  flagged that tiering is NOT a named v2 bet in 03-roadmap.md.

--- 2026-07-10 (carried forward) ---
- Diagram.html fixes shipped to master: #235 (title-band SVG click-overlay), #237 (overlay rect fill
  via inline style.fill). Both verified with real headless-Chrome popup capture.
- FABLE-AUTHORSHIP FINDING (forensics): the #201 DoR (docs/plans/13-model-tiering.md, commit e6ed12e,
  PR #342) was authored ENTIRELY by a Fable architect sub-agent; git attribution masked it because the
  `Co-Authored-By` trailer is a FIXED TEMPLATE, not the running model. (Sub-agent transcript:
  …/0e73f910…/subagents/agent-a0b54d80f7f8c6003.jsonl.)
- Ran the Fable devil's-advocate on PR #342 (maintainer's 7 inline comments + independent findings):
  BLOCKER F1 (OD-A routes every task to frontier when retries:0, inverting the cost thesis);
  provider-unavailability unhandled; no way to reserve a model for breakdown-only; §6.2↔§8 route-down
  contradiction. Posted the DA report to #342; submitted the maintainer's 7 pending review comments.
- Filed #349 — "Surface which Claude model actually ran each task/attempt" (the pilot-seat problem).
- Opus + KISS pass: single-model runtime path already ~90% clean; the one real violation is
  /plan-breakdown auto-tagging every task then GR2041-warning an untiered user (fix = gate tag
  authoring on routing being configured). Cut-list for v1: drop ladder #228 / probes #227 / steering
  #231. Architect confirmed #349 as the ideal first dogfood.
- LANDED the ratified STATIC-V1 DoR revision on PR #342 (commit e60b941, OPEN DRAFT): Invariant #7
  (single-model/zero-setup byte-identical, observability-exempt); plan-scoped "configured" vs "active";
  v1 = registry #224 + gated tagging #225 + static resolver #226-static + #230-lite; ladder/probes/
  steering [v2 — deferred]; DA fixes folded (GR2044 reserved≠default, GR2045 pin+tier, unavailability→
  #115 pause); the 3 open decisions (OD-A scope, route-down lever, D15 triggers) DEFERRED to v2.
- Updated the global tool preview.35 → preview.40 (diagram title-wrap was a stale-tool issue).
- DOGFOODED the #349 /plan-breakdown to a validated draft: docs/plans/pilot-seat-model-provenance/
  (12 tasks, 6 tiers), regenerated fresh on preview.40 skill+tool; validated clean, graphed, locked.

--- This session (2026-08-10) ---
- Evaluated the local-inference / provider-dial question against the current plan. Verdict:
  * ACTOR side (the model that DOES a task) IS designed: provider registry #224, difficulty tags #225,
    per-attempt tier→(provider,model,effort) resolution #226, escalation ladder #228, steering #231.
    A local Qwen-32B endpoint slots in as a first-class actor tier via the #224 registry seam.
  * VERIFIER/JUDGE side is the GAP: (1) no dial selects the judge's provider/model/tier independently
    of the actor's; (2) no invariant enforces judge tier ≥ actor tier; (3) #229 checks the ACTOR tier
    fits the task but never checks the JUDGE is strong enough to grade it. The
    `promptRunners.<name>.guardrailOverrides` seam gives verdict prompts a distinct profile
    (perms/tools/turns) but carries NO model/tier and relates nothing to the actor.
- AUTHORED docs/plans/model-tiering-verifier.charter.md — a Charter living-document plan for the gap.
  Framing: deterministic guardrails are model-agnostic and untouched; this governs only the LLM-verdict
  layer (prompt-judge guardrails, terminal <plan>/guardrails/ when it carries a judge, autonomous
  review surfaces). Principle: "a prompt may propose, only an equal-or-stronger judge may vouch."
  Names the failure it prevents = a weak actor grading its own homework (passing-but-blind #382 at the
  model layer). Contains frontmatter (charter-format-version: 1), a :::note (deterministic unaffected),
  a :::warn (self-certifying weak actor), a :::diagram (actor→judge≥ relationship), and 5 OPEN
  :::question blocks. Rendered clean.
- The 5 open decisions (all target:human, all UNANSWERED):
  1. enforce-mode — hard load-time GR error vs advisory #229 flag vs both.
  2. judge-default — when unset, default to actor's tier (equal) vs one above vs no-default (fail closed).
  3. granularity — per-plan vs per-judge-guardrail vs both (per-plan default + per-guardrail override).
  4. scope-surfaces (multi) — per-task judges / terminal <plan>/guardrails/ / autonomous review-gate
     #361 / overwatcher #269.
  5. escalation-compose — when the actor escalates on retry (#228), does the judge ride up to stay ≥
     (yes / no / yes-but-floored).
- SERVED the plan for review via `charter review model-tiering-verifier.charter.md --no-open`
  (session-scoped background server; re-serve on demand — the first server died on a session teardown).
- Confirmed Charter is at 0.11.0 (latest on NuGet) and BOTH its skills (charter-format, charter-review-
  ops) are stamped charter-version:0.11.0 — up to date.
- Answered the "does Charter support waves" question: Charter has NO first-class wave block; waves are
  expressed via markdown headings/prose (`## Wave N:` + "builds on Wave N-1") which the Guardrails
  plan-breakdown Step 0 already detects. So multi-wave charting is NOT a Charter feature request.

IN PROGRESS:
- model-tiering-verifier.charter.md is SERVED for the maintainer's review; the 5 :::question blocks are
  OPEN. Awaiting the user's answers (+ any inline comments) → `charter poll --apply` to fold answers
  inline → revise the plan → break it down via the Charter-aware /plan-breakdown (Step 0c, live on
  preview.49) → fold into the #201 epic / DoR #342. The plan file + its review artifacts
  (model-tiering-verifier.charter.review/, model-tiering-verifier.html) are UNTRACKED — will be
  committed when the decisions land and it graduates to breakdown.
- PR #342 (static-v1 DoR revision, commit e60b941) is an OPEN DRAFT awaiting the maintainer's #106
  INLINE REVIEW. 6 judgment calls flagged for confirmation: (1) #229 kept in v1; (2) GR2044/GR2045
  allocated; (3) a `no-route` residual kept; (4) unavailability at §6.3 not "§6.5"; (5) a `usage`
  provenance field kept "unless #349 already carries it"; (6) briefs only lightly touched. NOTE: memory
  says "do NOT merge #342; it needs the user's sign-off."
- The #349 pilot-seat dogfood folder (docs/plans/pilot-seat-model-provenance/) is a validated DRAFT.
  Next stage is maintainer-driven: review the folder → /guardrails-review → guardrails run. (Both the
  brief and the folder show as UNTRACKED in the current working tree.)
- Roadmap-gate question (re-admit model tiering as a named v2 bet in 03-roadmap.md, or leave as
  opportunistic backlog) — STILL open; worth deciding before any Stage-1 implementation.

NEXT STEPS:
- User reviews the 5 verifier decisions in the browser and hits "Send to agent" → I `charter poll
  --apply` to drain + fold answers → revise the charter → run the Charter-aware /plan-breakdown Step 0c
  on it → fold the resulting tasks into the #201 epic (likely as an addition to the DoR #342 / a new
  stage brief). The verifier plan is the vehicle that finishes the local-inference story: Qwen-32B as a
  local actor tier PLUS the judge-≥-actor guard so a weak local judge can't rubber-stamp weak local work.
- Maintainer reviews the #349 pilot-seat folder → /guardrails-review → guardrails run (maintainer-driven,
  live UI, per dogfood-user-involvement). #349 remains the agreed FIRST dogfood of the tiering arc —
  blocker-free, valuable today, and the model-provenance base #226/#230 assume.
- Maintainer does the #106 inline review of PR #342 (the 6 judgment calls + ratifying static-v1). Stage-1
  (#224/#225) implementation does not start until #342 clears.
- Decide the roadmap-gate question before Stage-1.
- When Stage-1 implementation starts, route it through the guardrails-* agents (architect / harness-
  developer / skill-author / test-author) with hands-on lead review — not solo.

KEY FILES:
- docs/plans/model-tiering-verifier.charter.md — NEW this session (UNTRACKED, under review). The Charter
  living-doc plan for the verifier/judge tiering gap (judge ≥ actor). 5 open :::question blocks
  (enforce-mode, judge-default, granularity, scope-surfaces, escalation-compose). Read fresh before
  resuming; poll answers with `charter poll --apply`.
- docs/plans/13-model-tiering.md — THE DoR (branch design/model-tiering-dor, PR #342 @ e60b941). Living
  SSOT for the epic; [v1]/[v2 — deferred] tagged. The verifier addition folds in here (or as a sibling
  stage brief) once its decisions land.
- docs/plans/model-tiering-{foundation,consumers,dynamic-behavior}.md — the 3 stage briefs under the DoR
  (on master). Foundation = Stage 1 (#224+#225); consumers = Stage 2 (#226/#227/#229/#230); dynamic =
  Stage 3. consumers.md §229 flags an ACTION-tier mismatch but NOT a judge≥actor constraint (the gap the
  verifier charter fills).
- docs/plans/pilot-seat-model-provenance.md + docs/plans/pilot-seat-model-provenance/ — the #349 brief +
  the generated 12-task dogfood folder (validated+locked; UNTRACKED). Maintainer's next stage.
- src/Guardrails.Core/Prompts/ClaudeStreamParser.cs — ~line 77-81 `Feed` discards the system/init model
  the CLI echoes; the load-bearing #349 change.
- src/Guardrails.Core/Execution/PromptExecutionSupport.cs:64 — ResolveModelForDisplay(taskModel,
  runnerModel); #349 adds ResolveActualModel/IsModelMismatch, #226 inserts tier resolution here, and the
  verifier work adds judge-tier resolution for verdict prompts beside it.
- src/Guardrails.Core/Prompts/PromptRunnerRegistry.cs — the single-runner-class extension seam #223/#224
  extend; `guardrailOverrides` (verdict-prompt profile) lives in the runner config and is where a
  judge model/tier field would attach.
- docs/plans/02-schemas-and-contracts.md §9 (prompt runners) / §3 (task.json) / §7 (provenance) — SSOT
  sections the epic + #349 + the verifier dial amend.
- docs/plans/03-roadmap.md — does NOT name model tiering as a v2 bet (unresolved roadmap-gate).
- .claude/skills/plan-breakdown/** and the installed `guardrails` tool — BOTH must be current for
  /plan-breakdown to produce correct output. Installed now: preview.49 (Charter-aware Step 0c live).

CROSS-REPO:
- ServantSoftware.Charter is the tooling used to author + serve this plan. Installed on this laptop at
  0.11.0 (latest on NuGet), skills stamped charter-version:0.11.0. Charter has no first-class wave block
  (waves via markdown headings). The Guardrails↔Charter .charter.md ingestion (#390–393, plan-breakdown
  Step 0c) shipped in Guardrails preview.49 and is what will consume this plan once its decisions land.
- Local inference: a Qwen-32B endpoint on the user's work MacBook is the motivating local actor tier —
  the concrete reason the actor/verifier dials and the judge-≥-actor guard now matter in practice.

BLOCKERS/NOTES:
- The judge ≥ actor principle: "a prompt may propose, only an equal-or-stronger judge may vouch" —
  deterministic guardrails are UNAFFECTED (model-agnostic, strongest gate); this governs ONLY the layer
  where an LLM renders the verdict. Never weakens the deterministic-first posture.
- `charter review` servers are SESSION-SCOPED (child of the running session, die on session teardown).
  If a review URL goes dead, re-serve with `charter review <file> --no-open` and drain answers with
  `charter poll --apply` in the same turn. Charter rotates the key each serve, so URLs are not stable.
- KEEP THE TOOL + SKILLS CURRENT (memory keep-guardrails-tool-current): a stale installed `guardrails`
  or stale .claude/skills/ produces already-fixed bugs. Fix = update, not a `dotnet run` workaround.
  Installed now: preview.49.
- Do NOT merge PR #342; it needs the user's sign-off (memory model-tiering-epic-dogfood).
- Drive the tiering arc THROUGH Guardrails (plan-breakdown → review → run), not hand-coded (memory
  model-tiering-epic-dogfood) — the verifier charter is the current dogfood vehicle for that.
- git commit `Co-Authored-By` trailers are a FIXED TEMPLATE, NOT the running model — do not infer
  authorship from them (the Fable-DoR finding). Sub-agent model is only in the sub-agent transcript.
- Dogfood-user-involvement (memory): "dogfood X" = the USER runs /plan-breakdown → /guardrails-review →
  guardrails run themselves with live UI, reviewing each stage; don't chain the pipeline autonomously.
- The escalation ladder's retry-budget question and OD-A cost profile are DEFERRED to v2 with the ladder.
