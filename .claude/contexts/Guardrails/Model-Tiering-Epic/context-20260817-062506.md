REPO: C:\DevAI\Guardrails
TOPIC: Guardrails/Model-Tiering-Epic

CONTEXT: What we were working on
Stage 1 of the model-tiering epic (#201) is COMPLETE and **v1.6.0 is published**. The **design of
record is MERGED** (PR #342 → `9624bed`), carrying a new **revision 4**. **Stage 2 (#226, the
resolver) has been chartered, broken down, and adversarially reviewed three times by five agents.**
Master is `9f939f5`, pushed. The breakdown is NOT yet marked reviewed — three DoR-level questions
are open (see BLOCKERS). The deepest output of the session is a doctrine finding about guardrail
construction (#468) that will change how every future breakdown is authored.

COMPLETED (carried forward + this session):
--- Stage 1 + 1.5 + workstream B (shipped in v1.6.0) ---
- **#457 + #451** (`29787bc`): UTF-8 pinned on every git boundary (a CP437 round-trip had produced
  514 mojibake in the SSOT from a merge with two CLEAN parents); merge-on-success DEFERRED past the
  terminal gate; plan-root `scope:"integration"` guardrails now actually run at unions via one
  `UnionIntegrationSet(plan)` helper. **#451 is DETERMINISTIC** — `AiMergeResolver.cs:86` is
  `conflictedFiles[0]`, so any union conflicting in 2+ files can never be AI-resolved.
- **Stage 1.5 schema reconciliation** (`6157faa`) — maintainer ruled **the DoR wins**. Added
  `routing.tiers`, `tiering.verifier.minTier`, `effort`, `openai-compat`, GR2047–GR2050.
- **Workstream B — `guardrails providers init`** (`8744231`, `2c62182`). `Utf8JsonReader` with
  CommentHandling.Allow + byte-offset INSERTIONS (System.Text.Json cannot emit comments).
- **#455** (`7f7877e` + `76474e1`), **#461/#450** (`dad1547`), **#447** (`b67e515`), **#450 macOS**
  (`cb39c5c` + `a05e3b8` — re-canonicalise an adopted link target; `/var`→`/private/var`).
- **v1.6.0 CUT AND PUBLISHED** — 12/12 pipeline jobs green, live on NuGet.
- **Toolchain current**: tool 1.6.0, skills v1.6.0 (user scope). `guardrails --version` prints the
  version alone (the #461 drift warning cleared) — that is the real confirmation.

--- The design of record is MERGED, with revision 4 (this session) ---
- **PR #342 squashed to `9624bed`.** `docs/plans/17-model-tiering.md` is on master for the first
  time, satisfying the charter's `s2-dor-gate` ruling. All four CI jobs green.
- **Revision 4 added three amendments**, all from the Stage 2 charter review:
  - **D28** (§6.2) — a BINDING costly ceiling must be LOUD. Changes what is logged, never what is
    selected, so D22 is untouched.
  - **D29** (§6.5 rule 5) — human sanction PROPAGATES: a pinned `costly` ACTOR licenses a costly
    JUDGE bump. The `default` pointer does NOT trigger it (plan-wide fallback, not a per-task call).
  - **D15a** (§7) — every budget-consuming failure gets ONE same-tier retry before the rung
    escalates. Revision 3 granted that only to timeout/max-turns/output-cap; `guardrail-failed` has
    the strongest shipped escalator of all (#179 re-emit), so the split was an oversight, not a
    trade. Bounded: once per rung, never re-opening a materially identical repeat (#264/#174).

--- Stage 2: charter → breakdown → three review rounds (this session) ---
- **Charter authored and reviewed** (`9ed66eb`): `docs/plans/model-tiering-stage-2.charter.md`, its
  review log committed alongside. **Five questions settled, all matching my leans:** §6.5 IS in
  Stage 2 · merge #342 first as a real gate · terminal DoR-conformance guardrail · WAVED ·
  maxTurns 80. Six maintainer notes actioned; one I pushed back on (GR2048 is a validate-time
  config refusal at zero spend, not a mid-run halt — and v1 has no escalation at all, so the
  "spend the whole retry budget on the same model" behaviour asked for is already what v1 does).
- **Breakdown authored** (`1b8e681`) — WAVED + JIT: `wave-01-resolver-core` fully authored (4 tasks,
  strict linear chain 01→02→03→04), `wave-02-attempt-launch-wiring` a declared stub with a seeded
  `brief.md`. `$tiering = not-configured` (this plan IMPLEMENTS tiering; it does not route on it).
- **THREE adversarial review rounds, five agents** (guardrails-devils-advocate ×2 incl. one on
  **Fable**, guardrails-architect, guardrails-harness-developer, guardrails-test-author).
- **The terminal conformance gate went through four designs** (`9f939f5` is v4), and its own header
  records why each failed — that history IS the rationale:
  - v1 could only fail for something PRESENT AND WRONG (absent deliverables passed silently).
  - v2 = a 14-clause GREP manifest; MEASURED to certify DECLARATIONS: against a wave-1-only tree
    with no wiring, **10 of 14 clauses went green** (a `bool NoRoute` property satisfied "the
    no-route outcome exists").
  - v3 moved behaviour to a contract test gated on `executed >= 6`; MEASURED gameable — `dotnet
    test` counts **theory data rows**, so one `[Theory]` with six `[InlineData]` rows cleared it.
  - **v4** = structural greps for what greps prove soundly + a **BEHAVIOUR MANIFEST over discovered
    test NAMES** (`--list-tests`), one clause per required behaviour. It **ratchets**: wave 3 lands
    a judge test and the §6.5/D29 clause goes green with nobody editing a script.
- **The fragile guardrail layer was DELETED, not patched again** (`9f939f5`, net −101 lines):
  `02-uses-shared-candidacy-predicate.ps1`, `03-resolve-still-stubbed.ps1`,
  `02-selection-not-duplicated.ps1`. D22a became a **PROPERTY TEST** instead — the resolver's
  candidate set must agree with `ServesTier` for every (block, rung) pair, which fails the moment an
  inlined copy drifts (the only moment the rule matters). No regex can express that.

--- Issues filed this session ---
- **Guardrails #464** — `WorktreeContainmentHook` can BLOCK a legitimate in-worktree write on macOS
  (same `/var` aliasing as #450); its tests build both sides of the comparison from one string, so
  the hazard is structurally unreachable. Do NOT reintroduce `realpath` (GNU-only; cost 13 macOS
  failures) — bake in both spellings.
- **Guardrails #465** — `BreakdownManifest.ShouldInclude` captures a waved plan's per-wave
  `diagram.md`/`.html` as authored content (`segments.Length == 1` is a flat-plan assumption).
  Scope correction: the neighbouring `state/` rule is NOT affected.
- **Guardrails #466** — plan-breakdown/guardrails-review should verify at USE time that the RUNNING
  skill matches the installed harness. THREE versions exist (Loaded/Disk/Binary); `--version` sees
  only Disk↔Binary, so nothing observes the LOADED copy. Two readings can't tell "disk stale" from
  "session stale"; three can. Remedy is always *install AND restart*.
- **Guardrails #467** — when the reviewing agent AUTHORED the breakdown, delegate the adversarial
  pass to independent agents. Evidence: this session's regressions.
- **Guardrails #468** — **THE session's most durable output.** Guardrails asserting
  IMPLEMENTATION-SOURCE shape are systematically defect-prone: across 3 rounds / 5 agents, EVERY
  blocker lived in that layer while the test-based machinery (#455 filters, #248 assumptions, #179
  re-emit, zero-match guards, the DAG) was **never broken by any agent in any round**. Carries a
  13-item measured failure taxonomy and four proposed doctrine changes — including the control
  agent's: a source-shape guardrail must ship its **valid/invalid fixture pair committed beside it**
  (#302 made durable rather than a per-round manual pass).
- **Charter #152** — skill-version drift is entirely invisible in Charter (no `--version` warning,
  no use-time check). `charter-drain` is the sharpest case: it branches on `poll` exit codes, so
  stale semantics make a FAILED drain read as "nothing to report".
- **Charter #156** — a deferral named in a plan should be checked against the tracker and raised as
  a `:::question` when nothing tracks it. Carries a **maintainer ruling at the top**: Charter must
  NOT grow issue-tracker integration; the binary stays dependency-free and the AGENT does the lookup.
- **Charter #157** — agent replies display under the human reviewer's name; the panel ignores the
  `actor` field, so `--as-human` is unobservable. Data is correct (`actor:agent` on every reply);
  display-layer only, so every existing log re-attributes correctly once fixed.
- **Charter #158** — a reviewer cannot continue a thread: `AppendReply` DEFAULTS to human but its
  only caller is the agent's CLI verb. Fix is two-part — a panel affordance AND delivery on the poll
  envelope (which carries only `annotations` + `answers`), or the button would silently change
  nothing.

--- Maintainer rulings worth not re-litigating ---
- **`costly` is TRI-STATE** (three states in the schema, TWO at the predicate: absent AND explicit
  false both serve, so an un-annotated registry stays routable).
- **A comment a human DELETED must not come back** (`providers init`).
- **Hand-code anything whose guardrails would need network calls.**
- **Difficulty maps to a candidate SET, not to strength** — tier → blocks whose `routing.tiers`
  contains that rung → ASCENDING strength → first wins. A `hard` task gets the WEAKEST model
  declared capable of hard. No numeric tier→strength mapping, ever.
- **The DoR wins** over the charter and over any plan text.

IN PROGRESS:
- **The Stage 2 breakdown is authored, reviewed three times, and NOT marked reviewed.** All fixable
  blockers are fixed; three remaining findings are DoR-level, not plan-level (see BLOCKERS).

NEXT STEPS:
1. **Settle the three DoR-level questions** (BLOCKERS below) — they gate `mark-reviewed`.
2. Then `guardrails mark-reviewed docs/plans/model-tiering-stage-2 --evidence <report>` per the
   #366 flow (plan-hash → report under `state/reviews/` → stamp), and run it.
3. **Restart Claude Code** if skills were reinstalled since (the loaded copies are what execute).
4. Fix #464/#465 when convenient; #468 and #467 are doctrine changes to the skills.
5. Stage 3 (the verifier route, §6.5) gets created as a stub by wave 2's JIT breakdown — the
   wave-2 brief now instructs it explicitly, AND the terminal gate's §6.5/D29 clause fails if it
   never happens (mechanism, not discipline).

KEY FILES:
- `docs/plans/17-model-tiering.md` — the DoR, **now on master**. §6 is Stage 2's spec; §16 carries
  the decision registry incl. D28/D29/D15a.
- `docs/plans/model-tiering-stage-2.charter.md` + `.charter.review/` — the reviewed charter and its
  review log (five settled answers, six actioned notes).
- `docs/plans/model-tiering-stage-2/` — the breakdown. Read
  `guardrails/03-dor-section-6-contract-landed.ps1`'s header first: it records why v1–v3 failed.
- `docs/plans/model-tiering-stage-2/wave-02-attempt-launch-wiring/brief.md` — the JIT input for
  wave 2. Carries the `tierSource` blocker, the test-name marker table, and the one-ahead
  instruction to create + seed `wave-03-verifier-route/`.
- `src/Guardrails.Core/Model/PromptRunnerConfig.cs` — `ServesTier` / `DeclaresTier` (D22a).
- `src/Guardrails.Core/Loading/PlanLoader.cs` — `Tier = rawAction?.Tier ?? defaultTier`, the line
  that makes `tierSource` uncomputable.
- `src/Guardrails.Core/Execution/PromptExecutionSupport.cs` — `ResolveModelForDisplay`, called from
  `TaskExecutor.cs`. Wave 2 replaces this path. (NOT in `TaskExecutor` itself — an earlier brief
  said so and would have under-scoped the writeScope.)

CROSS-REPO:
- Charter repo `C:\DevAI\Charter` (`github.com/Servant-Software-LLC/Charter`). **Charter 0.21.0 =
  latest**; skills stamped 0.21.0. #137/#142/#143 are CLOSED. Open: **#152, #156, #157, #158** (all
  filed this session).
- **Charter has no `--version` drift warning**, so a clean `charter --version` proves nothing about
  its skills — read the `metadata.charter-version` stamp instead.
- **Cross-repo version coupling**: Guardrails' `plan-breakdown` cites Charter's `charter-format` as
  normative, so a Charter skill governs a Guardrails skill's correctness and neither tool's
  `--version` can see the pairing (Guardrails #466, Charter #152).
- **The lane rule**: fixes go to the session that OWNS the repo receiving the commit. Filing an
  issue cross-repo is fine; committing code is not.

BLOCKERS/NOTES:
- **THREE DoR-LEVEL QUESTIONS GATE `mark-reviewed`:**
  1. **`tierSource` is NOT COMPUTABLE.** `PlanLoader` collapses `action.tier` and
     `tiering.defaultTier` at load and `ActionDefinition` keeps no provenance, so nothing can tell
     `task` from `plan-default`. Stage 1 already works around it with a guess
     (`PlanValidator.cs:534`). Shipping as-is emits `"task"` 100% of the time. Fixing it needs
     `PlanLoader`/`ActionDefinition` — outside every wave-1 writeScope. **Removed from wave 1;
     recorded as wave 2's blocker.** Found only because an agent actually IMPLEMENTED the tasks.
  2. **`tierSource: "override"` has no producing rule** anywhere in the DoR (§12.4 lists the value,
     §6.1 defines nothing that emits it).
  3. **§6.1 item 3 vs §6.2/D26 contradict** for the same condition: "no block serves the tier" ⇒
     legacy fallback, AND ⇒ halt. The DoR never reconciles them. Consequence if unresolved: a silent
     fallback routes to the `default` pointer, which may itself be the costly block.
- **THE PROCESS LESSON, five times over:** each fix round introduced a regression in a neighbouring
  guardrail — a regex matching a declaration not a call; a brace-truncating body extractor
  (false-green AND false-red); a costly-bypass ban requiring a defaulted bool; a positive check
  re-reading RAW content (re-breaking a round-1 fix); and literal backspace bytes from a
  Python-in-heredoc. **Fix: keep the mutation battery and re-run ALL of it after every edit**, not
  just the cases for the change just made. **Never author regex-bearing scripts via bash/Python
  heredocs** — backslashes get mangled (happened three times); use the file-write tool.
- **The Fable experiment** (paired, identical prompt/role, one variable): control slightly ahead —
  5 novel true findings to 4, and correct severity on the one they disputed (adjudicated by
  execution: `03-resolve-still-stubbed` was a BLOCKER, not a NIT). Zero false positives from
  either. Read as *no clear Fable advantage on this task*, not a verdict — one paired trial cannot
  separate model from run-to-run variance.
- **#452 remains the thing to watch for any Stage 2 RUN**: the overwatcher fired on Stage 1, spent
  $0.66, recorded ZERO decisions (its own Bash calls were permission-denied). Silent AND billed.
  The real Stage 1 defect it masked was `maxTurns: 50` being too tight — hence the charter's 80.
- **Do NOT run `guardrails skills install --project --force` in this repo** — `.claude/skills` is
  TRACKED SOURCE and the input the tool packs from. As of #461 the tool refuses it without
  `--overwrite-tracked`. Plain `guardrails skills install --force` (user scope) is correct.
- **Measured timings** (warm, this box): Core suite 2m00s / 1522 tests; Integration 12m11s / 776.
  Plan preflights run in a FRESH worktree with no `bin/obj`, so realistically **20–30 min before
  the DAG starts**, against a hard-coded 1800s re-verifier ceiling that does NOT read
  `defaultTimeoutSeconds`. A slow runner could halt the run before any task is scheduled.
- **Other open issues NOT from this session** (noted, unreviewed here): #463 (Stage 1's own
  guardrails still carry the #455 defect — 5 of 7 filter on the bare trait), #462 (`-v q` defeats
  #179 in four committed guardrails), #459, #458, #456, #454, #453, #452, #450, #449, #447, #444,
  #442. #463 and #462 look directly relevant to the guardrail-doctrine work.
