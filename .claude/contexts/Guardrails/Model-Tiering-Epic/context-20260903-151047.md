REPO: C:\DevAI\Guardrails
TOPIC: Guardrails/Model-Tiering-Epic

CONTEXT: What we were working on

**PLAN 34 SHIPPED — #585 (run event stream) + #560 (attach mode) are on master, CI green on all three
OSes.** That closes the item the prior context listed as next-step 3 and told the receiving session not
to preempt. Earlier in the same session: the Apple-foundation-models question resolved into the shipped
`apple-fm` engine hint, the local-inference + telemetry roadmap filed as **#570**, and plan 30's open
§3.2 bucket question answered from the real corpus.

master `c10a13a`, pushed, **Core 2408 / Integration 1113 pass (4 by-design skips)**. Tool **v1.16.0**;
user-level skills re-installed to v1.16.0 (repo's authored `.claude/skills/` untouched — that is source,
not a destination, #461).

Repo is back on **master, clean**. The prior context's "leave it on `design/34-…`" no longer applies —
that branch is merged and deleted both sides.

NEXT STEPS — start here

1. **#587 check B** — still the highest-value thing outstanding, and now *more* evidenced. The
   failure-time attribution that replaces the declined measurement record is **built but NOT merged and
   NOT in v1.16.0**, held on `feat/587-check-b-failure-attribution` with two blockers documented (the
   only unmerged branch on master). Plan 34 demonstrated check B's absence **a second time**: its merge
   broke `ProducerCoverageCorpusTests`, a closed-world tripwire no task's `writeScope` owned — exactly
   what plan 33 did to `BreakdownSalvageAllowListTests`. **Twice in consecutive plans.**
2. **#594 — the ambient corpus anchor.** Filed this session as **#593's sibling** (`GitLsFilesProbe`'s
   ambient repo anchor). The corpus root is a process-wide env var, so one class's override captures
   every parallel run's rows. Plan 34 scoped the victim assertion so it is immune; **the pollution is
   still there.** Real repair: an explicit corpus root on the call path.
3. **#591 — the outward trace, now with a third direction.** Commented with the mirror of #203: an
   *earlier* task's test asserting a premise a *later* sibling falsifies (measured on plan 34 task 09).
   Neither skill has a probe for it.
4. **#559 — the samples-verify contract.** Commented with dogfood evidence. Concrete ask:
   `plan-breakdown` Step 7.0d should call `guardrails samples verify`, since a hand-rolled smoke test
   cannot catch a contract nobody documented.
5. **#577** — the model-attribution census, still Phase A of #570. Unchanged across three contexts now.
6. **#584** — a fifth `maxTurns` archetype keyed on READ VOLUME. Still lowest priority (cost, not
   correctness).
7. **Plan 30 §3.2 bucket** — recommendation posted to #548, awaiting a decision before it is implemented.

COMPLETED

- **PLAN 34 → #585 + #560 SHIPPED.** Chartered (5 `:::question` blocks, all answered in a Charter review
  round), broken down as a 15-task DAG, reviewed TWICE, run autonomously **15/15 green, $31.65, 19 Sonnet
  invocations**, two terminal-gate fixes, merged `c10a13a`, **CI green on ubuntu + macos + windows +
  packaged-tool-smoke**. Delivers: `IRunObserver.AttemptFinished`, `events.jsonl`, `observer.jsonl` +
  `guardrails attach`, `GET /events`, and both projections wired at the production composition root via
  an extracted `RunCommand.BuildObserverChain`.
- **`apple-fm` engine hint shipped** (PR #555 / `940746c`) — the answer to "can Guardrails use Apple's
  OS foundation models". Yes, via `openai-compat`, with no new kind.
- **#570 filed** — the phased local-inference + telemetry roadmap against the Mac Studio (early Oct).
- **Plan 30 §3.2 answered from data** — the bucket recommendation, computed over **210 of 214** real
  corpus tasks joined to their `task.json`.
- **Issues filed/commented this session:** **#594** (new), **#559** (comment), **#591** (comment),
  **#570** (new).
- Prior sessions, still true: **Plan 33 → GR2060** (12/12, $58.89). Three harness fixes (`5d2b0f0`):
  #588, #586, #587-A → GR2071. **Plan 30 (27/27, $77.18)**; #578; #548 closed. Plan 31 → #554/#553/#545,
  v1.13.0. Plan 32 → #556, v1.14.0. #552 fixed. CI corpus tests fixed by the parallel session (#592,
  `fetch-depth: 0`, which also reactivated 4 silently-skipping tests).

KEY DECISIONS — do not re-litigate

- **Plan 34's shape: ONE emission seam, TWO projections.** The maintainer's Charter answer. `events.jsonl`
  is semantic/low-frequency/agent-facing; the observer stream carries render-fidelity live-only fields. A
  single stream serves one consumer badly.
- **`AttemptOutcome` IS the reason vocabulary — no new enum.** 14 existing members, already what the
  journal and the telemetry corpus record. #585's proposed closed set maps onto it ~1:1.
- **`BuildObserverChain` is PUBLIC**, not internal. `Guardrails.Cli` ships **no `InternalsVisibleTo`**, and
  the tests driving that seam live in `Guardrails.Integration.Tests`. A non-public seam is uncompilable
  from them. House pattern already agrees (`LiveRunObserver`, `OnTheFlyLogSiteObserver` are public).
- **A `DisableParallelization` collection for #594 was REJECTED on measured cost**: 4 classes mutate a
  process-wide env var, **20+** spawn real runs. Serializing that taxes every CI run forever to guard an
  assertion now immune. The scoped assertion is also sharper — every other assertion in that file was
  already runId-scoped; line 253 was the sole outlier.
- **Plan 30 bucket = `<archetype>/<size>`.** archetype from `writeScope` path prefixes (never the task
  name); size = `writeScope >= 4 OR guardrails >= 4`. Measured: archetype spreads first-pass 52→77%, size
  73% vs 41%. Rejected `maxTurns` (unset on 117/210 and circular per #527) and cardinality alone (1 vs 2-3
  is 69 vs 71%). `test-authoring` is the best first local-inference target: **24% of all spend**, $6.49/task.
- **Apple FM ≠ the Mac Studio bet.** AFM 3 Cloud Pro lives on **PCC** (Apple's servers) and never reaches
  your hardware; on-device is a fixed 3B / 20B-sparse you cannot choose. **MLX is what the unified memory
  buys.** `fm serve` is community-reported, not Apple-documented.
- **Carried forward, unchanged:** GR2070 DECLINED and held by name; GR2060's positive control is `544f7d5`
  not `1b8e681`; `544f7d5`→`5bd29da` is a recovered fires/silent pair; `PlanIsClosed` detects an empty stub
  not an un-authored wave (#583); GR2071 is WARNING permanently (`allowedTools` is a floor, #252); #586
  matches payload SHAPE not key name; six fingerprint buckets from `writeScope` roots never `taskId`; model
  digest in Phase 1 in full; #577 is the census only; pre-fix corpus era gets a boundary date not a
  backfill; the 64GB Mac Studio is the tighter box.

KEY FILES

- `docs/plans/34-run-event-stream-and-attach.charter.md` + `.md` + `.manifest.json` + `/34-…/` — the
  charter (answers folded inline), the flattened handoff, the custody manifest, and the 15-task folder.
  **Both review reports are under `state/reviews/`** — pass 1 (self) and pass 2 (non-authoring).
- `src/Guardrails.Core/Execution/IRunObserver.cs` — now carries `AttemptFinished`. **The 5th member with
  the standing decorator-swallow warning**; a decorator that does not forward it explicitly is silent.
- `src/Guardrails.Core/Execution/RunEventStream.cs` / `ObserverProjection.cs` — the two projections.
- `src/Guardrails.Cli/Commands/RunCommand.cs` — `BuildObserverChain` at the composition root, called by
  BOTH the live-UI and `--no-ui` branches. Task 13 extracted it; task 15 wired the projections in.
- `src/Guardrails.Cli/Commands/AttachCommand.cs` + `CommandFactory.cs` — `guardrails attach`, registered.
- `src/Guardrails.Core/Execution/AttemptJournaler.cs` — raises `AttemptFinished` from **11** completion
  paths. The prompt originally said to grep `Outcome = AttemptOutcome`, which finds **2**; the DA pass
  caught that and the fix is why the coverage is real.
- `src/Guardrails.Core/Samples/SampleVerifier.cs` — **read this before authoring any sample pair.** Supplies
  the sample BOTH positionally and as `GR_SUBJECT` (`:67,336`); also runs as a pre-DAG halt via
  `PlanPreflightPhase.cs:220`.
- `tests/Guardrails.Integration.Tests/TelemetryCorpusIsolation.cs` — the #547 module initializer. Its own
  reasoning (per-PROCESS so parallel suites cannot count each other's rows) is exactly why it cannot close
  #594's intra-process case.
- `tests/Guardrails.Core.Tests/GitEnvironmentCollection.cs` — the pattern #594 would mirror if cost allowed.
- Prior, still current: `33-unproducible-requirements.md`; `ProducerCoverage.cs` (GR2060);
  `PromptToolGrantCoverage.cs` (GR2071 — **its doc comment holds a LIVE positive control**, do not "fix"
  plan 33 task 02's prompt without replacing it); `NestedControlKey.cs`; `GitWorktreeProvider.cs`;
  `docs/plans/19-producer-coverage.md`.

CROSS-REPO: none this session.

BLOCKERS/NOTES: For the receiving session

- **THE PATTERN IS NOW AT ELEVEN INSTANCES.** Every serious problem is a defect in the INSTRUCTIONS or the
  PREMISES, not the checks, and all of them pass `validate`, `graph --check` and adversarial review. Plan 34
  added two: a sample pair that could never pass because the skill never taught `GR_SUBJECT`, and a test
  asserting the absence of the plan's own feature. #587 check B is still the fix that matters most.
- **`guardrails samples verify` IS A PRE-DAG HALT, and `plan-breakdown` never mentions it.** Following
  `stacks/dotnet.md §22` faithfully produced a pair where BOTH halves scanned the real tree and exited 1 —
  inert, plausible, committed. **The plan could not START.** Run the verb, not a hand-rolled equivalent
  (#559). Detected only by the NON-authoring pass.
- **THE TERMINAL GATE EARNED ITS COST — three review layers missed what it caught.** Two failures reached
  it because **each task's guardrails run against that task's own partial base**; the gate is the first
  place the contributions meet. Do not read a slow terminal gate as overhead.
- **A test can assert the ABSENCE of the plan's own feature.** Task 09 wrote "observer.jsonl was never
  written (ObserverProjection is still task 07's stub)" into its fixture, and tasks 08/15 falsified it. The
  tell for a reviewer: every sibling fixture controlled the file explicitly; only this one relied on its
  absence being a property of the tree. Mirror of #203, forward in time. Recorded on #591.
- **COST CALIBRATION: plan 34 was $31.65 for 15 tasks** (plan 33 $58.89/12; plan 30 $77.18/27). `--autonomous`
  REQUIRES a `--max-cost-usd` or silently applies **$20**, which would halt early — but $250 was **8× over**.
  ~$60–80 is a realistic cap for a plan this size.
- **`--autonomous` alone gives dial=`high`** (best-guess low/moderate, escalate high/critical) — the
  conservative default and the right one. **Never pair `--dial critical` with `proceed-unreviewed`.**
- **Carried forward, all still true:** never trust the delivery banner — verify `git branch --no-merged
  master` + `git cat-file -e master:<deliverable>`; do not start a run while another is alive (a halt
  MESSAGE is not process exit — check `ps`); mid-run plan edits WITHHOLD delivery; `guardrails reset`
  cascades to descendants; verify BEHAVIOUR not the version string (#564); a #181 baseline can halt its own
  run on a red the plan created — exclude by METHOD (#574); the plan branch is cut once, never rebased
  (#576); a running `guardrails logs` viewer locks the tool store; **adversarial passes pay off only with a
  NON-authoring agent** — plan 33's found six blockers, plan 34's found four including the unstartable one.
- **A whole-project #181 baseline is a slow tax.** Plan 34's preflights gated on the ENTIRE Core and
  Integration suites, so the pre-DAG phase ran minutes before task 01. Scope to the touched area.
- **Two of my own checks needed positive controls before their results meant anything** — a `$PWD`
  path-separator bug reported 36 false "PARSE FAIL"s, and a `.gitignore`d-path grep would have reported a
  false clean. **A probe whose pass condition is "no matches" is not a measurement until it is proven to
  have reached its subject** (#500).
- **Tiering data, still the only real numbers:** easy **$3.11** / medium **$9.11** / hard **$42.40** (plan 33).
  Hard is ~72% of spend on a third of invocations. The rubric models deliverable difficulty, **not mechanism
  difficulty** (plan 33 task 11: correctly `easy` on content, `.claude/` delivery demanded byte-exact anchors).

ISSUES: filed this session **#570** (roadmap), **#594** (ambient corpus anchor). Commented: **#559**,
**#591**, **#548**. Prior session: #583, #584, #585 (**CLOSED by plan 34**), #586 (CLOSED), #587 (A shipped,
B built-not-merged, C open), #588 (CLOSED). #560 **CLOSED by plan 34**. #474 stays open by design
(reachability is permanently review-only per doc 19 D2).
