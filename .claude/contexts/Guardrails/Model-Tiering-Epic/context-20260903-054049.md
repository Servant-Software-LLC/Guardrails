REPO: C:\DevAI\Guardrails
TOPIC: Guardrails/Model-Tiering-Epic

CONTEXT: What we were working on

**PLAN 33 SHIPPED, AND THREE HARNESS FIXES IT EXPOSED SHIPPED AFTER IT.** #474's mechanical half —
GR2060 producer coverage — is built, and the instruction-vs-enforcement issue family (#584–#588) came
out of running it.

master `5d2b0f0`, pushed, Core **2380** / Integration **1081** green.

**The repo is checked out on `design/34-run-event-stream-and-attach`** (maintainer's branch for #585,
with untracked `docs/plans/34-*` files). Leave it there. Plan 33 was merged to master separately —
see BLOCKERS for why that mattered.

NEXT STEPS — start here

1. **#587 check B** — the highest-value thing outstanding. It is GR2060's own question asked of a
   PROMPT rather than a guardrail: *does some task's `writeScope` own the file this prompt requires
   changing?* Plan 33 demonstrated its absence **on itself** — it broke
   `tests/Guardrails.Core.Tests/BreakdownSalvageAllowListTests.cs`, a tripwire no task owned, and the
   baseline preflight halted the run before the DAG. Check A shipped (GR2071); B is the one with teeth.
2. **#577** — the model-attribution census, still Phase A of #570. Unchanged from the prior context.
3. **#585 event stream / attach mode** — the maintainer started `design/34-run-event-stream-and-attach`
   for it. **Do not preempt that design.** The issue carries two arguments worth keeping: consumers
   must not have to enumerate event kinds correctly (a missed event looks identical to a healthy quiet
   run), and event timestamps must record when the event OCCURRED — stdout is block-buffered when
   redirected, and lines arrived **30 minutes late in one flush** during plan 33.
4. **#584** — a fifth `maxTurns` archetype keyed on READ VOLUME (search through a large file), not on
   kind-of-work. Lower priority: the harness auto-escalates, so it is cost, not correctness.

COMPLETED

- **Plan 33 → GR2060 SHIPPED.** 12/12 green, **$58.89**, delivered to master. `UnproducibleGateRequirement`
  at ERROR, wired at `PlanValidator.cs:101` through `IGitTrackedFileProbe`; the #501 mitigation that
  makes ERROR safe; the callee-parameter-list step in both skills. **GR2070 DECLINED and held by name.**
- **Three harness fixes merged (`5d2b0f0`)**: #588 delivery-target verification, #586 nested-control-key
  rejection, #587-A → **GR2071**. Next-free is now **GR2072**.
- Earlier in the same session: **plan 30 (27/27, $77.18)** reviewed, run and merged; **#578** shipped
  (`09f223f`); **#548** closed.
- Prior sessions, still true: plan 31 → #554/#553/#545, v1.13.0. Plan 32 → #556, v1.14.0. #552 fixed.

KEY DECISIONS — do not re-litigate

- **GR2070 is DECLINED and HELD BY NAME**, with its reason line in `DiagnosticCodes.cs`. Its
  true-positive population across the whole repo history is **zero**: the clause form it keys on first
  appears **36 minutes AFTER** the scope it would have caught was already fixed. The bar to revisit is
  **a defect, at a commit** — not a clause that happens to be written in the right shape.
- **GR2060's positive control is `544f7d5`, NOT `1b8e681`.** At `1b8e681` the plan has a zero-task wave,
  so `PlanIsClosed` is false and condition 10 suppresses the check — it CANNOT fire there. Verified.
- **`544f7d5` → `5bd29da` is a RECOVERED fires/silent pair on one artifact.** Gate script byte-identical
  at both; the only difference is that a task owns the SSOT at `5bd29da`. This falsified §8.3's "condition
  8 has zero exercises in the corpus" and replaced a constructed fixture with a recovered one.
- **`PlanIsClosed` is `Waves.All(w => w.Tasks.Count > 0)`** — it detects an EMPTY STUB wave, not an
  UN-AUTHORED one, and returns **true** for an authored partial prefix. It is NOT a soundness guarantee
  for the JIT gate; that is `wavePrefixIsIncomplete`. Filed as **#583**.
- **GR2071 is WARNING permanently, not pending a better extractor.** `allowedTools` is a FLOOR, not a
  ceiling (#252) — an operator's own settings merge in, so a command it reports as refused may genuinely
  run. An ERROR would block a correct plan on a wall that does not exist for that operator.
- **#586's detection matches on PAYLOAD SHAPE, never the key name.** A false rejection blocks a task on
  every attempt forever; the bug only cost attempts. Asymmetric, so the predicate is narrow — and it is
  pinned from BOTH directions (loosening it to name-only fails exactly the 4 false-positive guards).
- **Carried forward, unchanged:** six fingerprint buckets from `writeScope` roots + guardrail archetypes,
  never from `taskId`; model digest is in Phase 1 in full; #577 is the census only; the pre-fix corpus era
  gets a boundary date, not a backfill; the 64GB Mac Studio is the tighter box.

KEY FILES

- `docs/plans/33-unproducible-requirements.md` + `/33-unproducible-requirements/` — design of record and
  the 12-task folder. §16 records the maintainer decisions; §8.2/§8.3 carry the corrections above.
- `src/Guardrails.Core/Loading/ProducerCoverage.cs` — GR2060.
- `src/Guardrails.Core/Loading/PromptToolGrantCoverage.cs` — GR2071. **Its doc comment records a LIVE
  positive control**: plan 33 task 02's prompt instructs `grep … | wc -l` with neither granted. Do not
  "fix" that prompt without replacing the control — it is the check's only real-bytes exercise.
- `src/Guardrails.Core/Execution/NestedControlKey.cs` — #586.
- `src/Guardrails.Core/Execution/GitWorktreeProvider.cs` — the #588 `BranchMoved` guard, which runs FIRST
  because the dirty-path and merge-shape probes also computed against HEAD.
- `docs/plans/19-producer-coverage.md` — Milestone A now points at plan 33; D2 gained one sentence.

CROSS-REPO: none this session.

BLOCKERS/NOTES: For the receiving session

- **THE PATTERN, NOW AT NINE INSTANCES AND WORTH THE DESIGN CONVERSATION.** Every serious problem in plan
  33 was a defect in the INSTRUCTIONS, not the checks, and **all of them passed `validate`, `graph --check`
  and a full adversarial review**: a prompt naming an ungranted command; the harness contract contradicting
  its own `needsHarnessWrite` example; two clauses assuming formatting the prose did not have; a plan
  breaking a tripwire it did not own. The symptom always pointed at the agent. #587 is the issue; check B
  is the fix that matters.
- **TWO FALSE REDS OF THE SAME CLASS — a prose clause over markdown may not assume ANY formatting between
  its tokens.** `**declaring** file` does not contain `declaring file`; a phrase split by a line wrap does
  not match a line-scoped clause. Both read correctly on the page and both rejected correct work. **A false
  red is worse than a false green** — it dead-ends work that was already right.
- **Delivery: NEVER trust the banner.** #588 is fixed now, but the habit stands — the run merges and then
  reports, and it once printed `delivered to master` while merging into a branch created mid-run. Verify
  with `git branch --no-merged master` plus `git cat-file -e master:<a deliverable>`.
- **Do not start a run while another is alive.** A halt MESSAGE is not process exit: plan 33's run printed
  `needs-human` while a task child still streamed, a second run was started on top of it, and the two
  collided on a locked `claude-stream.jsonl`. Check `ps` for `dotnet/tools/guardrails` first.
- **Mid-run plan edits are detected and WITHHOLD DELIVERY.** Editing a task prompt to rescue a running task
  works, but the divergence gate then holds the whole run back — a clean re-run with nothing in flight is
  what earns delivery.
- **`guardrails reset <folder> <taskId>` cascades to descendants.** Resetting task 06 reset six tasks.
  Correct, but budget for the replay.
- **Carried forward, still true:** verify BEHAVIOUR not the version string (#564); `GR_SUBJECT` arrives
  ABSOLUTE (#559); a #181 baseline can halt its own resume on a red the plan created — exclude by METHOD
  (#574, hit AGAIN in plan 33); the plan branch is cut once and never rebased (#576); a running
  `guardrails logs` viewer locks the tool store; adversarial passes pay off **only with a NON-authoring
  agent** — plan 33's found six blockers, four of which were clauses that could never fire.
- **Tiering data from plan 33, the first real numbers:** easy **$3.11** / medium **$9.11** / hard **$42.40**.
  All three tiers routed correctly (haiku/sonnet/opus). Hard is ~72% of spend on a third of invocations,
  which is the thesis working and also why routing accuracy is worth caring about. One observation for the
  rubric: task 11 was correctly tiered `easy` on CONTENT but its `.claude/` delivery mechanism demanded
  byte-exact anchor transcription — **the rubric models deliverable difficulty, not mechanism difficulty.**

ISSUES FILED THIS SESSION: **#583** (PlanIsClosed cannot see an un-authored wave), **#584** (maxTurns vs read
volume), **#585** (no run-event subscription), **#586** (control key vs harness contract — CLOSED), **#587**
(instructions vs enforcement; check A shipped, B and C open), **#588** (delivery target — CLOSED).
#570 Phase A updated; #474 stays open by design (reachability is permanently review-only per doc 19 D2).
