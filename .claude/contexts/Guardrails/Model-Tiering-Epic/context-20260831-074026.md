REPO: C:\DevAI\Guardrails
TOPIC: Guardrails/Model-Tiering-Epic

CONTEXT: What we were working on

**THE EPIC'S HEADLINE IS NO LONGER INERT.** The prior context's standing note — *"every tier still
resolves to a Claude model; #223 has a design of record and no code"* — is now false. **Plan 28 ran
31/31 green through the harness and is merged (`8ac346c`). #223 is CLOSED.** The openai-compat runner
exists, `guardrails validate` gates it, and a real run routed across all three tiers for the first time:
haiku x6, sonnet x44, opus x11.

This was also the first **unattended overnight** run the project has done — ten runs, seven escalations,
every one correct to fire, none resolved by weakening a guardrail.

**IMMEDIATE STATE:** master at `0db4070`. Tool released and installed at **v1.12.0**; skills installed at
v1.12.0 (USER scope). Working tree clean, everything pushed. **This session's own skills were loaded at
v1.11.0** — the restart this save precedes is what picks up v1.12.0.

> **CORRECTION carried forward from 2026-08-30, still load-bearing:** read to the ACTUAL END of a run's
> output and check `git branch --no-merged master` before believing a green run shipped. The
> `*** WORK NOT DELIVERED ***` banner comes AFTER the green summary and the cost lines.

COMPLETED: What got done

--- Everything through plan 27 — carried forward, unchanged ---
- Stage 1/1.5/B (v1.6.0), Stage 2 (3 waves, 34 tasks, $229.50), Stage 3 (5 waves, merged `3bcfdbf`).
- Plan 26 (#510, `6efded9`, $21.77) and plan 27 (#522/#523/#524, `c1440c7`, ~$19.50) — both delivered.
- The `422bfac` skill-doctrine fixes (inert TRX zero-guard, covers-* census, comment-strip, split rule).
- **#458 still open** — `AiMergeResolver.cs:86` is `conflictedFiles[0]`.

--- PLAN 28 — #223 DELIVERED, and the epic's blocker is gone ---
- **31/31 green, $160.24**, merged `8ac346c`, #223 closed. Ten runs over one night.
- Tier spend: easy $2.06 (haiku x6) / medium $101.96 (sonnet x44) / hard $51.44 (opus x11).
- Ships: the `PromptRole` seam + role gate, the shared lenient `PromptJsonExtractor`, the adversarial
  loopback `FakeOpenAiServer`, tool containment, the `openai-compat` block schema AND its loader binding,
  runner transport + tool loop, kind-aware dispatch, GR2065–GR2067 action-reachability, the endpoint
  preflight, `providers check`, judge cost/usage, and the SSOT + skill-mirror records.
- **v1 is `OpenAiCompat` ONLY.** `Local`/`Codex`/`OpenRouter` stay reserved and stay GR2044 errors.
- **PR #555 (`940746c`, another session)** added the `apple-fm` engine hint on top.

--- SEVEN HARNESS BUGS, ALL FOUND BY RUNNING IT ---
Fixed and merged this session:
- **#550** — a re-driven attempt that PASSED was discarded ("the ORIGINAL settle stands"), never
  journaled, task left non-terminal `running`, dependents blocked with "did not succeed" about a task
  that succeeded. In worktree mode the succeeded record is written by the SCHEDULER's settle, so
  bypassing the worker loop dropped it entirely — one cause, three symptoms. Fix adopts a green re-drive
  only (asymmetric, so §6's "never faults the run" holds).
- **#551** — the overwatcher discarded EVERY verdict wrapped in a ```json fence. Two paid diagnoses were
  dropped on plan 28; one had independently diagnosed #550, the other found task 20's missing `maxTurns`.
  Fixed, then **superseded by plan 28** exactly as its disposition note asked: `Unfence` deleted, routed
  through the shared extractor; the noverdict body-logging (marked PERMANENT) survived.
- **#532 gap 2** — provenance only on SUCCEEDED attempts. 16 attempts split 7 with / 9 without, every one
  of the 9 a failure, **$21.61 of $37.90 unattributable**. Threaded through the five journaler methods
  reachable from `RunAttemptAsync`; deliberately NOT the two that carry no `CostUsd`.
- **#547** — the test suites wrote into the operator's REAL corpus. 719 fixture rows in two hours while
  the real $39.91 run contributed ZERO. Module-initializer redirect. Corpus then cleaned SELECTIVELY:
  1138 → 419, 0 unknown, backed up first.
- **corpus concurrency (`7495ba1`)** — `File.AppendAllText` holds no lock, so concurrent appends TORE
  JSONL lines (`'}' is an invalid start of a value`). NOT test-only: two concurrent `guardrails run`
  invocations corrupt the real corpus. Writers now exclusive+retry, readers ReadWrite+retry,
  `AlreadyRecorded` skips unparseable lines so an already-torn corpus stays ingestable.
- **#339 flake** — `timeoutSeconds` is a WHOLE-ATTEMPT budget; 3s made a trivial existence check
  load-sensitive. 3 → 20.
- **macOS port test (`0db4070`)** — asserted synchronous port release; now polls to a 5s deadline.

--- RELEASE ---
- **v1.12.0 cut and installed** (84 commits since v1.11.0, including all of Phase 0 telemetry).
- Skills reinstalled at v1.12.0 — the harness warns when they drift, and #169 stamps at INSTALL.

IN PROGRESS: What's partially done

- **#545** — parts 1 and 2 shipped (three-way drift prompt, actionable decline). **Part 3 NOT done:**
  *warn when a plan folder is edited during a live run.* Every one of the seven plan-28 escalations was
  fixed by editing the plan folder, each time hand-sequenced AFTER the run exited to dodge this hazard.
- **#532** — gap 2 shipped. **Gap 1 NOT done:** a `needsHarnessWrite` still leaves no trace.
- **Design 29 (#524/#528)** — still written, still not broken down.

NEXT STEPS: What should happen next

1. **THE AGREED NEXT PLAN — approved, not yet authored.** A release plan covering **#554 + #553 + #545
   part 3**, to be run **unattended through the harness** as a dogfood. Order: #554 first (cheapest fix,
   largest measured cost), #553 second (prevention beats recovery), #545 pt 3 third.
2. **#544 (v1.5 — task ACTIONS on openai-compat)** is gated on `providers check` meeting REAL hardware.
   Plan 28 §10 names this; the runner has only ever met the adversarial fake.
3. **#549** — `--dry-run` reports the wrong runner on tiered plans (`DryRun.ResolveRunner` never calls
   `TierResolver`); visibility, not spend.
4. **#552** — the log server is gated on a TTY, so headless/backgrounded runs can never have one.
5. **#528** unblocked now that #532 gap 2 landed.
6. Remaining priority:high, unchanged: #490, #478, #474, #471, #453, #382, #378, #375, #511, #496, #527,
   #540.

KEY FILES: Important file paths (relative to repo root)

- `docs/plans/28-local-inference-runner.md` + `docs/plans/28-local-inference-runner/` — **DELIVERED**.
  §13's implementation-handoff table is the artifact #553/GR2068 is built to check.
- `docs/plans/29-model-visibility-ux.md` — #524/#528, still not broken down.
- `docs/plans/30-telemetry-phase-1.md` — Phase 1 design of record (#548), cut from Phase 0's first REAL
  report. Reorders the charter: provenance-on-failures first, not turns-used.
- `docs/plans/17-model-tiering.md` — the DoR, revision 6.
- `src/Guardrails.Core/Execution/AttemptJournaler.cs` — 9 `AttemptRecord` sites; 7 now carry provenance.
- `src/Guardrails.Core/Execution/Scheduler.cs` — `SettleGreenIfWorktreeAsync` (extracted for #550).
- `src/Guardrails.Core/Execution/TaskExecutor.cs` — the `needsHuman` short-circuit that skips salvage
  (**#554**); the preserve helper is retry-only.
- `src/Guardrails.Core/Telemetry/TelemetryCorpusStore.cs` — `AppendLineExclusively` /
  `ReadLinesTolerantly`.
- `src/Guardrails.Cli/Commands/DryRun.cs:187` — `ResolveRunner`, the #549 defect.
- `src/Guardrails.Cli/Commands/RunCommand.cs:300` — `bool live = …`, the #552 gate.
- `src/Guardrails.Core/Loading/DiagnosticCodes.cs` — next-free **GR2068** (reserved for #553).
- `~/.guardrails/telemetry/` — 419 real rows, clean.

CROSS-REPO: Any work that touches other repos

- **Charter (`C:\DevAI\Charter`) — #172/#173 still OPEN.** #172 remains the Charter-side blocker for #496.
- The mailbox convention holds: `C:\DevAI\_mailbox\{charter,guardrails}\OUTBOX.md`, outside both repos.
  The lane rule holds — file issues and answer in place, never commit in another team's repo.

BLOCKERS/NOTES: Issues or context for the receiving session

- **UNREACHABLE WRITESCOPE IS THE DOMINANT FAILURE MODE — five instances in one plan.** A task is told to
  deliver an outcome its write surface cannot reach; it burns attempts discovering this, then escalates.
  Tasks 01, 09, 10, 15, 27. **#553/GR2068** exists to catch the statically-decidable subset: every path
  the plan's §13 handoff table names must appear in some task's `writeScope`, and the tasks implementing
  a row must COLLECTIVELY cover that row.
- **BUT #553 DOES NOT SUBSUME #554.** Only 4 of 7 escalations were writeScope-shaped. Task 05 was a
  design contradiction; task 28 was a self-refuting guardrail. #553 reduces FREQUENCY, #554 reduces UNIT
  COST. Both defenses are written into both issues so triage cannot conflate them.
- **#554 IS THE TOKEN-WASTE ITEM.** Retry salvage (#195/#306) works — but the `needsHuman` short-circuit
  returns before the preserve helper, so an agent that COMPLETES work and then escalates loses it, on
  exactly the path where a human is about to unblock and resume. Measured: **$22.02 discarded across six
  tasks**, plus $19.85 on the attempts that followed.
- **A "BUDGET GAP" IS OFTEN A DISGUISE.** Task 28 hit max-turns because it re-derived work #554 had
  thrown away. Task 20 and task 28 both looked like capability failures and were budget; task 30 looked
  like capability and was TOOL DISCIPLINE (Bash `grep` is not granted, so every search was refused and
  repeated refusals tripped the permission-wall detector). Check the tool census in `claude-stream.jsonl`
  before bumping a tier.
- **THE PERMISSION-WALL MESSAGE LIES.** A refused read-only `grep` is reported as *"write repeatedly
  refused (permission wall)"*. Seen on task 20 and task 30.
- **WRITE DISPOSITION COMMENTS WHEN A FIX HAS TWO HALVES.** #551 landed one TEMPORARY change and one
  PERMANENT one; the disposition notes are why plan 28 removed the first and kept the second. Without
  them, "clean up the #551 stuff" takes the forensics too.
- **CLOSE THE ISSUE.** `fix(#547):` is a conventional-commit SCOPE, not a GitHub closing keyword — four
  issues sat open for a day with their fixes merged. Use `Fixes #NNN` or close explicitly.
- **Unattended runs work, with a hard boundary:** never make a task green by weakening a guardrail,
  deleting a test, or narrowing an assertion. When the only path required that (task 05), the run
  stopped and asked. `--autonomous` + `--max-cost-usd` + `--no-merge-on-success` is the shape that worked;
  the cost cap is cumulative across resumes and needs raising as a per-run override, never in
  `guardrails.json` mid-run.
- **A plan-only commit CANNOT break a test.** Two CI failures on `docs/plans/**`-only commits were the
  tell that pointed at a shared-resource race, not at the commits.
- **`git tag` and `git push origin <tag>` must be SEPARATE Bash calls** — unchanged.
- **The four-folder model, `PlanHash` vs `PlanDefinitionHash`, #164** (fragment key = task folder name),
  and `workspace: "../../.."` all still apply exactly as before.
