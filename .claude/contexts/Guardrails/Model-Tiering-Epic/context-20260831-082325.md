REPO: C:\DevAI\Guardrails
TOPIC: Guardrails/Model-Tiering-Epic

CONTEXT: What we were working on

**Plan 31 is authored, broken down, reviewed, and READY TO RUN — but the run has NOT started.**
The session stopped deliberately at a durable point (laptop shutdown), with everything committed
and pushed. Nothing is in flight.

Plan 31 covers **#554 + #553 + #545 part 3** — the three defects plan 28's first unattended
overnight run exposed. The user's standing instruction for the next session is explicit:

> "carry that plan all the way through. If the harness stops, then fix and rerun in a loop until
> all is CI green and merged into master."

**IMMEDIATE STATE:** master at `f43c7b2`, working tree clean, everything pushed. Tool + skills at
v1.12.0. Local suite green (Core 2123, Integration 1036 + 4 skipped); master CI green at `1a027e8`.

NEXT STEPS: What should happen next

1. **START THE RUN.** The exact command, decided and ready:

   ```
   guardrails run docs/plans/31-unattended-run-hardening --autonomous --max-cost-usd 120 --no-merge-on-success
   ```

   - `--no-merge-on-success` is DELIBERATE here: the finished work goes to master **through a PR**
     so CI gates it before it lands, which is what "CI green and merged into master" actually
     requires. Delivery is NOT automatic — expect the loud `*** WORK NOT DELIVERED ***` banner
     AFTER the green summary, and check `git branch --no-merged master` before believing it shipped.
   - `--max-cost-usd 120` is a starting cap. It is **cumulative across resumes** and needs raising
     as a per-run CLI override, NEVER edited into `guardrails.json` mid-run.
2. **On a halt:** diagnose from the logs, fix **after the run exits** — never while it is live.
   Editing a live plan folder is the exact hazard #545 pt 3 exists to warn about, and #556 means a
   mid-run `task.json` edit produces a silent false green no resume can detect.
3. **After green:** push the plan branch, open a PR, wait for CI, merge. Then confirm master CI.

COMPLETED: What got done this session

- **Design of record** `docs/plans/31-unattended-run-hardening.md` (1099 lines), committed `1490d2a`.
- **Task folder** — 10 tasks in 3 independent tracks, committed `e48eebd`.
- **Adversarial review + fixes**, committed `f43c7b2`. Review recorded via `mark-reviewed`
  (planDefinitionHash `sha256:2aa19e55f85d`). `validate` exits 0 with NO warning.
- **Three issues filed** for defects the design surfaced but deliberately does not fix:
  **#556** (definition hash stamped from disk at settle, not from the bytes executed — a mid-run
  `task.json` edit is a silent false green), **#557** (JIT wave breakdown holds plan-wide write
  authority against a wave-scoped revert), **#558** (no way to look up a GR code; the SSOT mirror
  is incomplete by 16 codes and wrong about GR2013/GR2014).

KEY DECISIONS: Do not re-litigate these

- **GR2068 and GR2069 are TWO codes, both WARNING.** GR2068 = a handoff path no task can write;
  GR2069 = a row split across tasks. Split so the noisy half can be muted without silently taking
  the precise half with it. WARNING not ERROR because at ERROR this refuses `guardrails run` on
  every existing plan, and a correct shipped plan can carry a stale table cell.
- **BOTH of #553's motivating failures are GR2069, not GR2068** — in each case the path was
  reachable somewhere in the plan, just not by the task that owned it. So GR2069 carries 100% of
  #553's value and GR2068 is a stale-path lint. Counterintuitive; stated in the plan in 3 places.
- **Glob matching reuses `IsInScope` with arguments SWAPPED.** No new primitive. Strict containment
  would false-fire on plan 28 row 6, where seven tasks legitimately write concrete files under an
  open glob.
- **The plan-edit warning rides the existing `DecisionRecorded`**, which already reaches the live
  table, the `--no-ui` stream and both transparent decorators. A new observer event would fail
  silently via its default no-op body.
- **The wiring stage was SPLIT** (08 watch / 09 wiring) over the plan's original nine stages —
  it tripped both Step 2 over-size triggers and was #378's fan-in-sink fingerprint.
- **One `Rebaseline()` per CALL SITE, not per writer** — `SweepIncompleteTrailingTaskFolders` fires
  from two Scheduler sites (`:1484`, `:1999`).
- Task 10 stays tier `medium` — see if that is wrong from real evidence rather than pre-buying opus.

BLOCKERS/NOTES: For the receiving session

- **THE ADVERSARIAL PASS PAID FOR ITSELF, TWICE.** The design review found 5 blockers, two of which
  would have reproduced plan 28's dominant failure INSIDE the plan built to prevent it. The folder
  review found a blocker by MUTATION that reading missed: a guardrail that reported *"pins 1 and 2
  both assert GR2069"* while the assertion said GR2068 — and task 05 could not have corrected it,
  only conformed, shipping inverted semantics on a green run. **Run the pass with a NON-authoring
  agent; that is what made both work.**
- **Known-unguardable gaps, carried deliberately** (all named in prompts, none faked): §4.9 pins 6/7
  must assert on the FULL diagnostic list, and pin 5a must FAIL under the un-swapped argument form —
  both are properties of the SHAPE of an assertion. Four of §5.3's five re-baseline hooks have no
  mechanical proxy (all five writer symbols already appear in `Scheduler.cs`, so a presence clause is
  vacuous; a count floor of five would dead-end a legitimate helper-based implementation).
- **Fixture realism on task 04 is unguarded.** An invented row that yields GR2069 passes both the
  census and the keying guardrail. The likelier defect is a fixture using ABSOLUTE paths that never
  exercises the suffix arm — the arm row 7 actually needs.
- **Plan 28's leftovers still exist** — ~10 worktrees under `%TEMP%\gr-wt\0dec4adf\` and ~15
  branches. Harmless (a new run gets its own root); they are the orphaned trees behind #554's W1.
  Zero salvage refs, consistent with #554: the six escalating attempts left nothing behind.
- **`git tag` and `git push origin <tag>` must be SEPARATE Bash calls** — unchanged.
- Remaining priority:high backlog, unchanged: #490, #478, #474, #471, #453, #382, #378, #375, #511,
  #496, #527, #540, plus the new #556/#557.
