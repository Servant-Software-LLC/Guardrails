REPO: C:\DevAI\Guardrails
TOPIC: Guardrails/Model-Tiering-Epic

CONTEXT: What we were working on

**Stage 3 is merged (`3bcfdbf`) — the prior session's open merge is DONE.** This session delivered the
two plans that came out of the Stage 3 dogfood: **plan 26 (#510)** and **plan 27 (#522/#523/#524)**,
both green and merged. It also wrote the design of record for **#223 local inference** — the item the
prior context named as the epic's real blocker — and produced **sixteen filed issues (#525–#541)**,
nearly all found by running the harness rather than reading it.

**IMMEDIATE STATE:** master at `d40d6bb`. Two design docs committed and NOT yet broken down:
**28 (#223)** and **29 (#524/#528)**.

> **CORRECTION, 2026-08-30.** This file originally said "Both plan branches merged via `mergeOnSuccess`."
> That was **false for plan 27**: the run went wholly green and `mergeOnSuccess` never delivered, leaving
> 11 commits on `guardrails/27-operator-visibility` for a day while everything downstream reasoned from a
> master that did not have them. Merged by hand at `c1440c7` (clean; Core 2023/2023, Integration 922
> passed / 4 skipped). The silence is filed as **#542** — `run.json` has no field for the delivery at all.
> Plan 26's merge (`6efded9`) was real. **Verify with `git branch --no-merged master` before believing a
> green run shipped.**

COMPLETED: What got done

--- Everything through Stage 3 — carried forward, unchanged ---
- Stage 1 + 1.5 + workstream B (v1.6.0); #457/#451 (**#458 still open** — `AiMergeResolver.cs:86` is
  `conflictedFiles[0]`, so a union conflicting in 2+ files can never be AI-resolved). PR #342 merged.
- Stage 2 complete and merged: 3 waves, 34 tasks, $229.50. DoR at revision 6.
- v1.7.0–v1.10.0 and the nine dogfood-found harness fixes (#504/#507/#508/#512/#513/#514/#516/#517).
- **Stage 3: all five waves green, and now MERGED to master at `3bcfdbf`.** The prior context's
  IN-PROGRESS merge (clean text merge + verified build) completed.

--- PLAN 26 — #510 DELIVERED (the gap the prior context called out) ---
The prior note read: *"nothing ever executes a committed sample pair, so guardrail-quality proof is
100% prompt and 0% gate."* That is now shipped. `SampleVerifier` is on master, driven by both a
`guardrails samples verify` verb and the pre-DAG preflight phase.
- 6 tasks, **$21.77**, merged as `6efded9`. Re-cut out of an abandoned 5-issue bundle (plan 25, whose
  superseded doc is kept with a banner).
- `/guardrails-review` found **1 BLOCKER**: task 04 authored its own tests AND its implementation, so a
  hollow file (4 x `Assert.True(true)` plus one real call that passes against an UNWIRED phase, because
  `PlanPreflightPhase.cs:52` returns true when `PlanPreflights.Count == 0`) exited **0**. Reproduced by
  hand, then fixed by splitting into author-tests then wire; the mutant afterwards failed 4/4 with the
  discriminator row correctly exempted.

--- PLAN 27 — #522/#523/#524 DELIVERED ---
- 9 tasks (8 plus a mid-flight split), all green, all four terminal gates exit 0, `planGuardrails:
  passed` — but **NOT delivered by the run**; merged by hand at `c1440c7` the next day (see the
  correction at the top, and #542). #522 and #523 are now closed with the evidence on them.
- `/guardrails-review` found **2 BLOCKERs**: a collapsed task with no behaviour pins, and a Model column
  that an inert `AttemptRouteResolved` handler could satisfy while shipping design 29 section 1.1 verbatim.
- New `IRunObserver.AttemptRouteResolved`, raised PRE-LAUNCH — `AttemptModelResolved` fires only AFTER
  the action, so without it the column would read `(medium)` for the 14–22 minutes a task is running.

--- SKILL DOCTRINE FIXES (`422bfac`, +392/-14) — the leverage item ---
Four defects pushed from per-plan fixes into `plan-breakdown` + `guardrails-review`:
1. the inert TRX zero-guard (`@($null).Count` is **1**, so `-lt 1` never fires) plus the rule that a
   zero-match guard must be PROVEN to fire;
2. a `covers-*` naming floor with no per-test red census;
3. the comment-strip doctrine was source-only, so a `<!-- comment -->` satisfied doc guardrails (with
   the counter-rule that fences are NOT stripped);
4. a task authoring both its own tests and its own implementation must split (with the
   declared-exemption companion rule).
**Three of the four had live instances in plan 27**, whose tasks were authored at `cec5627`, before the
fix landed. Task 05, authored after, came out split-correct on its own.

--- DESIGNS OF RECORD (committed, NOT broken down) ---
- **`docs/plans/28-local-inference-runner.md`** (`2cb305a`, #223) — **v1 is a VERIFIER, not an actor**:
  Guardrail + Advisory roles only, read-only tools, human pin. Its own adversarial pass found 2 blockers
  in the first draft and it was rewritten.
- **`docs/plans/29-model-visibility-ux.md`** (`fed27d5`, #524/#528) — a 4th column carrying the runner
  BLOCK NAME at `Width(8)`, measured through real Spectre. The glyph-plus-legend counter-proposal was
  rejected on measurement: at 80 cols a 1-char glyph column costs the SAME as an 8-char word column
  (both 14 lines). #528's counterfactual: hard rung = **$0.9331/Mtok**, giving **$14.13 projected vs
  $8.2211 actual (~42% avoided)** — and it must be labelled a PROJECTION and built from ALL attempts.

IN PROGRESS: What's partially done

- **Designs 28 and 29 are written but not broken down.** Per #106 the next step for #223 is a branch
  plus a draft PR for inline review, not a straight `/plan-breakdown`.
- **#528 is blocked at the data layer by #532** — provenance is journaled ONLY on succeeded attempts, so
  a savings projection can price only the successes, which is exactly the flattering bias #528 forbids.

NEXT STEPS: What should happen next

1. **#223 local inference — the Mac Studio critical path.** Hardware arrives ~Sept 2026. Design is
   committed; #223 is the ONLY hard blocker (today just `PromptRunnerKind.Claude` has a concrete runner;
   `Local`/`OpenAiCompat` are reserved names that VALIDATE CLEAN and then throw at registry
   construction). Tiering itself is NOT the gap — the resolver already routes to a block; the runner
   does not exist.
2. **#532 before #528** — journal provenance on every attempt (the route resolves at
   `TaskExecutor.cs:648-654`, before the action, so it is known even for a failed attempt).
3. **#527 turn budgeting** — `maxTurns` is budgeted by ARCHETYPE, not by enumerated work, and nothing
   re-derives it when a review round adds work. Measured twice in one day.
4. **#540 two-mechanism tasks** — a `writeScope` mixing `.claude/**` with a normal repo path is
   decidable statically and selects exactly the failing task in plan 27.
5. **#511 provider wait-and-poll** — dropped from the re-cut (two isolated tasks, no plan warranted),
   still open, still the best reliability fix for long unattended runs.
6. **#496 auto-review invoker** — unblocked now the `RunCommand.cs` merge has landed.
7. Remaining priority:high, unchanged: #490, #478, #474, #471, #453, #382, #378, #375.

KEY FILES: Important file paths (relative to repo root)

- `docs/plans/17-model-tiering.md` — the DoR, revision 6.
- `docs/plans/28-local-inference-runner.md` — **#223 design of record (new)**.
- `docs/plans/29-model-visibility-ux.md` — **#524/#528 UX design of record (new)**; its section 7 lists
  exactly what it changes in plan 27 (tasks renumbered, banner added).
- `docs/plans/26-guardrail-quality-gate/` and `docs/plans/27-operator-visibility/` — both DELIVERED.
- `docs/plans/25-backlog-slate.md` — SUPERSEDED banner; its folder is deleted but recoverable at
  `cec5627`, which is also where the dropped #511 task chain lives.
- `.claude/skills/plan-breakdown/**` and `.claude/skills/guardrails-review/SKILL.md` — the `422bfac`
  doctrine fixes. **Repo-source skills are deliberately UNSTAMPED**: `metadata.guardrails-version` is
  written at INSTALL time (#169), so never hand-author a version there.
- `src/Guardrails.Core/Model/PromptRunnerConfig.cs` — `PromptRunnerKind`; only `Claude` is real.
- `src/Guardrails.Core/Execution/HarnessWrite.cs:117` — reads `needsHarnessWrite` off the fragment ROOT.
- `src/Guardrails.Cli/Ui/LogSiteRenderer.cs:386,403` — the plan-27 Model column, ExportSite only.
- `src/Guardrails.Cli/PlanPreflightPhase.cs:52` — the vacuous-pass line that made plan 26's BLOCKER real.
- `src/Guardrails.Core/Loading/DiagnosticCodes.cs` — next-free **GR2065**.

CROSS-REPO: Any work that touches other repos

- **Charter (`C:\DevAI\Charter`) — #172/#173 still OPEN.** #172 remains the Charter-side blocker for #496.
- **The mailbox convention** (established this session): the wire lives at
  `C:\DevAI\_mailbox\{charter,guardrails}\OUTBOX.md`, OUTSIDE both repos and therefore branch-independent;
  the repo keeps the record under `docs/asks/archive/`, with a shared monotonic `seq`. Reason: branch
  switching kept stranding uncommitted mailbox prompts. The lane rule holds — file issues and answer in
  place in their repo, never commit there.
- A Charter session for a **"local telemetry"** feature was running in parallel. Plan 27 writes
  `docs/plans/02-schemas-and-contracts.md` and `.claude/skills/guardrails-domain-knowledge/SKILL.md`;
  a charter must not edit those two files, or the end-of-run merge collides.

BLOCKERS/NOTES: Issues or context for the receiving session

- **THE EPIC'S HEADLINE IS STILL INERT — unchanged, but now with a design.** Every tier still resolves to
  a Claude model. #223 has a design of record and no code. Do not read plans 26/27 as advancing the
  local-inference thesis; they advance the SAFETY that makes a weak model usable.
- **EXECUTION FINDS THESE, READING DOES NOT — proven five more times.** Both plan reviews' BLOCKERs came
  from running a mutant, not from reading. Four further defects (#534/#538/#539/#540/#541) stayed
  invisible to a review pass that had already found two BLOCKERs.
- **THE REVIEW HAS A BLIND SPOT WORTH KNOWING.** Every probe asks about guardrail strength or DAG edges.
  **None asks "can ONE attempt deliver all of this through the write mechanisms available to it?"**
  (#540). And the #193 orphaned-golden probe only searches INSIDE task filters, so a golden that no
  filter selects broke plan 27 at its own preflight (#541). #455's push toward narrow filters makes #541
  MORE likely, not less.
- **PLAN 27 TASK 08 IS THE CASE STUDY.** 12 attempts, ~$5.22, never green — then split by write
  mechanism and both halves passed first try. The measured proof: attempts 7/8/9 passed the SSOT half,
  10/11 passed the SKILL half, **never both together**, and **all exited 0** (so not a max-turns
  ceiling). Not capability and not budget: an atomic attempt cannot bank the half it got right.
- **`needsHarnessWrite` goes at the fragment ROOT**, a sibling of the folder-name key — nesting it under
  that key (which the harness's own header demands for state) is silently ignored (#531; fixed in the
  prompts, the harness still reads root only).
- **A `.claude/` deliverable has an INSTALLED twin** at `~/.claude/skills/<name>/SKILL.md` that is
  visible to the agent's own runtime, and the two differ by the install stamp (#539).
- **A zero-match result is not a measurement until a POSITIVE CONTROL proves the search reached the
  file.** Committed this exact error mid-session: `git show "<branch>:<path>"` under Git-Bash mangles the
  path, `2>/dev/null` swallowed the `fatal:`, `grep -c` on empty input returned 0, and four zeroes read
  as "token absent". Use `git show <branch> -- <path>`.
- **Cost this session: ~$21.77 (plan 26) + ~$19.50 (plan 27).** A breakdown still costs ~$10 regardless
  of plan size. Caps are runaway-guards, not budgets — override with `--max-cost-usd`, never by editing
  `guardrails.json` mid-run (that changes `PlanHash`, which `state.json` is keyed on).
- **`workspace` defaults to `".."`**, which from `docs/plans/<name>/` is `docs/plans/`, NOT the repo root
  — masked entirely by worktree mode (#526). Both new plans set `"../../.."` explicitly.
- **`git tag` and `git push origin <tag>` must be SEPARATE Bash calls** — unchanged.
- **The four-folder model, `PlanHash` vs `PlanDefinitionHash`, and #164** (a state-out fragment key MUST
  equal the task folder name) all still apply exactly as before.
