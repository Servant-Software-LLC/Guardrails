REPO: C:\DevAI\Guardrails
TOPIC: Guardrails/Model-Tiering-Epic

CONTEXT: What we were working on
**Two releases shipped and the entire priority:high backlog cleared.** The session opened on the
Stage 2 follow-ups (#464/#468/#470/#462/#463/#485), cut **v1.7.0**, then swept every high-priority
issue, cut **v1.8.0**, and installed it. Master is `941a646`, tagged `v1.8.0`, pushed, 3-OS CI green.

**THE NEXT STEP IS THE #201 CHARTER PLAN** — a new waved, JIT-broken-down charter for the epic's
next stage. Everything below about JIT durability exists because that charter will run on it.

COMPLETED (carried forward + this session):

--- Stage 1 + 1.5 + workstream B (v1.6.0) — unchanged, see prior contexts ---
- #457/#451 UTF-8 at every git boundary; #451 remains DETERMINISTIC (`AiMergeResolver.cs:86` is
  `conflictedFiles[0]`, so a union conflicting in 2+ files can never be AI-resolved — still open as #458).
- Stage 1.5 schema reconciliation (the DoR wins); `guardrails providers init`; PR **#342 MERGED**.

--- Stage 2 COMPLETE and merged (prior session) ---
- 3 waves, 34 tasks, $229.50. DoR at **revision 6** (D30/D31/D32). Judge object = 8 members on
  `AttemptProvenance` (D32 — the only member reaching BOTH serial and worktree construction paths).
- #475 (`AttemptRecord.Usage` assigned by none of 12 sites) and #483 (actor half-routed) both closed.
  **A correctly-wired JUDGE is what made the broken ACTOR observable.**

--- v1.7.0 (2026-08-20) ---
- **#464 macOS gate CLOSED.** `WorktreeContainmentHook` now bakes a LIST of accepted root spellings
  (as-given + `RealPath.Resolve`). Also fixed a real quoting hole (`$` interpolation in a path like
  `C:\build\$tmp\wt`). `LinkedTree` promoted to a shared fixture; mutation-verified (dropping the
  resolved entry reddens 4 new tests, all 24 pre-existing stay green). Inverse direction proven
  unreachable by tracing every producer of `WorktreeHandle.WorktreePath`.
- **#468** source-shape guardrails demoted below behavioural proof; **#470** doctrine + **GR2057**;
  **#462** GR2037 registry entry + **#463** Stage-1 exemplar repair; **#485** needs-human `kind`.
- **#487** (regex timeout CRASHED validate) filed and fixed same day.

--- v1.8.0 (2026-08-21) — ALL 16 priority:high issues closed ---
- **JIT breakdown made usable**: #385/#402 (checkpointed authoring — the prior "fix" `39e4510` raised
  a turn cap that was NEVER BINDING; both truncations stopped at the 30-min `BreakdownTimeout` and the
  completing run used 35 turns), #471 (quarantine scope, inventory-scoped revert protecting
  hand-authored gates by PROVENANCE), **#489** (Ctrl+C skipped quarantine entirely), **#469** (the
  harness rendered a 30-minute authoring window as a FINISHED run — no observer event fires before
  `WaveStarting`, and a zero-task wave contributes no rows), **#477** (`intendedWaves` + GR2062).
- **#472/#488/#471(2) were ONE fix**: the per-wave review marker SSOT §13 already specified and nobody
  built. Every SUCCESSFUL JIT breakdown was staling the previous wave's attestation. Reuses the
  shipped `WaveDefinitionHash` — no fourth hash.
- **#452** overwatcher was a silent, billed no-op (its own Bash calls denied); **#461** drift warning
  fired at the wrong granularity and had over-corrected into TOTAL SILENCE about 3 stale installs;
  **#429** two shipped rules could not both be satisfied; **#442** child env not hermetic; **#459**
  GR2059.
- **New lints**: GR2059, GR2062, GR2063, GR2064. Suite **1733→1898 Core, 824→870 Integration**.

--- Six designs of record, docs 18–23 (all committed) ---
- **18** integration-proof proximity (#382) — four seam buckets, the **N4 trap** (*fake the WAIT, never
  the WAITER*), one-real-level + induction, T\*, join-check. M1/M2/M3 shipped.
- **19** producer coverage (#474/#477) — Milestone A skill half + Milestone B shipped; **GR2060 NOT built**.
- **20** JIT breakdown durability — durability half + attestation half both shipped.
- **21** run-fault triage (#453) — designed, NOT implemented.
- **22** run-authored test trust (#375) — M1/M2 shipped (per-test red census, operator 21); **M3 not built**.
- **23** JIT breakdown visibility (#469) — implemented.

--- Backlog triage (durable) ---
- **All open issues labelled** priority:high/medium/low + type. 63 open now.
- Closed as already-shipped-but-never-closed: **#484** (GR2055), **#473** (GR2056).

--- Tooling state (2026-08-21) ---
- Global tool **1.6.0 → 1.8.0**; skills reinstalled at v1.8.0 (`guardrails skills install --force`,
  user scope). **Verified by DIFF against repo source** — only difference is the 2-line
  `metadata: guardrails-version` block #169 injects at install. Drift warning clears.
- **RESTART Claude Code** — the `/plan-breakdown` and `/guardrails-review` loaded in the saving session
  are pre-update copies.

IN PROGRESS:
- Nothing mid-flight. Working tree clean, everything pushed, `v1.8.0` published to NuGet.

NEXT STEPS:
1. **The #201 charter plan for the epic's next stage** — the stated goal. It will be waved + JIT
   broken down, which is the first REAL exercise of everything in v1.8.0.
2. Remaining priority:high (8, all deliberate): **#490** (doctrine reaches the prose but not the three
   surfaces), **#478** (per-clause residual), **#474** (headline is permanently review-only by design),
   **#471** (part 1 shipped; confirm what remains), **#453** (doc 21, M1 = deterministic capture, zero
   AI spend, stands alone), **#382** (M3 shipped; headline open), **#378** (GR2042 shipped; residual is
   the relocation-first pointer), **#375** (M3 = the `reviewAuthoredTests` halt).
3. Cheap mediums deferred FROM v1.8.0 deliberately, to protect the coherence gate: **#431**
   (copy-pasteable run command), **#438** (pwsh on PATH in Step 0), **#354** (waved breakdown
   discoverability). All three would improve the charter experience.
4. **#492** — fixing #488 opened its MIRROR: once waves carry their own markers, a PLAN-ROOT gate edit
   re-stales nothing. Pinned in both directions by a test, so bounded and visible.

KEY FILES:
- `docs/plans/17-model-tiering.md` — the DoR, revision 6. §6 is Stage 2's spec; §16 the decision registry.
- `docs/plans/18-…` through `23-…` — the six new designs. Each carries a v1/deferred split AND the
  evidence that would falsify it.
- `docs/plans/02-schemas-and-contracts.md` — SSOT. **`eol=lf`-pinned** in `.gitattributes` and
  byte-compared by `SchemaDriftTests`; normalise back to LF after any editor touch.
- `src/Guardrails.Core/Loading/DiagnosticCodes.cs` — next-free **GR1011 / GR2065**. GR2060/GR2061
  reserved by name. The two ladders advance INDEPENDENTLY.
- `src/Guardrails.Core/Loading/BreakdownIntent.cs` — the intent-manifest READER is the CONTRACT.
- `tests/Guardrails.Core.Tests/SeamDoctrineAnchorTests.cs` — 30 normative clauses pinned across four
  skill files. This is what makes #382 doctrine enforceable.

CROSS-REPO:
- Charter repo `C:\DevAI\Charter` (0.21.0). Open there: #152, #156, #157, #158.
- Guardrails' `plan-breakdown` cites Charter's `charter-format` as normative — a Charter skill governs
  a Guardrails skill's correctness and neither `--version` sees the pairing (Guardrails #466, Charter #152).
- **The lane rule**: fixes go to the session that OWNS the repo receiving the commit.

BLOCKERS/NOTES:
- **THE DOMINANT DEFECT CLASS, and it recurs in our own same-day fixes**: a mechanism that fails
  SILENTLY in the direction that LOOKS FINE. GR2059 shipped in the morning and four skill commits
  landed after it without propagating it — the catalogue spent the day instructing authors to do the
  exact thing that lint warns is inert. A timing assertion shipped hours earlier measured **3 seconds**
  from passing for the wrong reason. Two design docs contained statements FALSE WHEN WRITTEN.
- **The two habits that catch it**: (1) read `git log` for the issue number BEFORE designing — FOUR
  issues this sweep were already shipped, two of them ineffectively; (2) run the adversarial pass with
  an agent that did NOT author the work (#467 applied to ourselves — the skills coherence audit found
  a release blocker precisely because it went to `guardrails-devils-advocate`).
- **THE JIT PATH HAS NEVER BEEN RUN since these fixes.** Everything is unit+integration tested; the
  charter's first waved breakdown is the first real exercise. A finding there is a finding, not a surprise.
- **The salvage depends on `plan-breakdown` writing `state/breakdown-intent.json`.** Harness half is
  complete; if the skill does not emit it, a truncation still quarantines wholesale. **GR2064** now
  reports a manifest that is present but unusable (a typo used to silently cost the whole salvage).
- **Empirical vs STRUCTURAL false-positive zeros.** GR2055/2056/2057 earned an empirical zero across
  500+ real scripts. **GR2062/GR2064 have only a STRUCTURAL zero** — nothing in the corpus can carry
  those fields yet. Do not let a later lint cite the wrong precedent.
- **#382's folder-observable half is weaker than its design assumed**: nothing in a plan folder declares
  WHICH SEAM a guardrail proves, and the two tells (a filename, `(passing-but-blind)`) are only an
  example and a template. `/guardrails-review`'s "re-derive from the folder" fallback hits the same
  wall. Recorded in doc 18 §3.4 as evidence toward GR2061.
- **`guardrails skills install --project --force` now REFUSES** when tracked+dirty (#461). Still
  proceeds when tracked+CLEAN (recoverable via `git checkout --`), and an UNTRACKED skill folder is
  still unprotected. Plain `--force` (user scope) remains the right command here.
- **NEVER author regex-bearing scripts via bash/Python heredocs** — doubled backslashes collapse in
  transport, `\b` becomes a literal 0x08, and the clause silently stops matching. Use the file-write tool.
- **Do NOT run `guardrails skills install --project --force` in this repo** — `.claude/skills` is
  TRACKED SOURCE and the input the tool packs from.
- **Dogfooding = the user runs the harness.** Hand over the exact copy-pasteable command line unprompted.
- **`mergeOnSuccess` defaults ON** — a green run delivers. Use `--no-merge-on-success` to inspect first.
- **Measured timings** (warm): Core ~1–2m, Integration ~7–10m. A plan preflight runs in a FRESH worktree
  with no `bin/obj`, so realistically 20–30 min before the DAG starts.
- **Maintainer rulings not to re-litigate**: `costly` is TRI-STATE · difficulty maps to a candidate SET,
  not to strength · a comment a human DELETED must not come back · hand-code anything needing network
  calls · **the DoR wins** over any plan text · `dial:critical` + `proceed-unreviewed` is FORBIDDEN.
