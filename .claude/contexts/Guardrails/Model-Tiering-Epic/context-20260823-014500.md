REPO: C:\DevAI\Guardrails
TOPIC: Guardrails/Model-Tiering-Epic

CONTEXT: What we were working on

**Stage 3 was authored, reviewed, adversarially re-reviewed, remediated, and RUN — wave 1 is merged.**
The session took the #201 arc from "charter the next stage" through a real `guardrails run`, and the
run's wave-2 truncation exposed **#501**, a defect in the #385/#402 salvage that no test could see.
A second thread built **`guardrails breakdown`** (#498, merged) after measuring that the wave-1-stub
workaround for the unattended epic does not work.

**IMMEDIATE STATE: on branch `fix/501-breakdown-salvage-gate` with UNCOMMITTED work** (see IN PROGRESS).

COMPLETED: What got done

--- Stage 1 + 1.5 + workstream B (v1.6.0) — carried forward, unchanged ---
- #457/#451 UTF-8 at every git boundary. #451 remains DETERMINISTIC: `AiMergeResolver.cs:86` is
  `conflictedFiles[0]`, so a union conflicting in 2+ files can never be AI-resolved — still open as #458.
- Stage 1.5 schema reconciliation (the DoR wins); `guardrails providers init`; PR **#342 MERGED**.

--- Stage 2 COMPLETE and merged (prior session) ---
- 3 waves, 34 tasks, $229.50. DoR at **revision 6** (D30/D31/D32). The judge object = 8 members on
  `AttemptProvenance` (D32). #475 and #483 closed.

--- v1.7.0 / v1.8.0 (prior sessions) ---
- #464 macOS gate; #468/#470/GR2057; #462/#463; #485; #487.
- JIT breakdown made usable: #385/#402, #471, #489, #469, #477. #472/#488/#471(2) turned out to be ONE
  fix (the per-wave review marker). Plus #452, #461, #429, #442, #459. New lints GR2059/2062/2063/2064.
- Six designs of record, docs 18–23.

--- STAGE 3 — authored, reviewed, DA'd, remediated, RUN, wave 1 MERGED (this session) ---
- **`docs/plans/model-tiering-stage-3.charter.md`** — authored via Charter 0.23.0, 6 `:::question`s,
  full review round drained (`charter-drain`), all 6 answered, 4 annotations actioned + replied to.
  Scope: **#229** (review-time model-appropriateness) + **GR2051/GR2052/GR2053** + **#349** (pilot seat).
- **The charter's load-bearing finding:** DoR §9.3 told Stage 2 to land `resolvedModel` "in the shape
  #349 specifies"; Stage 2 declined and wrote the refusal into `JournalModel.cs:401`. Two mechanisms
  contradicting each other, invisible from either side. **Settled:** `provenance.model` becomes
  best-known-actual, `requestedModel` written ONLY on mismatch. Amends §9.3.
- **`/plan-breakdown` → wave 1 authored (6 tasks), wave 2 a JIT stub.** `intendedWaves: 3`.
- **`/guardrails-review` self-pass found 2 of 4 BLOCKERs and mis-scoped both.** A
  `guardrails-devils-advocate` pass (user-directed — #467 applied to ourselves) found 2 more and upgraded
  3. **Every BLOCKER was found by EXECUTION, never by reading — reading was run twice and found none.**
- Remediation `9e93ea7` + re-review `23f3647`: census split into `$mustBeRed`/`$mustRunGreen` (silence
  assertions CANNOT be red before the feature exists — the census message was coaching the agent to
  destroy the Invariant-7 test); the reservation clause stopped inferring English tense; task 06's
  guardrail was simultaneously too strict on correct work and too loose on wrong work; wave-gate positive
  greps added; `writeScope` repointed to `ModelTiering/`; preflight repointed at the copy of
  `WaveBreakdownInvoker` that is actually inlined.
- **Stamped `review-artifact`**, `waveDefinitionHash be29ba066bda`, artifact under `state/reviews/`.
- **RUN: wave 1 6/6 GREEN, zero needs-human, $18.29.** Merged to master as **`7d567db`**:
  `DiagnosticCodes.cs +91`, `PlanValidator.cs +190`, `TieringRegistryWarningTests.cs +299`,
  `PinAndTierCoexistTests.cs +160`, SSOT +21, domain-knowledge +36. **GR2051/GR2052/GR2053 SHIPPED.**

--- `guardrails breakdown` — #498, PR #499, MERGED as `028faf6` ---
- **Measured first:** the wave-1-stub workaround for #496 **does not work** — a plan whose waves are all
  unauthored stubs fails **GR1009** before the scheduler runs. The JIT checkpoint IS index-agnostic
  (wave 1 reaches it once GR1009 is satisfied — verified on a throwaway fixture), but pipeline-start
  cannot reach it at all.
- `WaveBreakdownInvoker.InvokeCoreAsync` extracted; BOTH doors call it. `BreakdownInvocationPlan` made
  public (Cli has no `InternalsVisibleTo`); `WaveBreakdownOutcome` gained `CostUsd`.
- Runner resolution: `--runner-config` (through the REAL loader) else a built-in `claude`. A borrowed
  config yielding no usable runner is an ERROR, never a silent fallback.
- **Never marks the plan reviewed** — pinned by a test.
- **Exercised once for real:** exit 0, 55 turns of 550, ~15 min, **$10.60**; produced a correct TDD pair
  with the test-protection `writeScope`, a committed sample pair, real `# catches:` lines; GR2025 still
  fired afterwards as it should.

--- Issues filed this session ---
- **#493** guardrails-review writeScope-collision probe · **#494** Codex runner (v2) · **#495** record
  which model AUTHORED a breakdown · **#496** THE UNATTENDED EPIC · **#497** wave-1 halt text is false ·
  **#498** breakdown verb (CLOSED) · **#501** the salvage defect · **#502** waved fixtures are all serial.
- **Charter #172** (`handoff` has no strict mode — the unattended blocker) · **Charter #173**
  (`.headless.json` contract + the two naming collisions).
- **Closed with evidence:** #224, #225, #226 (confirm-and-close pass).
- **#201 epic body rewritten** with a state column and a sequencing section.

IN PROGRESS: What's partially done

- **`fix/501-breakdown-salvage-gate` — UNCOMMITTED.** `Scheduler.cs` (the gate fix + the
  `gate-decision.txt` tee), `SchedulerBreakdownDurabilityTests.cs` (+1 tee test; the unreproducible
  GR2028 test REMOVED rather than shipped as false coverage), new `BreakdownSalvageAllowListTests.cs`.
  **Core 1923/1923 · Integration GREEN (exit 0, verified 2026-08-23).** Ready to commit + PR.
- **PR body is written** at the scratchpad path in BLOCKERS — ready for `gh pr create --body-file`.
- `docs/plans/model-tiering-stage-3/.gitignore` is untracked (harness-written; decide before committing).

NEXT STEPS: What should happen next

1. **Commit `fix/501-breakdown-salvage-gate` and open the PR** (both suites are already green).
2. **Cut a release** — `#498` + `#501` must be in the INSTALLED tool before wave 2 is re-run (the re-run
   uses the global tool) and before the #496 harness (which needs `guardrails breakdown`).
3. **Split wave 2's brief.** It declared **12 tasks; 30 minutes bought 5**. Two waves of 6 sits right AT
   the measured rate — consider three. Editing `brief.md` re-stales wave 2's marker (accepted).
4. **Re-run wave 2**, then wave 3 (#229 review net) — Stage 3's remaining scope.
5. **Then the #496 harness** — Arm A (Guardrails-only, CI-runnable, gates) + Arm B (Charter, opt-in,
   asserts `handoff` output matches Arm A's fixture) + the planted-defect arm.
6. Remaining priority:high (unchanged): #490, #478, #474, #471, #453, #382, #378, #375.
7. Cheap mediums deferred from v1.8.0: #431, #438, #354. Plus #492.

KEY FILES: Important file paths (relative to repo root)

- `docs/plans/17-model-tiering.md` — the DoR, revision 6. **§13.3 now carries a dated STATUS UPDATE** —
  the gap it recorded was closed by Stage 2, so the note was false when read.
- `docs/plans/model-tiering-stage-3.charter.md` + `.charter.review/` — the charter and its review log.
- `docs/plans/model-tiering-stage-3/` — wave-01 (run, merged), wave-02-pilot-seat (stub + brief).
- `src/Guardrails.Core/Execution/InitialBreakdownInvoker.cs` — the #498 composer.
- `src/Guardrails.Cli/Commands/BreakdownCommand.cs` — the `guardrails breakdown` verb.
- `src/Guardrails.Core/Execution/Scheduler.cs` — `UnsatisfiableWhileIncomplete` + `TeeBreakdownGateDecision`.
- `docs/plans/02-schemas-and-contracts.md` — SSOT, `eol=lf`-pinned, byte-compared by `SchemaDriftTests`.
- `src/Guardrails.Core/Loading/DiagnosticCodes.cs` — next-free **GR1011 / GR2065**; GR2051–GR2053 now
  TAKEN, GR2054 still reserved.

CROSS-REPO: Any work that touches other repos

- **Charter (`C:\DevAI\Charter`) 0.23.0 — backlog no longer zero: #172 and #173 are OPEN** (filed here).
  **#172 is the Charter-side blocker for #496**: `charter handoff` has no strict mode, so an unanswered
  `:::question` hands off as prose and exits 0.
- **The lane rule** held: this session only INSTALLED from Charter and filed issues there; never committed.

BLOCKERS/NOTES: Issues or context for the receiving session

- **THE DOMINANT DEFECT CLASS, measured three more times this session.** #501: the manifest was written,
  GR2063 read it, the gate ran, the revert was clean — and the COMPOSITION discarded the prefix while
  announcing a resume. Also the census coaching an agent to delete the Invariant-7 test, and task 06's
  guardrail whose gradient pointed at its own hole.
- **READING DOES NOT FIND THESE. EXECUTION DOES.** Two full review passes found 0 of the 4 wave-1
  BLOCKERs; every one came from running a mutation. #501 came from a real run. Budget for execution.
- **#501's end-to-end repro does not exist** — `WavePlanBuilder` defaults to `maxParallelism: 1` and
  GR2028 only fires in worktree mode, which is why 15 durability tests passed while the salvage was
  broken. That is **#502**, and GR2028 is unlikely to be the only worktree-mode-only rule untested at
  the scheduler level.
- **Cost model, measured:** a breakdown is **~$10 REGARDLESS of plan size** (the composed prompt is
  283 KB, almost all inlined skill). Wave-1 execution was $18.29 for 6 tasks. A harness pass with two
  arms costs $20+ before any task runs. Do NOT trim the inlined skill to save money without measuring
  what a trimmed skill produces.
- **A 12-task wave does not fit one 30-minute ceiling.** Size a JIT wave to what one ceiling can author.
- Scratchpad (session-scoped, WILL vanish):
  `C:\Users\David\AppData\Local\Temp\claude\C--DevAI-Guardrails\d51b5191-b67e-4439-ae4f-e183460f02c2\scratchpad\pr-501.md`
  — **re-write the PR body from the issue if it is gone.**
- **`guardrails run` takes the PLAN folder, never a wave folder** (GR1010). Only `plan-hash` and
  `mark-reviewed` accept a wave.
- Launch dogfood runs from the **global tool** — a Release build in this checkout locks `Core.dll`, and
  the wave exit gate builds the solution.
- **NEVER author regex-bearing scripts via bash/perl/sed heredocs** — bit twice more this session
  (mangled C# string literals, collapsed escapes). Use the file-write tool.
- PowerShell variables are **case-INSENSITIVE** (`$Out` and `$out` are one variable) — cost a cycle.
- **Maintainer rulings not to re-litigate:** `costly` is TRI-STATE · difficulty maps to a candidate SET ·
  a deleted comment stays deleted · hand-code anything needing network calls · **the DoR wins** ·
  `dial:critical` + `proceed-unreviewed` is FORBIDDEN (the clamp is implemented in `AnswerableGates`).
