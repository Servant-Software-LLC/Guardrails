REPO: C:\Dev AI\Guardrails
TOPIC: Guardrails/Model-Tiering-Epic

CONTEXT: What we were working on
The #201 model-tiering arc, now well past design into review + first-dogfood. This session
(2026-07-10) did: a forensic investigation proving the #201 DoR was authored by a FABLE architect
sub-agent (git attribution masked it); a full evaluation of all 14 open GH issues; a Fable
devil's-advocate pass over the model-tiering DoR (PR #342) engaging the maintainer's 7 inline review
comments; posting that DA report to #342 + submitting the maintainer's comments; filing #349
(pilot-seat model provenance — "who's in the pilot seat"); an Opus+KISS agent pass that yielded the
single-model-zero-setup invariant + a static-only v1 cut; landing the ratified static-v1 DoR revision
on PR #342; and dogfooding the #349 /plan-breakdown (twice — first on stale preview.35 skill/tool,
then cleanly on preview.40 after updating both). Earlier in the same session (before compaction):
the diagram.html title-band click-overlay + fill fixes (#235/#237) and the #348 banned-pattern
registry review that surfaced the Fable-sub-agent thread.

COMPLETED:
--- Prior (2026-07-07 and before, carried forward) ---
- #201 extended with the "Resolution timing" design (static difficulty TAG vs DYNAMIC attempt-launch
  resolution of (provider,model,effort)); escalation ladder promoted to a core mechanism; renamed to
  "Epic: Model tiering for token/cost conservation…".
- #201 split into 8 sub-issues #223–#231 (registry #224, tier tagging #225, runtime resolution #226,
  budget probes #227, escalation ladder #228, guardrails-review appropriateness check #229,
  cost-by-tier #230, interactive steering #231; local-inference IPromptRunner #223 out of the wave
  sequence). Wrote the 3 stage briefs model-tiering-{foundation,consumers,dynamic-behavior}.md
  (Sonnet 5, commit aca1ea9) + the diagram-live-status-and-search.md brief (#219/#220).
- OSS-extraction verdict (agent-team, 3 independent perspectives): build in-repo behind a narrow
  IModelRouter-style seam; do NOT stand up a separate repo. Posted on #201. Devil's-advocate also
  flagged that tiering is NOT a named v2 bet in 03-roadmap.md.

--- This session (2026-07-10) ---
- Diagram.html fixes shipped to master: #235 (title-band SVG click-overlay — Mermaid can't bind clicks
  to subgraphs, so a post-render <a><rect> over the container's title row), #237 (the overlay rect's
  fill was overridden by Mermaid's own `.cluster rect` CSS → switched to inline `style.fill`). Both
  verified with real headless-Chrome popup capture.
- FABLE-AUTHORSHIP FINDING (forensics): the #201 DoR (docs/plans/13-model-tiering.md, commit e6ed12e,
  PR #342) was authored ENTIRELY by a Fable architect sub-agent (agent-a0b54d80f7f8c6003, model
  claude-fable-5, 44.5K-char single Write) dispatched by parent session 0e73f910; the parent committed
  it, and the `Co-Authored-By: Claude Opus 4.8` trailer is a FIXED TEMPLATE string (every commit gets
  it regardless of the running model), which is why git attribution never showed Fable. The committed
  DoR is byte-for-byte the Fable sub-agent's output; Opus changed nothing. (Sub-agent transcript:
  …/projects/C--Dev-AI-Guardrails/0e73f910…/subagents/agent-a0b54d80f7f8c6003.jsonl.)
- Evaluated all 14 open issues: 3 clusters — model-tiering epic (#201,#223–#231), worktree/dependency
  cost (#255 pool worktrees, #259 declared setup command [flagged HIGHEST near-term value — a real
  dead-end for Node/Python plans], #300 authoring nudge), and #298 (breakdown research-nudge). #255
  entangled with the unresolved-root-cause #253.
- Ran the Fable devil's-advocate on PR #342, engaging the maintainer's 7 pending inline comments +
  independent findings. Headline: BLOCKER F1 — OD-A ("final budgeted attempt at strongest served
  rung") routes EVERY task to frontier when retries:0, inverting the cost thesis. Also real gaps:
  provider-unavailability unhandled (network-down burns the retry pool climbing to unreachable
  models); no way to reserve a model (Fable) for breakdown-only / exclude from the ladder; §6.2↔§8
  route-down contradiction. Posted the full DA report to PR #342.
- Submitted the maintainer's 7 pending review comments on #342 (were in unsubmitted PENDING state).
- Filed #349 — "Surface which Claude model actually ran each task/attempt" (the pilot-seat problem;
  a TODAY problem independent of tiering, and the provenance foundation #226/#230 silently assume).
- Opus + KISS agent pass (user-requested). KISS verdict: the single-model runtime path is already
  clean (~90%); the ONE real violation is /plan-breakdown auto-tagging every task then GR2041 warning
  the untiered user — fix by gating tag authoring on routing being configured. Cut-list: drop ladder
  #228 / probes #227 / steering #231 from v1 (which also removes the DA's BLOCKER + route-down
  contradiction from the v1 path). Architect confirmed #349 as the ideal first dogfood + designed its
  mechanism.
- LANDED the ratified STATIC-V1 DoR revision on PR #342 (commit e60b941, still an OPEN DRAFT): new
  Invariant #7 (single-model/zero-setup byte-identical, observability-exempt); plan-scoped activation
  (tiering "configured" vs "active"); v1 = registry #224 + gated tagging #225 + static resolver
  #226-static + #230-lite; ladder/probes/steering tagged [v2 — deferred]; DA fixes folded (reserved
  omit-routing pattern + GR2044 reserved≠default warning, precedence chain action.runner/action.effort
  + GR2045 pin+tier warning, provider-unavailability→#115 pause at §6.3, doc-clarity batch); the 3
  open decisions (OD-A scope, route-down lever, D15 triggers) DEFERRED to v2.
- Updated the installed global tool preview.35 → preview.40 (`dotnet tool update -g
  ServantSoftware.Guardrails --prerelease`) — the diagram title-wrap bug was a STALE-TOOL issue (the
  wrap fix shipped in preview.36, commit 1434e7e). Saved memory keep-guardrails-tool-current.md.
- DOGFOODED the #349 /plan-breakdown to a validated draft: docs/plans/pilot-seat-model-provenance/
  (12 tasks, 6 tiers), regenerated FRESH on preview.40 skill+tool after the user updated both.
  Applied the 40-skill deltas the 35 run missed: #252 (read-only git in allowedTools), #191 (task 12
  = the .claude/ domain-knowledge skill write carries the needsHarnessWrite escape hatch, split out
  of the SSOT-doc task 11), #302 (author-time smoke-test — 5 runnable structural guardrails executed
  two-sided valid→0/invalid→non-zero, all pass; 28 .ps1 all parse-clean; build/test guardrails
  deferred as not-runnable-at-author-time). Validated clean under preview.40, graphed (correct
  wrapping), locked.

IN PROGRESS:
- PR #342 (static-v1 DoR revision) is an OPEN DRAFT awaiting the maintainer's #106 INLINE REVIEW. The
  DA report + the maintainer's 7 comments are posted on it. 6 judgment calls flagged by the revision
  agent for the maintainer to confirm: (1) #229 kept IN v1 (not in the explicit KEEP list); (2) two
  new GR codes GR2044/GR2045 allocated (maintainer may prefer plain warnings); (3) a `no-route`
  outcome kept as a defensive residual; (4) unavailability landed at §6.3 not the literal "§6.5";
  (5) a `usage` provenance field kept "unless #349 already carries it"; (6) briefs only lightly
  touched (DoR-pointer blockquotes).
- The #349 pilot-seat dogfood: the task folder is a validated DRAFT. Next stage is the MAINTAINER's —
  review the folder, then /guardrails-review, then `guardrails run` (per dogfood-user-involvement:
  the user drives these stages, live UI, not chained autonomously).
- Roadmap-gate question (re-admit model tiering as a named v2 bet in 03-roadmap.md, or leave as
  opportunistic backlog) — STILL open; worth deciding before any Stage-1 implementation.

NEXT STEPS:
- Maintainer reviews the #349 pilot-seat folder (docs/plans/pilot-seat-model-provenance/) →
  /guardrails-review docs/plans/pilot-seat-model-provenance → guardrails run (maintainer-driven). #349
  is the agreed FIRST dogfood of the tiering arc — blocker-free, valuable today, and the model-
  provenance base #226/#230 assume. Verified mechanism: parse the model the CLI echoes in its
  stream-json (system/init line; ClaudeStreamParser discards it today), do NOT force --model.
- Maintainer does the #106 inline review of PR #342 (the 6 judgment calls above + ratifying the
  static-v1 direction). Implementation of Stage-1 (#224/#225) does not start until #342's review
  clears; the reserved-model `routing.escalationTarget` field and OD-A re-scope are deferred WITH the
  v2 ladder.
- Decide the roadmap-gate question before Stage-1.
- When Stage-1 implementation starts, route it through the guardrails-* agents (architect/harness-
  developer/skill-author/test-author) with hands-on lead review — not solo. The DoR
  (docs/plans/13-model-tiering.md) is now the SSOT for the epic's shape; the 3 stage briefs are
  ratified UNDER it (their "Wave 1/2/3" renamed to "Stage 1/2/3").
- Near-term non-tiering backlog worth picking up: #259 (declared per-worktree setup command — highest
  near-term value, a real dead-end for gitignored-deps plans); #255 (worktree pooling, gated on
  #253's unresolved reset-safety); #298/#300 (skill-author lane).

KEY FILES:
- docs/plans/13-model-tiering.md — THE DoR (branch design/model-tiering-dor, PR #342 @ e60b941). The
  living SSOT for the epic; static-v1 sections are [v1]-tagged, deferred ones [v2 — deferred]. Read
  fresh before resuming.
- docs/plans/model-tiering-{foundation,consumers,dynamic-behavior}.md — the 3 stage briefs, ratified
  under the DoR (on master). Foundation = Stage 1 (#224+#225); consumers = Stage 2; dynamic = Stage 3.
- docs/plans/pilot-seat-model-provenance.md — the reviewed brief for #349 (KEEP; input to
  /plan-breakdown).
- docs/plans/pilot-seat-model-provenance/ — the generated 12-task dogfood folder (validated+locked on
  preview.40), a DRAFT for the maintainer to review → /guardrails-review → run.
- src/Guardrails.Core/Prompts/ClaudeStreamParser.cs — line ~77-81 `Feed` returns on any non-`result`
  line, DISCARDING the system/init model the CLI echoes; the load-bearing #349 change.
- src/Guardrails.Core/Execution/PromptExecutionSupport.cs:64 — `ResolveModelForDisplay(taskModel,
  runnerModel) => taskModel ?? runnerModel ?? "(cli default)"` (the requested-model source; #349 adds
  ResolveActualModel/IsModelMismatch beside it, #226 inserts tier resolution here).
- src/Guardrails.Core/Journal/JournalModel.cs:~261 — AttemptProvenance.Model (records the config guess,
  never the actual model); #349 adds resolvedModel/requestedModel/effort.
- src/Guardrails.Core/Execution/IRunObserver.cs — default-method events (AttemptStarting); #349 adds
  AttemptModelResolved.
- src/Guardrails.Core/Prompts/PromptRunnerRegistry.cs — the single-runner-class extension seam #223/#224
  extend (the IModelRouter precedent).
- docs/plans/02-schemas-and-contracts.md §9 (prompt runners) / §3 (task.json) / §7 (provenance) — SSOT
  sections the epic + #349 amend; drift-tested canonical-schema:promptRunners sentinel block.
- docs/plans/03-roadmap.md — does NOT name model tiering as a v2 bet (unresolved roadmap-gate).
- .claude/skills/plan-breakdown/** and the installed `guardrails` tool — BOTH must be current
  (preview.40+) or /plan-breakdown produces stale output (see keep-guardrails-tool-current memory).

CROSS-REPO: None — all work stayed in the Guardrails repo.

BLOCKERS/NOTES:
- KEEP THE TOOL + SKILLS CURRENT (memory keep-guardrails-tool-current): a stale installed `guardrails`
  or stale .claude/skills/ produces already-fixed bugs (the diagram title-wrap was exactly this). Fix =
  update, not a `dotnet run` workaround. Installed now: preview.40.
- A PROMPT task whose deliverable is under .claude/ MUST carry the verbatim `needsHarnessWrite`
  escape-hatch header (#191) — a direct .claude/ write hits the tool-permission wall and dead-ends at
  needs-human. (The 35 skill only fired on NEW subdirs; the 40 skill fires on any .claude/ file.)
- Multi-task plans get read-only git in allowedTools (#252); the 40 plan-breakdown applies this.
- git commit `Co-Authored-By` trailers are a FIXED TEMPLATE, NOT the running model — do not infer
  authorship from them (the Fable-DoR finding). Sub-agent model is only visible in the sub-agent
  transcript (…/subagents/agent-*.jsonl, the `"model"` field).
- The escalation ladder's retry-budget question (escalated attempt resets vs shares the pool) and OD-A
  cost profile are DEFERRED to v2 with the ladder — decide with #230-lite dogfood data in hand.
- Dogfood-user-involvement (memory): "dogfood X" = the USER runs /plan-breakdown → /guardrails-review →
  guardrails run themselves with live UI, reviewing each stage; don't chain the pipeline autonomously.
- Prior-context items now DONE (intervening Build-topic session): #219/#220 diagram features were
  dogfooded; #221/#222 were filed/addressed; the parallel #214 dogfood merged; NuGet preview.36–40
  cut. Those live in the Guardrails/Build context, not here.
