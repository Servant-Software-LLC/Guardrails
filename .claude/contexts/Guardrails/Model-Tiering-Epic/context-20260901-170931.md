REPO: C:\DevAI\Guardrails
TOPIC: Guardrails/Model-Tiering-Epic

CONTEXT: What we were working on

**TWO PLANS SHIPPED TODAY, AND A THIRD IS BROKEN DOWN AND WAITING FOR REVIEW.**

master `10816fb`, clean, everything pushed. Tool + skills at **v1.14.0** (verify the behaviour, not
the version string — see BLOCKERS).

NEXT STEPS — start here

1. **`/guardrails-review docs/plans/30-telemetry-phase-1`** — 27 tasks, committed at `10816fb`, NOT
   yet reviewed. **Point it hardest at the worktree settle**: no test drives
   `Scheduler.RecordSucceededSettle`, so that joint rests on ONE source-shape guardrail. Then apply
   findings, `mark-reviewed`, and run:
   `guardrails run docs/plans/30-telemetry-phase-1 --autonomous --max-cost-usd 200 --no-merge-on-success --no-ui`
   (27 tasks vs plan 32's 17 at $119 — budget accordingly. Do NOT pass `--no-log-server`: #552 is
   fixed, so the run starts its own server and prints the URL. **Give the operator the STATIC INDEX
   link, not the live-server root** — that root is a dead end, #573.)
2. **Two probes the breakdown handed over** for that review, both worth carrying:
   - every author-tests task either ships a `src/` stub or depends on a shape task that lands the
     members its tests name. Verified 11/11 today; **nothing checks it**, and a future hand-edit
     breaking it surfaces as task 22's whole-solution build failing — somebody else's problem.
   - a proposed `validate` diagnostic for that same property, beside GR2042/GR2068. Harness change,
     SSOT-first, not in plan 30's scope.
3. Then **#577** (the model-attribution census) — Phase A of #570.

COMPLETED TODAY

- **Plan 31 → #554/#553/#545 closed.** 10/10 green, $88.98, zero escalations. Shipped salvage on the
  needsHuman path, GR2068/GR2069, and the mid-run plan-edit warning. Released **v1.13.0**.
- **Plan 32 → #556 closed.** 17/17 green, $118.97, **three halts**, all defects in the INSTRUCTIONS,
  none in the harness. Released **v1.14.0** (NuGet + GitHub Release + 5 binaries + Homebrew tap,
  formula verified at 1.14.0).
- **#552 fixed** — the log server no longer needs a TTY.
- **Plan 30**: §3.2 fingerprint bucket SETTLED, Charter-reviewed (4 decisions), handed off with a
  custody manifest, broken down to 27 tasks.

KEY DECISIONS — do not re-litigate

- **The fingerprint bucket is SIX buckets** (`test-authoring`, `implementation`, `structural`,
  `code+tests`, `documentation`, `no-write`), derived at WRITE TIME from `writeScope` roots +
  guardrail archetypes. **Never from `taskId`** — the report's legend forbids it. Measured over 316
  tasks / 18 plan folders. It is about write surface and gate shape, **NOT difficulty** (that is
  `action.tier`) — do not collapse the two.
- **Model digest is IN Phase 1 IN FULL** (schema + capture). This OVERRODE the drafting agent's
  "field now, capture later" lean; do not drift back.
- **#577 = the CENSUS only** in Phase 1; the fix is its own issue.
- **The pre-fix corpus era gets a documented BOUNDARY DATE**, not a backfill, not a re-baseline.
- **64GB Mac Studio is the TIGHTER box** than the 128GB MacBook — confirmed by the maintainer.

BLOCKERS/NOTES: For the receiving session

- **VERIFY BEHAVIOUR, NOT THE VERSION STRING.** v1.12.0 reported its version correctly and simply did
  not contain GR2068 — it would have "validated" plan 32 against a check that did not exist. Check the
  installed DLL for the symbols you depend on (#564).
- **`guardrails samples verify` is the authority on sample pairs.** `GR_SUBJECT` arrives ABSOLUTE and
  must not be `Join-Path`ed. Extra cases are `X.invalid-<qualifier>.cs`, qualifier AFTER the token.
  Author-time staging into target paths does NOT exercise this and halted a run (#559).
- **A #181 baseline can halt its own resume** on a red the plan itself created (#574). Exclude by
  METHOD when a class must otherwise stay in the baseline.
- **The plan branch is cut once and never rebased** (#576). Merging only it delivers code beside a
  plan folder that never ran — master briefly carried exactly that today. Merge your own branch too.
- **A running `guardrails logs` viewer LOCKS the tool store** and makes `dotnet tool update` fail with
  access-denied. Stop viewers before updating.
- **Both adversarial passes paid off every time, and only with a NON-authoring agent.** Plan 32's
  folder review found a blocker by MUTATION that reading missed — a guardrail reporting "pins both
  assert GR2069" while the assertion said the opposite.
- **THE PATTERN WORTH A DESIGN CONVERSATION:** across plans 31 and 32, every halt was a defect in the
  INSTRUCTIONS, not the checks — a prompt demanding a namespace the language forbids, a ledger that
  never enumerated two affected tests, a baseline reddened by its own plan. #553 was built for
  unreachable `writeScope`s and **not one of these was one**. Five instances now.

ISSUES FILED TODAY: #556-#560, #563, #564, #566, #568, #569, #571-#574, #576, #577. #552 bumped+corrected.
#570 is the roadmap; its Phase A is updated (bucket decided, #577 next).
