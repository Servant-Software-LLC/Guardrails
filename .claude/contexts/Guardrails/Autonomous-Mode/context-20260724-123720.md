REPO: C:\DevAI\Guardrails
TOPIC: Guardrails/Autonomous-Mode

CONTEXT:
The #360/#361 autonomous-mode arc, built by dogfooding it through Guardrails' own loop. This
session brought the topic to a CLEAN CHECKPOINT: the arc is COMPLETE and on master, the #375
pre-merge security read found + fixed a real BLOCKER, the ENTIRE dogfood-findings backlog (7
issues) is merged, the four remaining loose ends are tracked as issues, and preview.46 is cut +
published. Nothing on this topic is in flight.

Two firm maintainer product calls still anchor the design:
- (A) FORBID `dial:critical` + `proceed-unreviewed` (GR2040 load-time error) — "Guardrails
  without guardrails is self-defeating."
- (B) Resume-time answer-injection is IN v1; the harness must NEVER forge a review attestation or
  write a review marker on a human's behalf (the #375/#366 floor).

COMPLETED:
- Autonomous-mode arc COMPLETE and delivered to master. Waves 1-4 all green; **feat -> master via
  PR #394 (merge commit fa7ea31)** — the whole #361 implementation + #383/#384 Windows MAX_PATH
  junction + #375 security hardening now on master. The #384 reconciliation was clean
  (GR2038 + GR2039 + GR2040 combined; WorktreeRoot + Autonomy config both kept).
- **#375 pre-merge security read found + fixed a real BLOCKER** (the #382 fake-masks-integration
  pattern again): the non-answerable clamp (hard/`critical` under `proceed-unreviewed`) was
  IMPLEMENTED but DEAD in production — `AnswerFileConsumer.Consume`'s `proceedUnreviewed` defaulted
  false and `Scheduler.ConsumePendingAnswers` never passed it. Fixed: computed + passed the flag,
  made the param compiler-REQUIRED, hardened the answer envelope (reject the `[…UNTRUSTED…]`
  delimiter + 8 KB cap, fail-closed), made the **wave-checkpoint answer channel LIVE**
  (`TryConsumeWaveProceed` tri-state), and made `hold` a DEFINITIVE honest-halt (no re-classify).
- **#386 fixed** (PR #388, c7150d6): `PlanDefinitionHash.Compute` now covers wave-level
  `<wave>/{guardrails,preflights}/` bodies. **#381 closed** (empty-StreamLogPath crash, fixed-in-arc).
- **The 7-issue dogfood backlog — ALL merged to master** (each CI-green on 3 OSes, squash):
  - #385 (#395, 39e4510): auto-breakdown turn budget raised + brief-scaled
    (`WaveBreakdownInvoker`: base 400 + 25/brief-signal, clamp [400,1000]) — large waves no longer truncate.
  - #379 (#396, db14ec9): live table collapses completed-wave rows (`LiveTableRows` pure planner +
    `RebuildRows` on `WaveFinished(Completed)`; `--all-tasks` opt-out; missing-row `TryGetValue` tolerance).
  - #380 (#397, 22b0030): per-wave `index.html` log site (`LogSiteRenderer.WriteWaveIndex` +
    `OnTheFlyLogSiteObserver`; `ExportSite` waves-overload is back-compat). Reconciled with #379 on `RunCommand`.
  - #389 (#398, 32c5720): **writeScope now REQUIRED** — `[]` = writes-nothing (VALID), absent =
    GR2041 error, runtime fail-closed. Caught + fixed a latent `PlanLoader` bug (`is { Count: > 0 }`
    collapsed `[]` -> null, which would have defeated the design). 53-fixture migration + golden regen.
    Skills: plan-breakdown EMITS `[]` for non-writing tasks (DB-config / verification / state-only
    examples); guardrails-review treats `[]` FIRST-CLASS, flags only a truly-absent field.
  - #378 (#399, 08aad40): GR2042 over-scope lint (WARN; `OverScopeTurnThreshold=60` named constant;
    3 co-occurrence clauses maxTurns×writeScope×dependsOn) + sharpened plan-breakdown/guardrails-review
    split-check (fan-in-sink archetype). No shipped-example false-positives.
  - #382 (#399): "drive-the-real-seam" doctrine vs fake-masked integration — plan-breakdown Step-4
    check + guardrails-review probe + a `guardrail-catalogue.md` archetype + `stacks/dotnet.md` pointer.
  - #387 (#400, e66a3e8): structured `needsHuman {question, options[]}` (free-text back-compat) +
    interactive Spectre `SelectionPrompt` (v1) + web-clickable `LogServer` POST buttons (v2).
    `AnswerableGates` is a SINGLE shared floor predicate used by `EscalationPick.WriteChoice` AND
    `AnswerFileConsumer`; `WriteChoice` refuses a non-answerable gate on both surfaces; never-forge test.
- **Follow-ups filed (loose ends tracked, not lost):** #401 (extend pick surfaces to prompt/plain-
  attended runs — the ACCEPTED #387 boundary), #402 (deeper #385: checkpointed/incremental breakdown
  authoring so a truncation leaves a valid prefix), #403 (git-backed CLI e2e waved-run test harness for
  #380), #404 (surface JIT-spliced wave rows in the live table — an `IRunObserver` contract gap from #379).
- **preview.46 CUT + PUBLISHED** — tag `v1.0.0-preview.46` on e66a3e8; the Release workflow SUCCEEDED
  end-to-end (3-OS tests + packaged-tool-smoke green, NuGet push + GitHub Release + all 5 platform
  binaries all succeeded). Becomes installable after NuGet finishes processing (preview.45 took ~75 min).

IN PROGRESS:
- (none) — the topic is at a clean checkpoint. preview.46 may still be finalizing on NuGet's side.

NEXT STEPS:
- When preview.46 is installable: `dotnet tool update -g ServantSoftware.Guardrails --prerelease`;
  refresh the home skills (`~/.claude/skills`) to match (repo SSOT skills untouched).
- The 4 tracked follow-ups (#401-#404) when prioritized.
- GR codes are clean through GR2042; next-free is **GR2043**.

KEY FILES (as-built on master, e66a3e8):
- src/Guardrails.Core/Execution/{AnswerFileConsumer,AnswerableGates,EscalationPick}.cs — the answer/
  pick channel + the single non-answerable floor predicate (#375/#387).
- src/Guardrails.Core/Execution/Scheduler.cs — `ConsumePendingAnswers` passes `proceedUnreviewed`;
  `TryConsumeWaveProceed` tri-state (Hold -> definitive honest-halt).
- src/Guardrails.Core/Loading/PlanValidator.cs — GR2041 writeScope-required + GR2042 over-scope lint;
  DiagnosticCodes.cs next-free GR2043.
- src/Guardrails.Core/Loading/PlanLoader.cs — `WriteScope = raw.WriteScope is { } ws ? [.. ws] : null`
  (presence-match preserves null-vs-empty). WriteScopeCheck.cs — fail-closed `scope ??= []`.
- src/Guardrails.Core/Journal/PlanDefinitionHash.cs — #386 wave-gate coverage.
- src/Guardrails.Core/Execution/WorktreeJunction.cs + SchedulerFactory.cs — #384 MAX_PATH junction (C:\.a).
- src/Guardrails.Cli/Ui/{LiveTableRows,LiveRunObserver,LogSiteRenderer,OnTheFlyLogSiteObserver,LogServer,
  EscalationPickPrompt}.cs — #379/#380/#387 UI.
- .claude/skills/{plan-breakdown,guardrails-review}/SKILL.md + plan-breakdown/references/
  {guardrail-catalogue.md,stacks/dotnet.md} — writeScope-required + over-scope + drive-the-real-seam.
- docs/plans/02-schemas-and-contracts.md — §3.4 (writeScope contract + GR2042), §7.2/§8/§9 (structured
  needsHuman + pick surfaces), §9.4 (needsHarnessWrite cross-ref).
- docs/plans/autonomous-mode-impl/ — the dogfood plan; Waves 1-4 complete; already writeScope-compliant (43/43).

CROSS-REPO: (none this session.)

BLOCKERS/NOTES:
- master HEAD = e66a3e8 (all 7 backlog + arc). preview.46 published (installable after NuGet processes).
- ACCEPTED #387 boundary (tracked as #401): the pick surfaces operate on escalation records, produced
  ONLY under the autonomous answer channel (`autonomyPolicy: auto` + an `autonomy` block); a plain /
  `prompt`-policy `needsHuman` still hard-halts as today.
- writeScope is now a REQUIRED contract (#389): every `task.json` must declare it; `[]` = writes-nothing;
  omission = GR2041 error. All existing plans/examples/fixtures were migrated.
- Standing constraints (unchanged): worktree ONLY when concurrent sessions (solo -> local checkout
  C:\DevAI\Guardrails; throwaway worktrees under %TEMP%, never C:\Dev). Don't run the Release binary while
  testing (self-locks Core.dll). NEVER `cd <path> && git` — use `git -C <root>`. Do NOT
  `guardrails skills install --force` targeting the repo (overwrites SSOT skills). mergeOnSuccess defaults
  ON (#340). Keep the tool current via `dotnet tool update -g ServantSoftware.Guardrails --prerelease`.
  Dogfooding = user runs the harness + reviews each stage. #253 mitigation in force (targeted `--filter`,
  never the full integration suite). #374 permission wall deferred.
