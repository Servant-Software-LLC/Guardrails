REPO: C:\DevAI\Guardrails
TOPIC: Guardrails/Autonomous-Mode

CONTEXT:
The #360/#361 autonomous-mode arc, being built by dogfooding it through Guardrails' own
loop. Rather than hand-delegated phases (the user's pivot: "we should just create one plan
with waves and let guardrails do a /plan-breakdown on it"), the whole #361 implementation is
authored as a single 4-wave plan (`docs/plans/autonomous-mode-impl.md`) and driven via
`guardrails run`. This session: shipped auto-breakdown-by-default (#376) + preview.44, split
an over-scoped Wave 3 task, and root-caused the dogfood findings blocking the run.

Two firm maintainer product calls anchor the design:
- (A) FORBID `dial:critical` + `proceed-unreviewed` — "Guardrails without guardrails is
  self-defeating; that fully-autonomous flow is firstmate's job, not `guardrails run`."
  Enforced as GR2040 (compound-config load-time error, keyed on reachable end-state).
- (B) Resume-time answer-injection is IN v1 scope (long-running unattended = a wall the user
  keeps hitting). Security-sensitive: the harness must NEVER forge a review attestation and
  never write a review marker on a human's behalf.

COMPLETED:
- #360 Phase 0 (brief.md convention, halt/decision plumbing) + Phase 1 (#368,
  WaveBreakdownInvoker auto-invokes plan-breakdown at the JIT checkpoint) — merged to master.
- Autonomous-mode design ratified + hardened through 2 adversarial passes: merged DoRs
  `docs/plans/12-autonomous-mode.md`, `11-overwatcher.md` §9, `16-review-attestation-
  provenance.md` (via #364, #369). Blocker resolutions: hard calls non-answerable under
  `proceed-unreviewed` (B1); review-attested answers dropped from v1 → filed #366 (B2).
- #366 evidence-hygiene design (#369) — review-attestation is evidence hygiene, NOT a runtime
  boundary; `attestation` block + `mark-reviewed`.
- Auto-breakdown-by-default feature (#376) SHIPPED — `RunConfig.AutoBreakdown` bool (default
  true); `Scheduler.RunJitCheckpointAsync` auto-invokes breakdown (token `auto-applied`)
  regardless of autonomyPolicy when true, else #368 policy-gated fallback; plan-breakdown
  auto-seeds `brief.md`. Merged to master (origin 7958320).
- preview.43 + preview.44 published (3-OS green, packaged-tool smoke gates publish) +
  installed globally; home skills (`~/.claude/skills`) refreshed to preview.44.
- Dogfooded Wave 1 (9 tasks, #366 evidence-hygiene) + Wave 2 (7 tasks, dial config) — GREEN.
- Wave 3 (classify-and-escalate) authored + `/guardrails-review`-clean + split. Over-scoped
  task 15 (timed out maxTurns:75, hammered by #374) split into 15/16/17 (commit 06083b4),
  re-reviewed, mark-reviewed (planDefinitionHash dfcb4b33), re-locked, GR2025 cleared,
  committed 0fd6283, pushed.
- Wave 4 (review-gate-policy) brief.md seeded (a45ba9a) so preview.44 auto-breaks-down at the
  checkpoint.
- Dogfood findings filed: #366, #371 (diagram wave-gate edge fan-out), #372, #374 (permission
  wall), #375 (JIT security-wave gap); reopened #253 (fixture-leak write-scope misattribution).
- Memory updated: autonomous-mode-arc-decisions, use-dedicated-worktree (→ conditional:
  local checkout when solo), mergeonsuccess-default-on.

IN PROGRESS:
- The dogfood run itself: `guardrails run docs/plans/autonomous-mode-impl` on preview.44.
  Waves 1–2 done; Wave 3 split tasks 15/16/17 not yet re-run; Wave 4 not yet broken down.
  Run is STOPPED (user stopped the prior process). Ready to resume — user said "I will run
  the harness."

NEXT STEPS:
1. USER resumes `guardrails run docs/plans/autonomous-mode-impl` on preview.44:
   - Tasks 1–14 skip (succeeded, preserved). Split 15/16/17 run (budget-sized; prompts steer
     to Bash-plain `dotnet test --filter` to sidestep #374; task 17 is the sink carrying the
     #120 SchedulerEscalationWiringTests full-5-facts proof driving the REAL
     SchedulerFactory.Create). Wave 3 finishes → honest-halt at Wave 4 checkpoint →
     auto-breakdown fires (autoBreakdown default + seeded brief.md) → halts at
     BreakdownComplete for /guardrails-review.
2. When Wave 4 checkpoint hits: user pings → I JIT-review the auto-authored
   `wave-04-review-gate-policy` against Wave 3's materialized code (AutonomyConfig dial,
   classify-then-act + escalation sink, answer-injection reply channel).
3. #374 harness fix (deferred posture decision, separate release, NOT required to unblock this
   run): `bypassPermissions` for contained task-actions vs broaden the Bash allow-list.
4. Pre-merge: the answer-injection security surface (Wave 3) wants a human code read before
   `feat/autonomous-mode-impl` merges to master (per #375 / the "pre-merge review" choice).

KEY FILES:
- docs/plans/autonomous-mode-impl.md — the dogfood plan (4 waves), the reviewed
  /plan-breakdown input.
- docs/plans/autonomous-mode-impl/ — generated task folder. wave-01-evidence-hygiene (9),
  wave-02-dial-config (7), wave-03-classify-and-escalate (17, task 15→15/16/17),
  wave-04-review-gate-policy/brief.md (seeded stub).
- docs/plans/12-autonomous-mode.md — autonomous-mode DoR (§5.2 review-gate + compound-config
  clamp, §9 Phase 4, §10 K).
- docs/plans/11-overwatcher.md §9, docs/plans/16-review-attestation-provenance.md — the other
  ratified DoRs.
- src/Guardrails.Core/Model/RunConfig.cs — AutoBreakdown bool (default true).
- src/Guardrails.Core/Execution/Scheduler.cs (RunJitCheckpointAsync) — auto-breakdown gate.
- src/Guardrails.Core/Prompts/ClaudePromptRunner.cs + Model/PromptRunnerConfig.cs —
  PermissionMode="acceptEdits" (the #374 root cause).
- docs/plans/autonomous-mode-impl/wave-03-classify-and-escalate/{15,16,17}/ — split wiring
  tasks: 15 wire-escalation-components-into-factory (writeScope SchedulerFactory.cs,
  Scheduler.cs; dependsOn 05,07,09,11,13); 16 wire-classify-then-act-into-scheduler (Scheduler.cs,
  ActionRunner.cs, PromptComposer.cs; dependsOn 15,14); 17 wire-exit-code-and-verify-composition-
  root (RunCommand.cs, ExitCodes.cs; dependsOn 15,16,14; the sink;
  ExitCodes.EscalationsPending=4).

CROSS-REPO:
- Charter repo (parallel session) hit #373 (branch divergence: mid-run resources must go on
  the plan branch, not master) — noted for future work, not this session's.

BLOCKERS/NOTES:
- master HEAD = origin 7958320 (#376). feat/autonomous-mode-impl HEAD = 0fd6283 (pushed).
  Local `master` ref may lag origin until fetched.
- #253 (reopened): write-scope check attributes untracked cruft (outside.txt/src/output.txt)
  to the agent when a task runs the FULL integration suite (fixture leak). Tripwire
  HostRepoCleanlinessGuard only guards RetrySalvageTests, but
  ScriptActionReproductionShortCircuitTests / WriteScopeCheckTests / HarnessWriteRunTests share
  the fixtures unguarded. Mitigation baked into every Wave 3 task: targeted `--filter`, never
  the full integration suite.
- #374 (permission wall): harness runs agent with `--permission-mode acceptEdits` +
  `Bash(...)`-scoped allowedTools missing `git checkout`; Claude Code's parser (NOT Guardrails')
  flags `dotnet test --filter` / pipes / `git checkout -- <path>` as "multiple operations
  requiring approval." Fix = broaden allow-list or bypassPermissions (deferred).
- #375: JIT security-wave gap — run-authored security tests gated only by comment-gameable
  naming floors, reviewed by no human. Drives the pre-merge human read requirement.
- Standing constraints: worktree ONLY when concurrent sessions (solo → local checkout
  C:\DevAI\Guardrails; throwaway worktrees under %TEMP%\guardrails-worktrees\, never C:\Dev).
  Don't run Release binary while testing (self-locks Core.dll). Dogfooding = user wants
  involvement, don't chain the pipeline autonomously. NEVER `cd <path> && git` — use
  `git -C <root>`. Do NOT `guardrails skills install --force` in the repo (overwrites SSOT
  skills). mergeOnSuccess defaults ON (#340, preview.40+).
