REPO: C:\DevAI\Guardrails
TOPIC: Guardrails/Autonomous-Mode

CONTEXT:
The #360/#361 autonomous-mode arc, built by dogfooding it through Guardrails' own loop: the
whole #361 implementation is one 4-wave plan (`docs/plans/autonomous-mode-impl`) driven via
`guardrails run`. This session: root-caused + fixed the wave-3 task-16/17 needsHuman halts
(two integration bugs + a Windows MAX_PATH blocker), shipped the #383/#384 short-junction
worktree-root fix as preview.45, validated it live (task 17 green under `C:\.a`), authored +
reviewed Wave 4 by hand (the #376 auto-breakdown truncated on it), and filed a batch of
dogfood findings — including a real review-attestation coverage gap (#386).

Two firm maintainer product calls still anchor the design:
- (A) FORBID `dial:critical` + `proceed-unreviewed` (GR2040 load-time error, keyed on the
  reachable end-state) — "Guardrails without guardrails is self-defeating."
- (B) Resume-time answer-injection is IN v1; security-sensitive — the harness must NEVER forge
  a review attestation or write a review marker on a human's behalf (the #375/#366 floor).

COMPLETED:
- #360 Phase 0/1 merged; autonomous-mode DoRs ratified (`12-autonomous-mode.md`,
  `11-overwatcher.md` §9, `16-review-attestation-provenance.md`) via #364/#369. #376
  auto-breakdown-by-default shipped. preview.43/44 published.
- Dogfooded Waves 1 (evidence-hygiene, 9), 2 (dial-config, 7), 3 (classify-and-escalate, 17
  incl. the 15→15/16/17 split) — all GREEN. Wave 3's #120 composition-root proof
  (SchedulerEscalationWiringTests, all 5 facts) drives the REAL SchedulerFactory.Create.
- **Wave-3 task-16 needsHuman root-caused + fixed at source** (2 integration bugs the #120
  wiring test caught, both outside task 16's writeScope): B1 = `CriticalityJudge.BuildInvocation`
  handed `StreamLogPath=""` → real `ClaudePromptRunner` threw → judge safe-defaulted to Escalate
  for EVERY assessment (fix: ClaudePromptRunner tolerates empty StreamLogPath, skip the log tee);
  B2 = a class-(b) transient that resolves within the executor pause budget recorded no
  `blocker-retried` (fix: `TaskExecutor` surfaces a `ResolvedTransient` signal; `Scheduler`
  records it, gated on autonomous mode). Strengthened the masking unit tests. Committed to the
  plan branch. Filed #381 (ClaudePromptRunner empty-StreamLogPath crash) + #382 (the DEEPER
  finding — fake-masked unit guardrails certify GREEN while the real composition-root path is
  broken; risk concentrates in one over-scoped end-of-wave sink).
- **Wave-3 task-17 needsHuman = Windows MAX_PATH** (`CreateProcessW` Win32 206 on a 264-char
  test-exe path; `dotnet test`'s out-of-process launcher; LongPathsEnabled does NOT lift the
  MAX_PATH cap on the process application-name). Filed #383. Implemented **#384** (PR merged to
  master 12cfe93): short default worktree root `<temp>/gr-wt/<hash>` + `GUARDRAILS_WORKTREE_ROOT`
  env override + the `guardrails.json worktreeRoot` key (WIRED — it was documented but dead) +
  **GR2038** run-start preflight + the primary lever, a Windows short **JUNCTION** (`WorktreeJunction.cs`,
  a `C:\.a` reparse point → the real root; resume-restore/hard-fail-on-mismatch, link-only
  teardown, graceful fallback). Empirically validated the junction (274-char FAIL → 233 via
  `C:\.a` PASS; MSBuild does not canonicalize the junction back).
- **preview.45 cut** (tag `v1.0.0-preview.45`, release green, NuGet publish OK) — note NuGet
  validation was UNUSUALLY SLOW (~75 min pushed→installable). Tool updated to preview.45; home
  skills refreshed to match (repo source-of-truth skills untouched).
- **Resumed the run on preview.45 — the junction fix works LIVE**: `worktreeJunctionRoot: C:\.a`,
  task 17 forked under it, guardrail `02-factory-wires-escalation-tests-pass` (the failing
  `dotnet test`) PASSED. **Waves 1-3 COMPLETE.**
- Wave-4 auto-breakdown (#376) TRUNCATED on the large (~11-task) wave → invalid partial output
  (GR1001) → quarantined + halted (the safety net worked; the auto-breakdown didn't). Filed
  #385. Chose to hand-author Wave 4 via plan-breakdown (guardrails-skill-author, full budget).
- **Wave 4 (review-gate-policy) authored (10 tasks), lead-reviewed, and `/guardrails-review`'d
  clean**: narrow recon-driven scope (wires only the 3 absent pieces — review-gate resolution;
  mergeOnSuccess-off + distinct exit 5 + N-unreviewed surface on a machine decision; overwatch
  auto-tier gated on the `autonomy`-block presence w/ byte-identical back-compat). Collision-safe
  (Scheduler.cs writers 05/09 chained), no over-scope. Review found 1 WEAK (`05/02-no-forged-review-marker`
  greped a non-existent `ReviewMarker.Write` API → tightened to forbid ANY `ReviewMarker.` access;
  smoke-tested) — fixed + mark-reviewed. Then a wave-4 preflight false-RED (`class\s+ReviewMarker`
  but ReviewMarker is a `record`) halted the run; fixed to `(class|record)`, smoke-tested all 11
  assertions, re-mark-reviewed.
- Also filed this session: **#378** (plan-breakdown/guardrails-review must catch over-scoped tasks
  BEFORE the run — mission #1, from the wave-3 task-15 over-scope), **#379** (live table: collapse
  completed waves' rows), **#380** (per-wave `index.html` log site), **#386** (see BLOCKERS).

IN PROGRESS:
- **Wave 4 is EXECUTING** — the plan branch shows `wave-04-.../01-ssot-phase4-delta` integrated
  (73f452b). The USER is running `guardrails run docs/plans/autonomous-mode-impl` (preview.45,
  same `C:\.a` junction). It runs the 10 wave-4 tasks; a GREEN Wave 4 completes the #361 arc.

NEXT STEPS:
1. Wave 4 finishes green → the #361 autonomous-mode arc is COMPLETE. Then pre-merge: a human
   code read of the answer-injection security surface (#375) before `feat/autonomous-mode-impl`
   → master.
2. **#386** (HIGH) — extend `PlanDefinitionHash.Compute` to hash each wave's `<wave>/guardrails/**`
   + `<wave>/preflights/**` (+ optionally repair per-wave `mark-reviewed` GR1001). Small change +
   regression test, then a preview. Offered to the user.
3. **#385** — raise/scale the auto-breakdown invocation's maxTurns (truncates on large waves).
4. **#381** — the ClaudePromptRunner empty-StreamLogPath crash is a standalone core bug; could
   cherry-pick to master ahead of the arc.
5. **#374** (permission wall) + **#382** (the fake-masks-integration methodology gap) + **#378**
   (over-scope pre-run detection) — deferred design/fix work.
6. Clean up the `gr-383` worktree (`%TEMP%\gr-383`, branch fix/383-... now MERGED to master).

KEY FILES:
- docs/plans/autonomous-mode-impl/ — the dogfood plan. Waves 1-3 complete;
  wave-04-review-gate-policy/ = 10 authored+reviewed tasks + preflights/01-wave3-materialized.ps1
  (the fixed `(class|record)` entry gate) + guardrails/ (wave terminal gate).
- docs/plans/12-autonomous-mode.md — the DoR (§5.2 review-gate + compound-config clamp, §9 Phase 4).
- src/Guardrails.Core/Execution/WorktreeJunction.cs — the #384 short-junction component.
- src/Guardrails.Core/Execution/SchedulerFactory.cs (WorktreeRootFor / EffectiveWorktreeRoot),
  WorktreePathPreflight.cs (GR2038), Loading/DiagnosticCodes.cs (GR2038) — the #384 fix.
- src/Guardrails.Core/Journal/PlanDefinitionHash.cs — the #386 bug is here (Compute lines 61-63
  hash only plan-ROOT gate folders; wave-level `<wave>/{guardrails,preflights}/` fall through).
- src/Guardrails.Core/Prompts/ClaudePromptRunner.cs (empty-StreamLogPath guard, #381),
  Execution/TaskExecutor.cs + RunReport.cs + Scheduler.cs (ResolvedTransient / blocker-retried, B2),
  Execution/CriticalityJudge.cs (the B1 empty-StreamLogPath origin) — wave-3 fixes (plan branch).

CROSS-REPO:
- Charter repo (parallel session) hit #373 (mid-run resources must go on the plan branch, not
  master) — noted, not this session's.

BLOCKERS/NOTES:
- HEADs: origin/master = 12cfe93 (#384). feat/autonomous-mode-impl = 3be63f3 (wave-4 authored/
  reviewed + preflight fix). plan branch guardrails/autonomous-mode-impl = 73f452b (wave-4 task 01
  integrated — run in progress). preview.45 published + installed.
- **#386 (HIGH, security-adjacent) — the review marker's `PlanDefinitionHash` does NOT cover
  WAVE-LEVEL preflight/guardrail bodies for waved plans.** Proven by code (Compute hashes only
  `<plan>/guardrails|preflights/**` + per-TASK files) AND empirically (editing a `<wave>/guardrails/`
  or `<wave>/preflights/` body did NOT re-fire GR2025; a task-level edit did). So a post-review edit
  that `exit 0`s a wave terminal gate or breaks an entry preflight escapes the nudge — the marker
  keeps vouching. Compounded by per-wave `mark-reviewed <plan>/<wave-NN>` erroring GR1001 (wave
  subfolder isn't a standalone plan). THIS run is safe (the preflight was fixed + smoke-tested +
  manually reviewed; the gap is coverage, not execution).
- #383/#384 MECHANISM: git 2.53 canonicalizes the junction away in its OWN worktree registrations
  (stores the real path), so the chosen `C:\.a` lives only in `run.json` (`worktreeJunctionRoot`);
  FORWARD segment creation uses the junction alias (short child cwd → short exe path), BACKWARD
  prune keys on the real root; resume restores from the journal; teardown removes the LINK ONLY.
- #253 mitigation still in force: targeted `dotnet test --filter`, never the full integration
  suite (fixture leak). #374 permission wall (deferred). #375 pre-merge security read required.
- Standing constraints: worktree ONLY when concurrent sessions (solo → local checkout
  C:\DevAI\Guardrails; throwaway worktrees under %TEMP%, never C:\Dev). Don't run the Release
  binary while testing (self-locks Core.dll). Dogfooding = user runs the harness + reviews each
  stage; don't chain autonomously. NEVER `cd <path> && git` — use `git -C <root>`. Do NOT
  `guardrails skills install --force` targeting the repo (overwrites SSOT skills; default home
  target is safe). mergeOnSuccess defaults ON (#340). Keep the tool current via
  `dotnet tool update -g ServantSoftware.Guardrails --prerelease`, not a `dotnet run` workaround.
