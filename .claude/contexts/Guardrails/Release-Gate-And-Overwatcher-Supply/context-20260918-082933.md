REPO: C:\DevAI\Guardrails
TOPIC: Guardrails/Release-Gate-And-Overwatcher-Supply

CONTEXT:
Two linked arcs in one long session. (1) A pre-release hardening gate the user asked for after plan 39/40 shipped: find and fix everything blocking a real release, cut it, and fix the tooling used to cut it. (2) Building and running plan 41 (#712, design 41: "overwatcher missing-resource auto-resolve") from a Charter design the user had already reviewed and decided.

COMPLETED:
- **Plan 39 (per-wave incremental delivery, #525) merged** via PR #715 as a merge commit; branches cleaned.
- **Release gate — 10 original issues + #525, ALL closed, 5 fix PRs merged, each gated on a non-authoring adversarial review + a 12-combination (3 OS × Core/Integration × build-and-test/solution-wide) skip-count check before squash-merge:**
  - #725→`0bd481ff` (#710 delivery-cause naming), #731→`cc26a639` (#704 run liveness), #734→`5aadec14` (#525 real-CLI wave-delivery proof)
  - PR #735→`0d454bf4` (#713/#714/#727 live view + AtomicFile retry + diagram link)
  - PR #737→`52372723` (#705/#706/#707 write-scope: found ALL THREE harness-decided halts — write-scope gap, #86 permission wall, #201 no-route — were being routed as JUDGMENT CALLS via a summary-string-prefix match, so in autonomous mode the criticality judge decided them and silently disabled delivery)
  - PR #740→`a60c0b41` (#708 permission wall at outcome; 3 adversarial review rounds — each round found the PREVIOUS round's *proofs* broken, not its code, e.g. a test whose mutation-proof polarity was inverted and passed green)
  - PR #743→`4e517ce9` (#726 delivery-target recording + resume-refusal; review found the "recorded only when delivered" assertion was VACUOUS — passed because run.json didn't exist yet, not because the field was absent)
  - PR #745→`a9f27e1d` (#722 fan-in worktree creation off the scheduler's settle lock — the 28-hour-dead-run bug; 3 adversarial rounds; design: defer to DEQUEUE not capture-under-lock, because the "safer-looking" option B still leaves an unbounded git call under `_gate`)
- **v1.21.0 RELEASE CUT AND VERIFIED LIVE on NuGet** (nupkg URL returns 200; `dotnet tool update -g` picks it up). Tag `v1.21.0` on `a9f27e1d`. GitHub Release's `--generate-notes` body was REPLACED with real notes carrying the #726 upgrade-path warning (a mid-flight ≤1.20 plan gets pre-#726 behavior exactly once on its next resume).
- **`cut-release` skill (repo-only, `.claude/skills/cut-release/SKILL.md`) corrected** — it had drifted from `release.yml` (claimed "5 jobs", real count is 11 incl. `create-release`+5-RID `binaries` matrix it never covered since those jobs were added; claimed "`gh release list` is normally empty", false — it's idempotent and non-empty). PR #748→`f5ccaa13`.
- **NEW `.github/scripts/release-preflight.sh`** — moves the mechanical release steps (MSYS_NO_PATHCONV path-mangling trap, PIPESTATUS-not-tail's-exit-code, the #747 time-window guard, a committed `.github/release-baseline.txt` for the 12-combination counts) out of ad-hoc shell into a verified, committed script. A delegated review found **10 bugs in the first draft**, incl. that the author's own stated verification command (`preflight <sha>`) could never have passed.
- **Plan 41 (20 tasks, 8 tiers, design 41/#712) built, reviewed (2 non-authoring adversarial rounds), merged to master** via a MERGE COMMIT (`68bd9113`, never squash — the corpus row pins commit `519a9640` by sha) after re-verifying it still merged cleanly against current master.
- **Plan 41 run in progress, resumed 4 times, 12/20 green, ~$36 spent** (cap $150, `--autonomous --max-cost-usd 150 --no-merge-on-success`, parallelism 3 unchanged per user instruction):
  - Run 1: OOM-killed by the OS (external, not the harness) after 9/20.
  - Run 2: needs-human halt at task 03 — a genuine, well-argued escalation: the shared `TempGitRepo` test fixture committed `vendor/resource.js` unconditionally into BOTH `_checkout` and `_integration`, making 4 of 20 pinned rows in `MissingResourceFactsTests.cs` mutually unsatisfiable under design 41 §2.2's row-5-before-row-6/8/9/10 check order, regardless of implementation. Verified the claim myself by reading the fixture from git.
  - **Fix applied and committed to the plan branch** (`76f56f63`, on `guardrails/41-overwatcher-supply-autoresolve`): gave `TempGitRepo` a `withVendoredResource` ctor param, `_integration` now starts clean (`new(withVendoredResource: false)`), `_checkout` unchanged. Verified `TempGitRepo` is file-private (nested `private sealed class`), so this cannot affect the 49 other test files with a same-named class. Build confirmed 0 warnings/0 errors.
  - Resumed at 10/20 → task 03 succeeded on attempt 5 (fix worked) → 11/20, then task 04 → task 15 also went green.
  - Run 3: aborted (exit 1, `RUN ABORTED`, harness/environment fault, NOT the fixture) — `git merge --no-commit --no-ff` into the integration worktree "failed unexpectedly" during task 15's settle. Task 05 was cancelled mid-attempt, journaled cleanly back to `pending` (no loss). Reproduced the SAME merge in a clean scratch worktree — it succeeded ("Automatic merge went well") — so first diagnosed as transient.
  - Run 4: **the SAME abort recurred** at the SAME integration worktree path (`C:\.a\a1a556af\_integration`) merging task 05's branch, across two different junction roots/runs. This is state, not chance — my "transient" diagnosis was wrong. 12/20 green now (task 15 landed before this abort), ~$36 spent.
  - **Root cause suspected, not confirmed:** the repo has 32 stale registered worktrees (from 3 dead run generations: `54ac4104`, `82a3f31d`, `0ff75495`) all still pointing under the current junction target `gr-wt/8012c271`, plus a ~1,608-root leaked-worktree backlog (see BLOCKERS below) — every non-fast-forward merge into `_integration` may be happening inside accumulated stale state. NOT yet confirmed: never replayed the FAILING merge into the ACTUAL persisted `_integration` dir (only into a clean scratch one, which is exactly the condition that doesn't reproduce it).
- **`/skills` discovery investigated and RESOLVED**: repo-local `.claude/skills/` (`cut-release`, `guardrails-dev-knowledge`, `guardrails-domain-knowledge`) auto-discovers with no install step and no trust gate — confirmed empirically when they appeared in this session's own skill listing partway through. The earlier "discovery gap" self-diagnosis was WRONG; the real failure was never checking. Two personal-level copies (`~/.claude/skills/guardrails-domain-knowledge`, `plan-breakdown`) were found LAGGING the repo copies (94 and 42 lines behind respectively) — personal wins over project by Claude Code's collision rule, so every session using them has been reading stale doctrine. NOT YET fixed — user has not yet said whether to overwrite the personal copies.
- Filed issues from adversarial findings during this arc: **#736→#747→confirmed** (a THIRD wall-clock test in `WebhookEventSinkTests`/`PromptRunnerReliabilityTests` fails deterministically in a ~30min DAILY window — `resetHint: "11:20am"` hardcoded, arithmetic verified to sub-second precision across 3 real occurrences, not load-related), #738, #739, #741, #742, #744, #746, **#749** (a "tests fail on stubs" guardrail asserts THAT a row is red, never WHY — so a self-contradictory fixture passes it; found via plan 41 task 03's escalation), **#750** (`GitWorktreeProvider.Integrate` throws `"failed unexpectedly"` while DISCARDING git's own stderr via `TryGitIn(...)`'s unused first tuple element — directly caused this session's inability to diagnose the run-3/run-4 aborts without a hand-built merge replay).

IN PROGRESS / NEXT STEPS (in order):
1. **Decide plan 41's recovery path — user was asked, mid-answer when session ended.** Two options given:
   - `--fresh` (tears down plan branch + all worktrees + runtime state, re-seeds; loses the 12-task journal, ~$36 + hours re-run, but definitely clears whatever `_integration` state both aborts point at).
   - Surgical: `git worktree prune`, manually clear the 32 stale worktree registrations + dead `_integration` dirs under `gr-wt/8012c271`, then resume normally (keeps the 12 green tasks).
   - Optional first step offered: replay the FAILING merge (task 05's branch) into the actual persisted `_integration` dir (not a clean scratch one) to confirm the stale-state theory before choosing.
2. If resumed successfully: watch through tier 6's fan-in (task 14, 7 dependencies — the first production exercise of #722's fan-in-off-the-lock fix), then read PAST the green summary to the `--no-merge-on-success` delivery banner (work lands on `guardrails/41-overwatcher-supply-autoresolve`, not delivered).
3. Tasks 19/20 (SSOT + domain-knowledge-skill doc updates) are the plan's own flagged weak spot — their guardrails assert prose *mentions* tokens, not that the docs are correct. Read their green skeptically.
4. Decide on the two lagging personal-level skill copies (see COMPLETED) — overwrite from repo, or leave.
5. Decide on a `CLAUDE.md` (or similar) line requiring a `/skills` check before any irreversible/outward-facing operation — the concrete fix for the discovery-gap misdiagnosis; proposed but not yet adopted.
6. File (or fold into #409) the worktree-leak-backlog finding: 1,608 leaked `gr-wt` roots as of last measurement, all pre-dating this session by up to a week, draining at ~250-390/run under #450's capped startup sweep — a static backlog, NOT an active leak, but the drain rate means ~6-7 more runs to clear it, and nothing surfaces the backlog SIZE to an operator.
7. Kill 3 orphaned `dotnet`/1 `VBCSCompiler` process from the run-1 OOM if still present — auto-mode classifier denied this as "Interfere With Workloads"; needs the user to run it or grant a permission rule.
8. A real (harmless but wrong) `core.hooksPath = C:\Dev AI\Guardrails\.git\hooks` (note the space — doesn't match the actual `C:\DevAI\Guardrails` path) is set in this repo's `.git/config`, predating this session (set 2026-09-16 07:41). Confirmed NOT the cause of the run-3/4 aborts (probed: merges succeed fine with it inherited). Still wrong and worth fixing someday.
9. **md-comments** (`c:/DevAI/3rdParty/md-comments`, github.com/md-comments/md-comments, a real decentralized-via-git-refs Markdown commenting product) was mentioned by the user as something they'd cloned; oriented on it (pnpm monorepo, Chrome/Safari extensions, Obsidian plugin) but given NO task yet. User has not said what they want done there. Recommend a separate session — different repo, different conventions.

KEY FILES:
- `docs/plans/41-overwatcher-supply-autoresolve/` — the plan folder (20 tasks; run state under `state/` + `logs/2026-09-16T23-35-25Z-5ee5/`)
- `docs/plans/41-overwatcher-supply-autoresolve.charter.md` / `.md` / `.manifest.json` — design 41 source + handoff
- `tests/Guardrails.Core.Tests/Supply/MissingResourceFactsTests.cs` — the fixed fixture (commit `76f56f63` on the plan branch)
- `.claude/skills/cut-release/SKILL.md` — corrected release procedure (master, PR #748)
- `.github/scripts/release-preflight.sh`, `.github/release-baseline.txt`, `.gitattributes` (eol=lf pin for the baseline) — new release tooling (master, PR #748)
- `.github/workflows/release.yml` — the 11-job pipeline the skill now correctly documents
- `src/Guardrails.Core/Execution/GitWorktreeProvider.cs:208` — the `Integrate()` throw site that discards git stderr (#750)
- `C:\Users\David\.claude\projects\C--DevAI-Guardrails\memory\pre-release-fix-set-after-plan-39.md` — this session's working memory file, extensively updated with every finding, baseline count, and methodology lesson (e.g. "diff against origin/master, never the stale working tree"; "a subagent reporting done is not evidence its work landed"; "git merge-tree mispredicts against a squash-merged base")

CROSS-REPO:
- `c:/DevAI/3rdParty/md-comments` — cloned, oriented, no task assigned yet (see NEXT STEPS #9).

BLOCKERS/NOTES:
- **This machine's memory/git-child-process reliability under this harness's concurrent worktree-mode dispatch (maxParallelism 3) is now suspect after 2 of 4 plan-41 runs ended by the environment rather than the plan.** Not yet root-caused with certainty (leading theory: 32 stale registered worktrees + a large `gr-wt` leaked-root backlog accumulating inside the same junction root every run reuses). Do not retry blindly a 5th time without either the `--fresh` reset or the surgical cleanup.
- **Never trust a "done" report from a subagent without checking `git log origin/<branch>..HEAD` + `git status --porcelain` against ITS OWN worktree.** This bit repeatedly this session — one agent's worktree became permanently unverifiable after holding ~670k tokens of uncommitted work; another held 139 untracked files; a third's stated verification command in a *script meant to prevent unverified claims* could not itself have passed.
- **`dotnet test | tail` / `| head` reports the PIPE's exit code, not the test run's** — caught once, cost a false "exited with code 0" claim on a run that had a real failure.
- **`git show "origin/master:path"` on Git Bash for Windows needs `export MSYS_NO_PATHCONV=1`** or the colon gets rewritten and the read silently returns nothing (no error, just empty — caught this bug 3 separate times in one session despite writing it into a script as trap #1).
- User's standing instructions reaffirmed this session: always hand over the Live Status URL unprompted on every `guardrails run`/resume; stay on latest `origin/master` before starting new work (`pull --ff-only`, never diff against a stale working tree); merge commit (never squash) for anything a corpus/pinned-sha test depends on; tag releases with `git tag` then `git push origin <tag>` as SEPARATE calls; never `cd path && git cmd`, always `git -C path cmd`.
- Attribution for NEW commits/PRs from this point forward in the session: `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>` (changed from the Opus line used earlier in this same session — a mid-session system update, not a user request; if resuming under a different model, re-check the live attribution reminder rather than copying this file's line).
