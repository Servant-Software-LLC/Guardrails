REPO: C:\Dev AI\Guardrails
TOPIC: Guardrails/Preflights-Two-Scope
CONTEXT: Two-scope (plan-level + task-level) preflights/guardrails feature (#183) — the dogfood operation where the Guardrails harness builds itself. This session took it from a fresh plan-breakdown all the way through /guardrails-review, a full harness dogfood run to green, a 4-agent hole-finding audit + lead-verified fixes, a public demo repo, opening the feature PR (#209), and filing every dogfood-surfaced GH issue. The feature branch of record is now feat/preflights-cleanup (PR #209); the earlier feat/preflights-two-scope only adds dogfood-scaffolding commits on top.

COMPLETED:
- (Earlier) Ran /plan-breakdown docs/plans/preflights-impl.md → draft folder, guardrails validate exit 0, guardrails plan confirmed the DAG.
- Origin branch cleanup: 15 stale/merged branches deleted, 4 kept (design/preflight-first-class is a KEEP — the #183 standing design branch).
- Merged the #183 design doc (docs/plans/09-preflight-first-class.md + example/) into the impl branch so task-17 could recast against a real example.
- Ran /guardrails-review on the regenerated plan. Decisions locked: (a) HARD-RETIRE the integrationGate:true task kind → GR2029 hard error; (b) add a full MIGRATION wave; (c) re-home GR2018 content teeth onto <plan>/guardrails/ → GR2028; (d) chose "update the brief + regenerate" over hand-patching. Result: regenerated to a 22-task DAG (supersedes the old 19-task 00–18 numbering in the prior context).
- Folded all guardrails-review findings back into docs/plans/preflights-impl.md (deliverable 12 migration wave, a "Guardrail-strength requirements" section, and per-fix notes: SSOT guardrail on task-05, per-project baseline, GR2028 facts, no-burn covers the token, renderer old-model removal, example meta-test, --nologo ban, #196 Status-not-Attempts, #205 OverlappingWriteScope).
- Dogfooded `guardrails run docs/plans/preflights-impl` to GREEN. Suite on feat/preflights-cleanup: Core 625 pass / Integration 381 pass (1 skip) = 1006 pass, 0 fail.
- Diagnosed every dogfood hiccup as bug-vs-correct-behaviour and filed GH issues (see NEXT STEPS list). Real bugs fixed inline where they belonged to this feature (union-clean regex line-anchoring, --nologo drop, task-14 orphaned-golden writeScope widen, task-08 worktree AttemptRecord test relax).
- 4-agent hole-finding audit over every task deliverable/result + lead verification (grepped TaskPreflightFailed usage, read the "Superseded" fences, re-ran the full suite). Fixed "All 3 HOLES + 2 WEAKs" via the agent team + hands-on lead review.
- Built the public demo repo DaveRMaltby/guardrails-preflights-demo (local clone C:\Dev\guardrails-preflights-demo): TextTools.sln, add-slugify plan, four-folder breakdown, and a run clone — validated BOTH preflight scopes fired (planPreflights passed, planGuardrails passed, task-04 JIT preflight gated its action). Branched so the demo shows a before-state on main and the harness run + PR as the "after".
- Reconciliation + PR: pushed feat/preflights-cleanup to origin, opened PR #209 "Two-scope preflights & guardrails, first-class (#183)" (base master ← feat/preflights-cleanup): https://github.com/Servant-Software-LLC/Guardrails/pull/209
- Filed the diagram-renderer edge-anchoring issue #210 (container→container edges attach to interior invisible anchors, not the box outer edge — arrows appear to pierce the boxes).

IN PROGRESS:
- PR #209 awaiting review/merge into master.
- Pre-merge dogfood-issue cleanup — the user's explicit ask: "for this branch to be complete and to merge into main, we need to resolve all of the GH issues that came out of the dogfooding." Scope-batching proposed but NOT yet confirmed/executed (see NEXT STEPS).

NEXT STEPS:
- Confirm the pre-merge cleanup scope with the user, then execute the agreed set via the agent team + lead review + a final dotnet build/test on feat/preflights-cleanup. Proposed batching:
  - IN this PR (directly complete/harden the feature): #187 (union-regex catalogue), #193 (orphaned-golden review probe), #194 (--nologo catalogue), #196 (worktree AttemptRecord), #205 (#175-attribution port), #207 (GR2028 hardening), #208 (migration residuals), #188 (re-verify logging), #189 (pause-budget default), #198 (model/worktree per-attempt log), #210 (diagram edge anchoring — visual, version-sensitive).
  - SEPARATE PRs (feature-scale, their own effort): #195 (retry salvage), #199 (containment hook), #200/#201 (per-task model override + model-tiering Epic), #190/#191/#192 (rate-limit/journal-staleness, .claude write hatch, stash scoping), #197 (ergonomics), #206 (static-log attempt dropdown), #202/#203/#204 (plan-breakdown snapshot/line-number/maxTurns).
- The user said they will implement the #205 regression "right after we complete this plan, with moderate testing."

KEY FILES:
- docs/plans/preflights-impl.md — the /plan-breakdown source brief (now carries the migration wave + guardrail-strength section + per-fix notes).
- docs/plans/preflights-impl/ — the regenerated 22-task breakdown folder (guardrails.json, baseline, diagram.md/.html, tasks/). NOTE: task numbering was regenerated after /guardrails-review — the old 00–18 stableIds in the prior context are stale.
- docs/plans/09-preflight-first-class.md + docs/plans/09-preflight-first-class/example/ — the #183 design-of-record + worked example (merged in from the design branch).
- src/Guardrails.Core/Execution/TaskExecutor.cs (~L92–124) — task-preflight slot; on fail journals a real AttemptRecord (Outcome=TaskPreflightFailed) and short-circuits BEFORE MarkRunning (no-burn preserved structurally).
- src/Guardrails.Core/Execution/AttemptJournaler.cs (~L396) — new TaskPreflightFailed() writes feedback.md + AttemptRecord.
- src/Guardrails.Cli/Commands/RunCommand.cs — PrintTerminalGateFailure / TryReadPlanGuardrailFailures read planGuardrails.failedChecks and print "FAILED: <name> — <reason>".
- src/Guardrails.Core/Graph/MermaidRenderer.cs — container/anchor emission; target of #210 (subgraph→subgraph vs interior invisible-anchor edges).
- src/Guardrails.Core/Loading/DiagnosticCodes.cs — GR2027+ new codes; GR2028 (re-homed GR2018 content teeth on <plan>/guardrails/), GR2029 (integrationGate hard-retire error).
- docs/plans/02-schemas-and-contracts.md — SSOT (§4.3 union re-verify, §10 renderer). Contract edits land here in the same change.
- tests/Guardrails.Core.Tests/PreflightExampleShapeTests.cs — meta-test (5 facts over the example plan).
- tests/Guardrails.Integration.Tests/OverlappingWriteScopeAttributionTests.cs — #205 legacy-pin, class-level XML doc flags it as a deliberate bypass.

CROSS-REPO:
- DaveRMaltby/guardrails-preflights-demo (local: C:\Dev\guardrails-preflights-demo) — public demo showcase; diagram.html at docs/plans/add-slugify/diagram.html. #210 was surfaced here.
- .claude/skills/plan-breakdown/, .claude/skills/guardrails-review/, .claude/skills/guardrails-domain-knowledge/ — the four-folder migration completed in checklists/field-refs/review-probes; old integrationGate rules kept inside "Superseded" blockquote fences. (Also mirrored under C:\Users\David\.claude\skills\ for the user-scope install.)

BLOCKERS/NOTES:
- Four-folder model: <plan>/preflights/ (Full Flight Checks, pre-DAG) + <plan>/guardrails/ (Terminal Gate, merged HEAD); tasks/<id>/preflights/ (JIT dependency-delivery) + tasks/<id>/guardrails/ (postconditions). integrationGate:true is RETIRED (GR2029); scope:"integration" per-union tag is KEPT.
- F9 outcome split: plan-preflight-failed / task-preflight-failed / plan-guardrail-failed (all exit 2). task-preflight is a TaskOutcome inside tasks{}; the other two live in new top-level planPreflights/planGuardrails sections (planHash-keyed).
- no-burn: a red task-preflight short-circuits to needs-human BEFORE MarkRunning / the attempt loop — it does NOT consume a retry.
- Open dogfood issues: #187–#208 + #210 (model-tiering Epic = #201). Feature PR = #209.
- SAFETY (persist): never `cd <path> && git …` — use `git -C <repo>`. Never run the dogfood harness from the Release binary (Core.dll self-lock — launch via global tool or `dotnet run` Debug). Throwaway worktrees go under %TEMP%\guardrails-worktrees\…, never C:\Dev. Substantial work → agent team + hands-on lead review. Demo box has a GLOBAL GitGuardian pre-commit hook; reset the demo clone by RE-CLONING.
- Model was switched to Sonnet 5 mid-session (via /model) and saved as the new session default.

Resume with: /context-load Guardrails/Preflights-Two-Scope
