REPO: C:\DevAI\Guardrails
TOPIC: Guardrails/False-Green-First-Backlog

CONTEXT:
Two linked arcs. (1) Closed out the Release-Gate-And-Overwatcher-Supply arc: recovered plan 41's
run through multiple halts, delivered it via PR #751 (merged, closes #712), and traced every halt
back to its authoring-time root cause rather than just patching the run artifacts. (2) That recovery
surfaced a recurring defect class — an authored artifact blocking the one task forbidden from
editing it — which led to rediscovering a 2026-09-06 backlog-severity report the user remembered but
could not place ("False Green First"), verifying its current completion state, researching two
candidate backlog items, and agreeing a 5-item priority plan. Now mid-implementation on item 1 (#456).

COMPLETED:
- **Plan 41 fully delivered.** 20/20 tasks green, terminal gate passed, $74.83 total. Root-caused and
  fixed FOUR halts at the source, not just patched: (a) task 02's fixture made 4 rows unsatisfiable →
  fixed the ctor + task 02's prompt; (b) task 12's prompt hardcoded `1 resource(s)` beside a plural
  `<paths>` placeholder → task 13 could only halt; fixed the prompt to say `<n>` = `paths.Count`;
  (c) task 01's proof fixture gave its plan a second leaf with no GR2028 terminal gate, so the run
  died at VALIDATION before `run.json` existed — task 14 burned 3 attempts while its own guardrail
  printed a static "fix Scheduler.cs" blame line that was flatly wrong (9/10 proof rows passed
  throughout); fixed the fixture + task 01's prompt; (d) task 16 correctly reworded a banner, but the
  pinning test was in NO task's `writeScope` → halted the TERMINAL GATE at 20/20 green; gave task 16
  ownership of that test file.
- **PR #751 merged** (`100d09e2`) — all 7 CI checks green across macOS/Windows/ubuntu, closing the one
  verification gap that couldn't be proven locally (the `.sh` guardrail path + GR2027 `catches:` fix).
  Closes #712. Durable prompt fixes (`927ef49c`) confirmed present in the merged tree.
- **Filed #752/#753/#754** (misleading `guardrails supply` remedy; a guardrail blaming a cause it
  never diagnosed; plan-breakdown must give a text-changing task the tests that pin it) + commented on
  #749 with two fresh instances from this run (a red census that can't tell "red because absent" from
  "red because unsatisfiable").
- **Cleanup verified content-based** (the PR was SQUASHED, so `git branch --merged` would lie): deleted
  the merged remote branch `guardrails/41-overwatcher-supply-autoresolve`; audited all 21 `.claude/worktrees/`
  agent worktrees — completely clean, branches all merged; the two lingering `gr-wt` attempt worktrees
  hold nothing master lacks (one sampled diff showed 376 "differing" lines that were `EffectiveThresholdToken`,
  code task 14's OWN prompt ordered deleted — master correctly lacks it by design).
- **Found "False Green First"**: a published Artifact (https://claude.ai/artifact/KtrVo6xxrawL5JHXtZJ1T2,
  updated 2026-09-06), NOT a chartered plan — a backlog severity ranking in 4 bands ("the guardrail
  primitive itself is unsound" → "a check reports false success" → "the run states something false" →
  "unattended runs die/burn budget"). Traced via conversation-transcript search after repo/issue/Charter
  searches all came up empty. This is also where the `triaged: 2026-09` label on ~100 issues comes from.
  **Verified against its own 30 ranked issues (robust parse + non-vacuity control, after my FIRST check
  silently miscounted a grep-miss as "closed"): 25/30 closed, 5 open** — #449 (band 1), #382 (band 2),
  #557 (band 3), #453 + #582 (band 4).
- **Corrected a stale memory**: `guardrails-plan05-false-green-remediation.md` claimed #125 was still
  open; verified closed along with #120/#124/#126 — that arc (plan-05→plan-08, "built-but-unwired")
  is fully done. Rewrote the memory to state the arc is closed and point at its living descendants
  (#382, #478) instead of a stale ledger.
- **Forked research on #478 and #529** (both completed, full reports already synthesized into the plan
  below — see NEXT STEPS for the substance). New registry entry added for this topic.

IN PROGRESS (item 1 of the 5-item plan — #456, "allow --revalidate-task in worktree mode when the
task's work is already integrated on the plan branch"):
- **Design settled and largely implemented, uncommitted, build clean, 6/7 tests passing.** Files
  touched: `src/Guardrails.Core/Execution/GitWorktreeProvider.cs` (made `WorktreeForBranch` public —
  visibility only, locates the plan branch's existing worktree WITHOUT creating one), `TaskExecutor.cs`
  (`RevalidateAsync` gained an optional `workspaceOverride` param), `Revalidate.cs` (replaced the
  unconditional worktree-mode refusal with `ResolveWorktreeVerifyPath`: checks
  `GitWorktreeProvider.ReadPlanBranchTaskHashes` for the task's `Guardrails-Task:` trailer — NOT a
  journal field, which #456 assumed exists but doesn't; refuses with a specific reason for 3 distinct
  situations instead of one misleading "set maxParallelism to 1" message), `RunCommand.cs` (updated
  the `--revalidate-task` description to stop saying "serial mode only").
- **The non-vacuity test (`Revalidate_WorktreeMode_AlreadyIntegrated_VerifiesThePlanBranchTree`) is
  the open problem — THREE built premises falsified in sequence, each for a real reason:**
  1. First attempt: task settles green in worktree mode → assumed that alone gives "integrated + later
     stranded". Wrong: a task whose action succeeds but guardrail fails never green-settles, so
     `SettleGreenIfWorktreeAsync` never calls `provider.Integrate` — no trailer at all.
  2. Second idea: `guardrails reset <taskId>` after a green integrate, to get "integrated but pending".
     Wrong: in worktree mode `RunReset.Task` routes through `ScopedReset`, which REWINDS the plan
     branch on success — destroying the very trailer the test needs. (Journal-only reset is only used
     when NO plan branch exists.)
  3. Third theory (matching #456's own motivating story — a sibling's over-broad guardrail filter
     stranding an already-integrated task): traced `Scheduler.SettleAsync` line-by-line. **Every
     non-FF settle failure path (Conflict, AI-merge-failed, plain union-reverify-failed) does a B1
     "four-effect rollback"** — restores `state.json`, calls `provider.RollbackMerge`, journals
     NeedsHuman — so the commit is UNDONE, not left standing. FastForward integration short-circuits
     re-verify entirely ("FF is free"). So "trailer present + journal non-succeeded" appears
     UNREACHABLE through any single run's `SettleAsync` path — contradicting what #456's own bug
     report describes as having actually happened in the wild.
- **Was investigating, when interrupted for this save:** `RunReset.ScopedReset`'s `Refused` outcome
  (does a refused scoped-reset — "not a provably-safe trailing suffix" — leave the branch AND journal
  in the target state?), and whether any existing `*WiringTests.cs` / integration-test fixture already
  manufactures an integrated-but-not-succeeded task (reuse over invention). Have NOT yet found how
  #456's real-world state (`74ba8b5` an ancestor of the plan branch, task genuinely needs-human)
  actually arises in the current codebase — it may be a SECOND-RUN phenomenon (a resume, a drift
  event) rather than a single-run one, which would mean the test needs two `guardrails run` invocations,
  not one.
- **User's explicit direction: "Option A. Let's do this right."** — i.e. do NOT ship #456 with only a
  refusal-message test; the allow path needs a real end-to-end, non-vacuous proof before this lands,
  even though it's costing more than the "small opener" framing implied. Do not weaken this without
  asking again.

NEXT STEPS (in order — the 5-item plan, confirmed by the user):
1. **#456 (in progress — see above).** Finish the non-vacuity test properly (Option A), or — only if a
   second full investigation also comes up empty — go back to the user with the finding that the
   allow-path may be a multi-run phenomenon and ask how to proceed, rather than silently weakening the
   test.
2. **#754** — plan-breakdown must give a task that changes user-visible text the tests that pin it
   (already prevented today's halt (d) once patched into plan 41's own prompts; #754 makes it a skill
   rule so future plans get it for free).
3. **#753** — a guardrail's failure text must not assert a cause it never diagnosed (prevents halt (c)'s
   3-attempt misdirection class).
4. **#529 Case A only** (self-healing, workspace-only repairs). Full research already done (forked
   agent `ab8ab390a9a4f1f64`, report already synthesized into the prior turn's summary — do not re-fork):
   the safety boundary (`OverwatchFixClassifier`, `PlanDefinitionHash` folding action prompts) is
   ALREADY correct and sufficient; gaps are a write-capable healer role, a workspace `FileEdit`
   allowlist, `auto` still degrading to `prompt` (`Overwatch.cs:25`), no reproduce-before-repair
   vocabulary. **#456 first** unblocks the confirm-a-hand-fix verb `#529` needs, and design 41's
   just-merged certify-then-act structure is the reusable precedent. Of today's 4 halts, only 1 (the
   task-02 fixture) was Case-A-repairable — (b)/(c) are prompt defects (Case B, must always halt), (d)
   is an out-of-scope test. Size: medium.
5. **#382** — a real-seam proof has no declared marker (band-2 item, precondition for GR2061).
6. **Close or rescope #478**, don't build it. Forked research (agent `a39c0c81ddb84574a`) found its
   own title conflates it with #587 (A/B/C checks — A shipped as GR2071, B unbuilt, C declined — that's
   a SEPARATE issue), and #478's own maintainer comments already retire its lever: the "12 of 13
   pre-satisfied" finding that motivated it was 100% a tree-selection artifact (a squash-landed
   breakdown commit), and only ~11% of the 188 candidate clauses even resolve a target file. #479
   already shipped the mechanism that reaches the real population. Building #478 as titled would break
   `ProducerCoverageCorpusTests.Findings()`'s `direct == wired` assertion with zero positive control.
   Side-finding worth a small separate fix: `feat/587-check-b-failure-attribution` sits ~40 commits
   stale on origin with a LIVE GR2072 collision (master allocated GR2072 to `CheckSetPredatesSourceTree`,
   #564) — should be closed/deleted, not built on.

KEY FILES:
- `src/Guardrails.Cli/Commands/Revalidate.cs` — #456's core change (uncommitted); `ResolveWorktreeVerifyPath`
  is the new eligibility gate
- `src/Guardrails.Core/Execution/GitWorktreeProvider.cs` — `WorktreeForBranch` now public (~line 1925);
  `ReadPlanBranchTaskHashes` (line 954) is the read-only trailer query #456 relies on; `SettleAsync`
  (line 5878) is where all three of today's falsified premises were traced — every non-FF failure path
  rolls back (~lines 5940-6041)
- `src/Guardrails.Core/Execution/TaskExecutor.cs` — `RevalidateAsync` (line 571, now takes
  `workspaceOverride`), `ResolveRevalidateWorkingDirectory` (~line 3001)
- `tests/Guardrails.Integration.Tests/RevalidateCliTests.cs` — the fixture needing rework;
  `RevalidatePlan` gained `commitWorkspace`/`MakeGuardrailAlwaysPass`; `Dispose` fixed to use
  `SafeDelete.DeleteDirectory` (was throwing `UnauthorizedAccessException` on Windows loose objects —
  a real, separate bug this session found and fixed regardless of #456's outcome)
- `src/Guardrails.Core/State/RunReset.cs` — `ScopedReset` / `ScopedResetOutcome.Refused` (~line 149) is
  the next thing to read
- `.claude/contexts/Guardrails/Release-Gate-And-Overwatcher-Supply/context-20260918-082933.md` — prior
  arc's full context, now closed out
- `C:\Users\David\.claude\projects\C--DevAI-Guardrails\memory\fresh-reset-destroys-plan-branch-handfixes.md` —
  broadened this session to the general "authored artifact blocks the task that may not edit it" class

CROSS-REPO: (none this session)

BLOCKERS/NOTES:
- **The corrected methodology lesson from #456, worth repeating to any future session touching this
  code:** never infer "the body ran, only teardown failed" from a stack frame position — verify with an
  instrumented assertion. Caught TWICE today doing exactly this kind of premature inference (a CRLF-diff
  artifact read as "real difference"; a shell grep-miss silently counted as "issue closed" in the False
  Green band-check). The fix pattern both times: build a small standalone repro/control BEFORE trusting
  a negative result.
- **Working tree has 5 uncommitted files for #456** (`git status` clean otherwise, master in sync with
  origin). Nothing has been committed for this item yet — the test is the gate before that happens.
- **Two forked-agent research reports (#478, #529) are fully absorbed into NEXT STEPS above** — do not
  re-fork or re-research either; the synthesis is already final, only implementation remains.
- User confirmed timeline pressure: Mac Studio Ultra arrives end of Oct 2026 (~5.5 weeks from
  2026-09-20); this 5-item plan is explicitly meant to leave the harness's guardrail-quality posture in
  good shape before then, not a general backlog grind.
