REPO: C:\DevAI\guardrails
TOPIC: Guardrails/Event-Stream-Webhooks

CONTEXT: What we were working on

Three arcs, all COMPLETE and on master:

1. The #585/#560/#595 three-layer run event stream + a 5-issue stabilization pass — shipped as
   **v1.18.0** (live on NuGet).
2. A backlog triage sweep (the 10 oldest open issues) with a durable tracking label.
3. The **#570 local-inference roadmap** taken to its hardware boundary: Phases A' and B closed,
   plus #298, #230, and **#228 (the escalation ladder)** built end-to-end through the full
   Charter → plan-breakdown → guardrails-review → `guardrails run` pipeline.

COMPLETED:

**Released v1.18.0** (tag on merge `702e9a61`; live on NuGet, GitHub Release + 5 native binaries).
- v1.17.0 was tagged but NEVER published — its build failed and the publish job correctly skipped.
  Tag deliberately LEFT IN PLACE as accurate history.
- **Version scheme is stable `vX.Y.0` ONLY** (release.yml header, #421). Never cut a `.1` patch;
  roll fixes into the next minor. User explicitly approved this reasoning.

**#570 Phases A' and B — PR #620** (`702e9a61`), 3 parallel subagent lanes in TEMP worktrees,
merged with no conflicts, verified solution-wide by the lead (Core 2539/0, Integration 1188/0/4):
- **#577** — the census CORRECTED the issue's own premise. Of 413 no-model rows in the 806-row
  corpus: **319 (77%) are the once-per-task summary row** (correct by construction), 2 are script
  actions, **93 are the real defect** — all pre-boundary `schemaVersion: 1` failure rows, i.e.
  #532's ALREADY-CLOSED population. The recording gap was shut before the work began. Rows now
  carry `modelAttribution`, corpus `schemaVersion` 3, nullable so legacy rows still read. Era
  handled by a DOCUMENTED BOUNDARY, not a backfill.
- **#549** (dry-run called TierResolver), **#517** (poll-gap suspend detector — on Windows NEITHER
  `TickCount64` NOR `Stopwatch` excludes suspend, measured ~15h ahead), **#515** (transient pauses
  journaled), **#532 gap 1** (harnessWrite disposition persisted).

**#619 — PR #621.** `telemetry report` now states attribution coverage above the table,
unconditionally. Denominator is `AttributableTokens` (recorded + cli-default + not-recorded), never
every row.

**#298 — PR #622.** `plan-breakdown` **Step 5d**: resolve a foreseeable `needsHuman` by cheap
research. Framed as the sibling of #578 (that forbids an unmeasured STRUCTURAL claim; this requires
measuring a CAPABILITY claim). Four-part gate; a design choice is a DECISION not research, and
pinning one is strictly worse than the halt it replaces. **NOTE: a skill change takes effect only
after `guardrails skills install --force` AND a session restart.**

**#230 — CLOSED, already shipped.** `RunCommand.PrintTotalCost` has emitted `Per-tier spend:` all
along via `JournalTierSpend` (class doc calls itself `#230-lite`). My triage was WRONG — I grepped
`Execution/` case-sensitively for `tierSpend`; the class is `JournalTierSpend` in `Journal/`.

**#228 THE ESCALATION LADDER — PR #630, merge `bc0859cc`.** Full pipeline, first time end-to-end:
- **Charter** (`docs/plans/228-escalation-ladder.charter.md`), reviewed, both questions answered.
  **Decision A: escalation SHARES the retry pool.** The research flipped my own lean —
  `feedbackPath` is passed to the next attempt REGARDLESS of which model runs it, so escalating
  does NOT trade away the retry-feedback loop; option B never reaches `hard` from an `easy` tag at
  the default budget. **Trigger: `guardrail-failed` ONLY.** Judges out of scope.
- **plan-breakdown** delegated to a fresh agent ON PURPOSE (my session had skill v1.16.0 loaded
  against a v1.18.0 harness; the delta was exactly #608's block-capture re-emit).
- **/guardrails-review** by a NON-AUTHORING agent found **3 BLOCKERs, all measured**, all fixed.
- **`guardrails run`**: 7/7 green, **zero retries**, **$11.96**, ~42 min.
- **Verified shipped code against the charter** (not just green guardrails): trigger scope, budget
  untouched, cap-at-strongest, and `Escalated` keyed on `EscalatedFrom` with `Climbed` excluded.

**Backlog triage — 10 oldest, label `triaged: 2026-09`.** Closed #227 (budget probes, not planned)
and #231 (interactive steering, superseded by direction); #228 raised low→high (then shipped);
#300 retitled to its surviving half; #350 rescoped (seed shipped as the #375 red census; #608 is
fresh evidence for the library half); #351 deferred behind #350; #298/#230/#255/#259 kept.
**Query for what's left: `is:open -label:"triaged: 2026-09"`** (~114 remaining).

**Five contention-sensitive webhook defects fixed this cycle**, three of the sub-shape *a premise
asserted but never established*: `EveryDroppedRowIsCounted` (pump not parked),
`BackoffScheduleIsOneTwoFourWithJitter`, `PerRowCeilingIsFortyFiveSeconds` (100ms discrimination
never available), `CancelledTeardownFitsInsideTheProcessBudget`, and
`TeardownNeverReportsAFailureForARowItNeverSent` (PR #626 — `<= 1` was true PER PHASE; teardown has
TWO sending phases).

**New issues filed:** #619 (done), #624 (`samples verify` never walks PLAN-ROOT guardrails and
reports `OK: 0 verified` instead of admitting it looked nowhere).

IN PROGRESS: nothing. Working tree clean on master at `bc0859cc`.

NEXT STEPS:

- **#570 Phase C — BLOCKED until the Mac Studio arrives 2026-09-22.** `providers check` against real
  MLX (`mlx_lm.server`) and LM Studio; the §6.6 tool-capability probe is the expected failure point
  (plan 28 flags MLX as most likely to diverge); then a real plan with a pinned local judge. No
  loopback fake retires dialect risk (plan 28 §8). Phase D (#544) follows C by design.
- **Resume the backlog sweep** — next 10 via `is:open -label:"triaged: 2026-09"`. **First move on
  every issue must be `git log --grep='#NNN'`**, not a symbol grep (see BLOCKERS).
- **#624** — plan-root sample pairs are never verified.
- **#616** — `BlockerRetry`'s waits are uncancellable past the 15s ceiling.
- **#587** (checks B and C) and **#608** (the census `.Add()`-before-creation defect, still live on
  master) — both half-done, both in the guardrail-quality cluster.
- The **Charter `rationale` type-drop bug** (scratchpad `charter-bug.md`) is still only drafted,
  never filed — it belongs to Charter's tracker.
- **`charter` CLI is 0.26.0; 0.27.0 is published.** Its warning points the WRONG way (it would
  downgrade the newer on-disk skills). `dotnet tool update -g ServantSoftware.Charter` + restart.

KEY FILES (relative to repo root):

- `src/Guardrails.Core/Prompts/EscalationLadder.cs` (new) — `NextRung` caps at
  `ActionTiers.All.Count`; `Apply` declines on pinned/legacy/no-route/null-tier.
- `src/Guardrails.Core/Prompts/TierResolution.cs` — `EscalatedFrom` (non-null IS the signal; one
  field, not a bool+rung).
- `src/Guardrails.Core/Prompts/TierProvenance.cs` — maps `{ EscalatedFrom: not null } =>
  TierSource.Escalated`. **`Climbed` deliberately plays no part.**
- `src/Guardrails.Core/Execution/TaskExecutor.cs` — `guardrailFailedRetries`, incremented ONLY on
  `AttemptOutcome.GuardrailFailed`; retry `budget` untouched.
- `src/Guardrails.Core/Prompts/StallWatch.cs` — poll-gap suspend detector (#517).
- `src/Guardrails.Core/Telemetry/{ModelAttribution,TaskActionKindReader,TelemetryCollectionSwitch,
  TelemetryAttributionCoverage}.cs`.
- `src/Guardrails.Core/Execution/WebhookEventSink.cs` — `LastPumpGraceUsed`, `ComputedBackoffs`,
  `LastPerRowCeilingUsed` (internal test-observables — the house pattern).
- `.claude/skills/plan-breakdown/SKILL.md` — Step 5d (#298).
- `docs/plans/{02-schemas-and-contracts.md,17-model-tiering.md}` — both reconciled by #228 task 07.
- `docs/plans/228-escalation-ladder{.charter.md,/}` — the design of record + task folder.

CROSS-REPO: Charter `rationale` wrong-type silent-drop bug — drafted, not filed.

BLOCKERS/NOTES:

- **`git log --grep='#NNN'` BEFORE designing anything.** Cost me the wrong verdict on #230 this
  session, and 4 cases in an earlier sweep.
- **A grep proves nothing until you check WHAT it matched.** Twice this session: #230 (wrong folder,
  wrong case) and the #228 census verification (counted comment text, nearly flagged correct work as
  wrong).
- **`Closes #A, #B, #C` closes ONLY #A.** Repeat the keyword. Always check issue states after a
  multi-issue merge.
- **Assert the decision, not the duration** — and not a COUNT that stands in for it. Five instances
  this cycle.
- **A `# catches:` line that promises more than its predicates deliver** is the recurring guardrail
  defect — 5 instances in one folder, 4 tracing to two declared exemptions. Declaring an exemption is
  honest but does NOT restore the teeth it gives away.
- **Delegate skill-driven work to a FRESH agent when this session's loaded skill is stale.** Skills
  install to disk but the session keeps the copy loaded at start.
- **Run the adversarial pass with a NON-AUTHORING agent** — it found 3 measured BLOCKERs the author
  missed, and corrected the author's reasoning on a 4th while agreeing with its conclusion.
- **`--no-ui` suppresses the log server**, so a backgrounded run gives no live URL. Omit it and the
  run prints `Live tailing server: http://127.0.0.1:<port>/` plus a live status diagram.
- User rules: designs go to Charter never a draft PR; `git tag` and `git push origin <tag>` as
  SEPARATE calls; never `cd <path> && git ...`; worktrees under TEMP; hand over copy-pasteable run
  commands; keep merge commits for `Guardrails-Task:` provenance (no squash).
