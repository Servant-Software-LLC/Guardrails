REPO: C:\DevAI\guardrails
TOPIC: Guardrails/Event-Stream-Webhooks

CONTEXT: What we were working on

Two arcs, both now COMPLETE and SHIPPED as **v1.18.0** (live on NuGet, GitHub Release with
10 assets):

1. The #585/#560/#595 three-layer run event stream (`events.jsonl`, `GET /events`,
   `--on-event` webhooks, `attach`) plus a 5-issue stabilization pass — this was v1.17.0's
   content, which **never published** because its tag build failed.
2. #570's local-inference roadmap, Phases A' and B — every item that does not require the
   Mac Studio arriving **2026-09-22**.

COMPLETED:

**Released v1.18.0** (tag `v1.18.0` on merge `702e9a61`). Pipeline fully green: 3-OS test
matrix, packaged-tool smoke, NuGet publish, GitHub Release, 5 native binaries.
- **v1.17.0 was tagged but NEVER published** — its build failed on `test (ubuntu-latest)`,
  and the publish job correctly skipped. The tag is deliberately LEFT IN PLACE as accurate
  history (deleting is destructive for no benefit); v1.18.0's notes open by explaining it.
- Version scheme is stable `vX.Y.0` ONLY (release.yml header, issue #421) — every tag has
  been `.0`. Do NOT cut a `.1` patch; roll fixes into the next minor. User explicitly
  approved this reasoning.

**PR #611** — the #585/#560/#595 event-stream feature (merged earlier).

**PR #617** — 5-issue stabilization: #564 (CheckSetProbe + GR2072), #566 (solution-wide CI
job, mirrored into release.yml), #596 (WorktreeModeResolution, tri-state git probe), #597
(delivery banner + `forcedPastDecision`), #603 (derived 15s termination ceiling).

**PR #618** — two contention-sensitive webhook tests that broke the v1.17.0 release gate.
Both failed ONLY under `dotnet test Guardrails.sln` (the concurrent profile #566 added):
- `EveryDroppedRowIsCounted` — asserted a premise it never established (the displacement
  arithmetic needs the pump PARKED in the handler holding `block-head`; nothing waited for
  it). Starve the pump and `block-head` is evicted before it is read, the blocking branch
  never fires, the pump drains concurrently with the writer, and `flood-00000` gets the POST
  the test says it never gets. Fixed with a real barrier (`blockHeadParked` TCS).
- `BackoffScheduleIsOneTwoFourWithJitter` — measured wall-clock gaps. Fixed by exposing
  `WebhookEventSink.ComputedBackoffs` and asserting what the sink DECIDED; bands became
  EXACT (zero slack).

**PR #620** — #570 Phases A' and B. Three parallel subagent lanes in separate TEMP
worktrees, partitioned by file ownership, merged with NO conflicts, verified solution-wide
by the lead: **Core 2539/0, Integration 1188/0/4** under the concurrent profile.
- **#577** — THE key finding, and it corrected the issue's own premise. Of 413 no-model
  rows in the real 806-row corpus: **319 (77%) are the once-per-task summary row** (correct
  by construction — summarizes N attempts, so no single route), **2 are script actions**,
  **93 are the real defect** — and all 93 are pre-boundary `schemaVersion: 1` failure rows,
  i.e. #532's ALREADY-CLOSED population. The recording gap was shut before this work began.
  The live defect was that `model: null` meant three things at once. Rows now carry
  `modelAttribution` (`recorded`/`cli-default`/`script-action`/`task-grain`/`not-recorded`/
  `unknown`), corpus `schemaVersion` 3, nullable so legacy rows still read. Era handled by a
  DOCUMENTED BOUNDARY, not a backfill (a backfill cannot attribute a row whose task folder
  is gone, because script-vs-prompt is read FROM that folder).
- **#549** — `--dry-run` never called `TierResolver`. Second copy of §6.1 DELETED, not
  corrected. Needed `PromptRunnerRegistry.DispatchNameFor` for the full dispatch expression
  (route → action.Runner → frontmatter.Runner → registry default), since TierResolver alone
  still misses the frontmatter tail. Added a TIER column.
- **#517** — the briefed premise was STALE: the Claude-side fix already shipped
  (`a520f9e3`); the live defect was in `OpenAiCompatPromptRunner` (the local-inference
  runner). **Measured on Windows 11, 4.8 days after boot: neither `Environment.TickCount64`
  nor `Stopwatch` excludes suspend** — both ~15 h ahead of `QueryUnbiasedInterruptTime`. The
  obvious monotonic-clock fix would have passed review and changed nothing. Working fix is a
  POLL-GAP detector in a new `StallWatch` (polls at `bound/20`; a poll >4x its interval late
  means the machine was not running). Linux/macOS half is documented, NOT measured.
- **#515** — transient pauses journaled as `tasks.<id>.transientPauses[]`, written BEFORE
  the wait. Task grain (a pause happens between attempt launches, so no AttemptRecord exists
  yet). Bonus find: a `blocker-retried` decision entry existed but was gated behind
  `if (_escalationSink is not null)`, so an ordinary run recorded nothing.
- **#532 gap 1** — `AttemptRecord.harnessWrite` persists requested/applied/disposition/
  reason/entries[]. Gap 2 shipped earlier in `3129919`. The worktree threading
  (`PendingAttempt` + `RecordSucceededSettle`) was initially UNPROVEN — lead review caught
  it; now covered by `Worktree_AppliedHarnessWrite_IsJournaled_NotJustReachedTheSerialPath`.
- **The telemetry flake** — `TelemetryCorpusStore.Append` read the process-global
  `GUARDRAILS_TELEMETRY` opt-out AT WRITE TIME while another xUnit collection set it to
  `off` around an await. Two lanes root-caused it independently. Fixed at the mechanism
  (`TelemetryCollectionSwitch`, constructor state), NOT by serializing the six victims.

**Issue hygiene swept.** Closed with what-shipped-and-where comments: #560, #564, #566,
#596, #597, #603, #535 (stale-open), #577, #549, #517, #515, #532. Corrected the record on
four the v1.17.0 notes overstated: #604/#605 (deliberate deferrals, nothing shipped), #587
(only check A / GR2071 shipped), #608 (only the re-emit fixed).

**#570 updated** — retitled, body rewritten, completion comment added.

**Memory updated:** `github-closing-keywords-bite-both-ways` (new third failure mode),
`local-inference-telemetry-roadmap` (A'/B done), new
`assert-the-decision-not-the-duration`.

IN PROGRESS:

- NuGet index polling for `1.18.0` to appear (publish job succeeded; flat-container index
  lags a few minutes). Verify with:
  `curl -s https://api.nuget.org/v3-flatcontainer/servantsoftware.guardrails/index.json`
  Note 1.15.0 and 1.17.0 are both ABSENT from the published list — both failed their gates.

NEXT STEPS:

- **Phase C of #570 — BLOCKED until the Mac Studio arrives 2026-09-22.** `guardrails
  providers check` against real MLX (`mlx_lm.server`) and LM Studio; expect the §6.6
  tool-capability probe to be where it fails (plan 28 flags MLX as most likely to diverge);
  then a real plan with a pinned local judge guardrail. No loopback fake retires dialect
  risk (plan 28 §8). Phase D (#544) follows C by design.
- **#619** — `telemetry report` does not surface attribution coverage. Worth landing BEFORE
  bring-up week, not during it.
- **#616** — `BlockerRetry`'s waits are uncancellable; Ctrl-C during a blocker-retry pause
  still waits out minutes past the 15s ceiling #603 established.
- **#587** (checks B and C) and **#608** (the census `.Add()`-before-list-creation defect,
  still live on master) — both half-done, both belong to the guardrail-quality cluster.
- The **Charter `rationale` type-drop bug** (scratchpad `charter-bug.md`) is still only
  drafted, never filed — it belongs to Charter's tracker, not Guardrails'.

KEY FILES (relative to repo root):

- `src/Guardrails.Core/Execution/WebhookEventSink.cs` — sink; `LastPumpGraceUsed`,
  `ComputedBackoffs` (both internal test-observables, the house pattern).
- `src/Guardrails.Core/Prompts/StallWatch.cs` (new) — poll-gap suspend detector, `bound/20`
  cadence, `SuspendFactor = 4`; drives BOTH runners.
- `src/Guardrails.Core/Prompts/TierProvenance.cs` (new), `PromptRunnerRegistry.DispatchNameFor`.
- `src/Guardrails.Cli/Commands/DryRun.cs` — now calls `TierResolver`; TIER column.
- `src/Guardrails.Core/Telemetry/` — `ModelAttribution.cs`, `TaskActionKindReader.cs`,
  `TelemetryCollectionSwitch.cs` (all new), `TelemetryRow.cs` (schemaVersion 3).
- `src/Guardrails.Core/Journal/JournalModel.cs`, `RunJournal.cs` — `TransientPauseRecord`,
  `HarnessWriteRecord`.
- `docs/plans/02-schemas-and-contracts.md` (SSOT) — §7 (run.json), §9, §14 (clock
  measurement), §15.2/§15.2b/§15.6 (telemetry attribution).
- `.github/workflows/ci.yml` + `release.yml` — BOTH carry the solution-wide test.
- Scratchpad (session TEMP, may not survive): `release-1.18.0.md` (full notes, also embedded
  in the annotated tag), `pr-570.md`, `570-update.md`, `charter-bug.md`.

CROSS-REPO:

- Charter `rationale` wrong-type silent-drop bug — drafted, not filed. Charter's repo.

BLOCKERS/NOTES:

- **`Closes #A, #B, #C` closes ONLY #A.** Measured on PR #620 — the keyword must be repeated
  before every number. Four issues merged with their fixes and stayed open. ALWAYS check
  issue states after a multi-issue merge.
- **Assert the decision, not the duration.** Four instances this cycle. Never add slack,
  retries, or a serialization attribute to a flaky test — expose what the component DECIDED
  and assert that. Serializing hides exactly the class of defect #566's concurrent profile
  exists to expose.
- **Parallel-lane pattern that works:** partition by FILE OWNERSHIP, one TEMP worktree each,
  tell every lane NOT to run solution-wide (file-lock contention), then the lead runs
  solution-wide after merging them. This caught a reflection-arity regression in the
  previous cycle and an unproven worktree path in this one.
- **Review delegated work hands-on.** Both times the lead asked "is that actually proven?",
  the answer was no (the #532 worktree threading; the #517 stale premise). Agent reports are
  good but not self-verifying.
- User rules in force: design-of-record reviews go to Charter, never a draft PR; `git tag`
  and `git push origin <tag>` as SEPARATE Bash calls; never `cd <path> && git ...` (hook
  blocks it); worktrees under TEMP, never C:\Dev.
- User granted standing autonomy for this session's remaining decisions ("go with your
  recommendations and we can talk about them in the morning").
