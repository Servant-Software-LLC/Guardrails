REPO: C:\DevAI\Guardrails
TOPIC: Guardrails/Verifier-Infrastructure

CONTEXT: What we were working on
The guardrail-strength / verifier arc, continued. This session (2026-08-11) drove the #374 salvage-advice
thread end-to-end: a four-lens agent panel on #427, a WAVED Charter-authored plan
(`salvage-advice-provisioning`), its breakdown + adversarial review, and the FIRST REAL RUN of that plan —
which failed twice and produced four new harness findings. Two harness defects were fixed and released
(v1.1.0, v1.2.0). The run is currently HALTED with 2 tasks at needs-human, blocked on **#437**.

COMPLETED:
--- Prior threads (2026-07-10, carried forward — see context-20260710-123702.md for full detail) ---
- **#349 pilot-seat model provenance** — reviewed brief + generated 11-task/5-wave folder
  `docs/plans/pilot-seat-model-provenance/` (validated, graphed, locked). Still a DRAFT; user drives
  review → /guardrails-review → run. Mechanism: PARSE the CLI-echoed model (init line), never force
  `--model`; `requestedModel` vs `resolvedModel`, mismatch observe-only.
- **#350 specialized deterministic guardrail-script LIBRARY** — ship a `guardrail-lib/` of vetted
  parameterized scripts the breakdown REFERENCES instead of re-authoring per generation. RED case
  collapses to a single guardrail (git self-reconstructs the delta at taskBase); the preflight/guardrail
  PAIR is only needed for the GREEN outcome-baseline.
- **#351 verifier store** — deterministic preflight→guardrail handoff channel; the enabler #350's green
  side needs.

--- This session (2026-08-11) ---
- **#421 stable versioning** — dropped `1.0.0-preview.N` for stable `vX.Y.0`. Starts at **1.1.0** because
  `0.13.0 < 1.0.0-preview.49` under SemVer (a 0.x line would be a NuGet downgrade). Merged as #424.
- **v1.1.0 and v1.2.0 released** (tag-driven, OIDC). Tool + skills installed locally at 1.2.0.
- **#374 salvage advice** (merged #426) — the retry-salvage feedback recommended `git apply` and
  `git checkout <ref> -- <path>`, neither granted by the #252 read-only default. Now leads with
  `git show "<ref>:<path>"`. NOTE: the `--` pathspec theory in the issue was WRONG; so was my first
  "writeScope-enforced at write time" rationale (there is NO write-time writeScope enforcement —
  `WriteScope.IsInScope` has 3 call sites, all retroactive or harness-own).
- **#427 four-lens agent panel** (architect / devil's-advocate / skill-author / harness-developer).
  Verdict: **do NOT grant `git restore`** (3 of 4). Decisive finding: **`allowedTools` is a FLOOR, not a
  ceiling** — Claude Code merges it with the operator's `~/.claude/settings.json` (which grants
  `Bash(git checkout:*)`), so a plan's list can only GRANT, never WITHHOLD. Cost data killed the
  loosening argument (largest real emission = 13% of the output cap).
- **#422/#423** merged (DEPLOYMENT.md nonexistent `-Source` flag; installers → `/releases/latest`, which
  also fixed a pre-existing `set -euo pipefail` abort from `curl | grep -m1`).
- **#433 FIXED + released** — `RetrySalvageTests`' #253 tripwire asserted from an `IClassFixture` Dispose
  that the enclosing repo stayed clean. Inside a Guardrails worktree the harness is concurrently mutating
  that tree → assert fires → surfaces as `TestPipelineException` = NON-ZERO exit with `Failed: 0`.
  Discriminator: linked worktree has `.git` as a FILE, normal checkout a DIRECTORY. Merged #434.
- **#432 FIXED + released** — a FAILED gate persisted NOTHING (terminal-only). There are **FOUR** gate
  folders, all sharing one seam (`IReVerifier`/`GuardrailReVerifier`) that discarded stdout/stderr. Now
  writes `logs/<runId>/[<wave>/]{preflights,guardrails}/<check>/{stdout,stderr,result}` + a top-level
  `halt` record (cleared on resume). Merged #435. Verified with an isolated probe repo (before: nothing;
  after: artifacts + halt). NOTE: my "waves was empty" claim in the issue was an unverified inference —
  corrected in the PR.
- **#253 CONFIRMED** (long-unconfirmed suspicion, now proven) — a live run attributed `outside.txt` and
  `src/output.txt` to an innocent agent (its transcript had NO `dotnet` call at all). The leaked fixture
  files are physically present at `%TEMP%/gr-wt/<hash>/<hash>/_integration/outside.txt`. Mechanism: a
  preflight runs `dotnet test` covering `RetrySalvageTests` in `_integration`; the tests leak fixture
  files there; the write-scope check's `git add -A` blames the agent. Commented on #253 with the repro.
- **Issues filed this session:** #428 (grep spanning multiple source-line literals silently false-passes),
  #429 (#179 re-emit form defeats GR2028's recognizer), #430 (`mark-reviewed --evidence` downgrades to
  `bare` on a RELATIVE path), #431 (reports should end with a copy-pasteable run command), #432, #433,
  #436 (index.html doesn't render the new `halt` record), **#437** (see below).
- **Charter**: updated to 0.13.0 (skills 0.13.0). Filed Charter #124 (drained notes give no visible
  feedback), #125 (`(Recommended)` suffix on `:::question` options), #126 (a "run /plan-breakdown" button;
  depends on Charter #116 — recommend the SIGNAL shape, not spawning). Charter #120 (answer visibility)
  was CLOSED in 0.13.0.
- **`docs/plans/salvage-advice-provisioning`** — Charter-authored (`.charter.md`, 4 decisions folded as
  prose under maintainer delegation), broken down WAVED (4 waves / 14 tasks), adversarially reviewed
  (2 BLOCKERs + 2 WEAKs found and fixed), stamped `review-artifact`.

IN PROGRESS:
- **THE RUN IS HALTED** — `logs/2026-08-11T09-12-47Z-b9ad`, wave-01 at needs-human. Statuses:
  01-correct-retrypolicy-rationale **succeeded** (after a #253 write-scope violation on attempt 1),
  02-fix-ssot-drift **succeeded**, 05-…domain-knowledge **succeeded BUT ON A WEAK GUARDRAIL (false
  green — it now fails the corrected one)**, 03-…plan-breakdown **needs-human**,
  04-…guardrails-review **needs-human**. Waves 2–4 all `pending` behind the barrier.
- **#437 is the blocker and is NOT yet implemented** (this session's next task, per the user).
- The three skill-wording tasks (03/04/05) cannot complete until #437 lands OR the five wording
  corrections are made by hand (see NEXT STEPS).

NEXT STEPS:
1. **Implement #437** — `needsHarnessWrite` demands FULL file content, so it is unusable above ~100KB.
   Proven size gradient in one wave, same prompt/guardrail, only size differing: 82KB **succeeded**,
   104KB **failed** (agent tried a `.claude/settings.json` workaround → permission wall), 204KB **failed**
   (agent halted honestly). Proposed fix: an **anchored edit form**
   `{"needsHarnessWrite": {"path": …, "edits": [{"old": "<verbatim anchor>", "new": …}]}}` so cost scales
   with the CHANGE not the FILE, failing loudly if `old` is absent or non-unique. Also: fail early when the
   target exceeds a workable fraction of `maxOutputTokens`, and add plan-breakdown Step 5b guidance.
   Then push + cut a release (v1.3.0).
2. **Make the five wording corrections by hand** (this session CAN write repo `.claude/` — the wall applies
   to harness subprocesses only), then RESUME (not `--fresh`; 01/02 genuinely succeeded). Tasks 03/04/05
   will then find the work done and pass. The stale claims are in:
   `.claude/skills/plan-breakdown/SKILL.md` (+ `references/schemas.md`, `references/example-breakdown.md`),
   `.claude/skills/guardrails-review/SKILL.md`, `.claude/skills/guardrails-domain-knowledge/SKILL.md`.
   The correct claim: **allowedTools is a FLOOR — it grants and cannot withhold.**
3. Resume the run; waves 2–4 are untouched and unproven.
4. Still open from the prior thread: #349 dogfood (review → run), #350 guardrail-lib, #351 verifier store.

KEY FILES:
- `docs/plans/salvage-advice-provisioning/` — the WAVED plan (4 waves/14 tasks), reviewed + stamped
  `review-artifact`. Review reports under `state/reviews/`. UNTRACKED (not committed).
- `docs/plans/salvage-advice-provisioning.charter.md` — the Charter source (decisions folded as prose).
- `wave-01-correct-the-record/tasks/0{3,4,5}-…/guardrails/01-floor-not-ceiling-wording.ps1` — FIXED this
  session to be whitespace-normalized; the old single-line regex was newline-blind (#428 class, 3rd
  instance) and would have false-passed 2 of 3 files.
- `wave-01-correct-the-record/preflights/0{1,2}-baseline-*.ps1` — rewritten to a THREE-WAY diagnosis
  (`Failed: 0` → not pre-existing breakage; no summary → runner/host died; `Failed: N>0` → genuine red).
- `src/Guardrails.Core/Execution/GateArtifacts.cs`, `src/Guardrails.Core/Journal/RunHalt.cs` — new in #432.
- `src/Guardrails.Core/Prompts/ClaudePermissionScanner.cs` — still blind to Bash refusals (wave 2's target).
- `tests/Guardrails.Integration.Tests/RetrySalvageTests.cs` — #433 fixed here; the #253 LEAK is still
  unfixed (some fixture path is relative to process CWD, not rooted under `Path.GetTempPath()`).
- `%TEMP%/gr432probe/` — the isolated before/after probe repo for gate-failure persistence. Reusable.

CROSS-REPO:
- Charter 0.13.0 installed; issues #124/#125/#126 filed there. Charter #116 (spawn vs signal) gates #126.
- The Guardrails↔Charter `.charter.md` ingestion (plan-breakdown Step 0c) is live and was exercised
  end-to-end this session.

BLOCKERS/NOTES:
- **Self-hosting hazard (the session's biggest lesson).** Running Guardrails ON the Guardrails repo means
  a guardrail that runs this repo's own test suite has SIDE EFFECTS on the tree it is protecting. The #181
  baseline preflight caused BOTH run failures: first misdiagnosing a cleanup fault as red tests (#433),
  then leaking fixture files blamed on an innocent agent (#253). Scope baseline preflights narrowly.
- **`allowedTools` is a FLOOR, not a ceiling** — it grants, it can never withhold. The skills currently
  claim otherwise; that is the wording NEXT STEP 2 corrects.
- **There is NO write-time writeScope enforcement.** `WorktreeContainmentHook` enforces worktree
  containment only; `WriteScopeCheck` is retroactive for BOTH git writes and tool writes.
- `mark-reviewed --evidence` needs ABSOLUTE paths or it silently downgrades to `source: bare` (#430).
- Do NOT re-run with `--fresh` — tasks 01 and 02 genuinely succeeded.
- Three findings this session came from USING the tooling, not reading it: the #302 smoke test caught a
  false-passing guardrail, `validate` caught a fake GR2028 gate, and the stamp caught a silent downgrade.
- A `guardrails run` on this plan costs real money (~$2 for a partial wave-1).
