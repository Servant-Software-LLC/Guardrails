REPO: C:\DevAI\Guardrails
TOPIC: Guardrails/Verifier-Infrastructure

CONTEXT: What we were working on
**The #374 salvage-advice arc is DONE.** After being an accidental bug-finder across four releases
(v1.1.0 → v1.4.0, 11 harness issues), the `salvage-advice-provisioning` plan finally delivered its own
content: **14/14 tasks green across 4 waves**, merged to master as `1aceeb5`, pushed. Total run cost
$50.99 (this run's incremental spend ~$25 for the 6 tasks that actually executed).

This session: cleared the wave-3 halt, ran a review pass that found three BLOCKERs in my own fix,
hardened the guardrails, drove the run to green, merged, and filed three new harness issues exposed by
the delivery itself.

COMPLETED:
--- The wave-3 #193 halt, and the review that corrected my fix ---
- **The halt** was the #193 orphaned-golden trap: wave-3's exit gate filter
  `~ToolGrant|~ClaudePromptRunner` swept in the PRE-EXISTING `ClaudePromptRunnerArgsTests`, whose
  assertions the wave's change invalidates, while wave-03/02's `writeScope` could not edit it.
- **First fix (commit f36d089):** (A) added `ClaudePromptRunnerArgsTests.cs` to wave-03/02's writeScope
  + a re-baseline prompt section; (B) narrowed the gate filter to `~ToolGrantInjection`.
- **An INDEPENDENT adversarial agent then found 3 BLOCKERs, two against that fix.** The central error:
  **(A) and (B) are COMPLEMENTS that I applied as ALTERNATIVES.** (A) grants ownership of the golden;
  (B) removes the obligation to satisfy it — together they left NOTHING in wave 3 verifying the
  re-baseline. My claim that wave 4's full-suite gate would catch it was **wrong twice**:
  `ClaudePromptRunnerArgsTests.cs` is in no wave-4 writeScope (so a bad re-baseline = an UNFIXABLE
  wave-4 halt), and **a DELETED test cannot fail a full-suite gate at all**.
  Proof, same tree same moment: narrow filter `Failed:0/Passed:10`, broad filter `Failed:2/Passed:18`.
- **Resolution (commit 1a7398a):** narrowing KEPT (a wave gate may only assert what a wave task can
  fix), coverage RE-HOMED to task 02 where (A) makes failures fixable. Added:
  `03-golden-args-tests-pass`, `04-golden-coverage-preserved` (anti-deletion/anti-softening, #221 —
  7 `[Fact]` names pinned verbatim, the 8th prefix-matched so the honest rename stays legal),
  `05-ssot-contract-line-landed`, task 03's `03-log-header-echoes-injected-grants`, zero-match guards,
  `Expected:|Actual:|Strings differ` in the #179 re-emit, comment-strip on the forbidden-verb scan.
- **F8 DECLINED on the record** (maintainer's call): task 01's covers-key-behaviors extension would
  have re-run a green, undrifted task for a low-value WEAK.
- **Review addendum 3 stamped**: `state/reviews/review-a8af8b3496fa-2026-08-12T050627Z.md`,
  `attestation.source: review-artifact` (no downgrade). Chain: a3dce557c2f5 → df9db1264998 →
  8a75af7835c7 → a8af8b3496fa.
- **The guardrails WORKED.** The re-baseline landed honestly: exact-equality pin kept with an updated
  expected value, and `NoAllowedTools_OmitsTheFlag` → `NoAllowedTools_StillEmitsTheFlag_CarryingExactly
  TheInjectedGrant` (prefix preserved as the guardrail required), re-pointed to
  `Assert.Equal("Bash(git show*)", …)` — STRONGER than what it replaced.

--- The run, and what it delivered ---
- Waves 1–2 + wave-3/01 skipped (already green); 02/03 re-ran on definition drift; wave 4 ran fresh.
- Delivered: RetryPolicy salvage advice rewritten (patch route first, DiffStat-routed);
  ClaudePromptRunner injects `Bash(git show*)` unconditionally; ClaudePermissionScanner detects Bash
  refusals; AttemptProvenance + TaskExecutor record declared-vs-injected grants; WorktreeContainmentHook
  block message reconciled; PromptComposer advisory; SSOT contract line. ~900 lines of new tests.
- **Suite verified on the MERGED result**: Core 1390/0, Integration 720/0 (2 env-gated skips).

--- Three NEW harness issues filed (all from the delivery failing) ---
- **#447** — `guardrails graph <plan>` regenerates ONLY the plan-level diagram on a WAVED plan; per-wave
  `wave-NN/diagram.md` stay stale, and `graph --check` then falsely returns **exit 0**. Reproduced on a
  throwaway copy. Also means `/guardrails-review` Step 1 trusts a signal that under-reports.
- **#448** — `mergeOnSuccess` refused a 14/14 green run because the tree was dirty — with dirt **the run
  itself created** (regenerating the stale wave diagrams over TRACKED files). The harness passes its own
  run-start F4 preflight, then creates the dirt, then fails on it. Also: the gate is **coarser than
  git's own** — the dirty paths (`docs/plans/**/diagram.*`) were disjoint from the merge's path set;
  `merge-tree` predicted zero conflicts and the manual merge had zero. Suggested narrowing to "do the
  dirty paths intersect what this merge touches?" + naming the offending paths in the message.
- **#449** — comment-blind forbidden-string grep (#97/#98) GENERATED despite the catalogue forbidding
  it: wave-04/04's guardrail greps the raw file for `mine.patch` / `git checkout -- ` while its own
  prompt invites documenting exactly those strings. Cost a full retry (~22 min, ~$2.40): attempt 1
  rewrote both block messages correctly but reintroduced `mine.patch` in its own explanatory comment,
  and pre-existing comment L278 also matched. **Retry feedback was actively misleading** ("the block
  message still names mine.patch" — it didn't; comments did). Four review passes missed this; the issue
  proposes a deterministic lint since the probe exists but isn't reliably applied.

--- Housekeeping ---
- `guardrails/salvage-advice-provisioning` deleted (verified ancestor of master, local-only, clean);
  its stale integration worktree under `%TEMP%\gr-wt\…` removed.
- **#443 is CLOSED** (the prior context wrongly recorded it as open pending the macOS reporter).
  **#444 is OPEN** and tracks re-enabling `EnableCompressionInSingleFile` for `osx-*` — well-formed:
  quantifies the cost (38MB → 83MB Homebrew binary), names the trigger (ship on an SDK carrying
  dotnet/runtime#127355), gives step-by-step verification, and an explicit non-goal (don't remove
  speculatively). Realistically waits for .NET 11.

IN PROGRESS:
- Nothing mid-flight. The arc is closed and merged.

NEXT STEPS:
1. **#447/#448/#449** are the freshest, most concrete harness work — all three were found by USING the
   tool, and #448 undercuts the headline `mergeOnSuccess` default (#340).
2. **10 per-task segment branches** remain (`guardrails/e9fe9329/*`, `guardrails/fa43b34f/*`) —
   deliberately NOT deleted; they are forensic artifacts of the run. Delete when done post-morteming.
3. Still open from prior threads: **#349** pilot-seat dogfood, **#350** guardrail-lib, **#351** verifier
   store, **#442** (env hermeticity — the production-side twin of #253), **#428**, **#429**, **#431**.
4. Consider whether the #382 integration-proof gap is now better or worse evidenced by this run.

KEY FILES:
- `docs/plans/salvage-advice-provisioning/` — COMPLETE. Left byte-for-byte as it ran; the review marker
  is `PlanDefinitionHash`-keyed, so editing a guardrail now would re-stale a marker that correctly
  attests to the definition that actually executed. #449 carries the fix for the NEXT plan.
- `wave-03/tasks/02-implement-grant-injection/guardrails/03,04,05-*.ps1` — the re-homed coverage.
- `tests/Guardrails.Core.Tests/ClaudePromptRunnerArgsTests.cs` — the honestly re-baselined golden.
- `src/Guardrails.Core/Execution/GitWorktreeProvider.cs:1446` — `IsWorkingTreeDirty()`, the #448 gate
  (`git status --porcelain --untracked-files=no`; untracked correctly excluded).
- `src/Guardrails.Cli/Commands/RunCommand.cs:1550` — the merge-refusal message.
- `logs/2026-08-11T16-16-04Z-bab5/` — the full run (both runIds: e9fe9329 then fa43b34f).

CROSS-REPO:
- dotnet/runtime **#127355 / #123324 / #112167** — macOS root cause, fix in .NET 11; tracked by #444.
- Charter 0.13.0; #124/#125/#126 open there; #116 gates #126.

BLOCKERS/NOTES:
- **The session's recurring lesson, earned three more times: EXECUTE, don't reason.** (a) I asserted a
  zero-match `--filter` "fails closed" — execution showed it exits **0**; (b) I reasoned wave 4's suite
  would catch a bad re-baseline — it provably would not; (c) a comment-only `JournalModel.cs` exited 0
  on a guardrail that read fine. Every new/edited guardrail this session was smoke-tested against VALID
  and INVALID samples (#302) before stamping.
- **Do not solo-review your own fix.** The independent agent found 2 BLOCKERs in a change I had just
  reasoned about carefully and described confidently to the user.
- **A wave gate may only assert what a wave task can fix.** That is the durable statement of #193;
  narrowing a gate is right, but the coverage must be RE-HOMED, never dropped.
- `IsWorkingTreeDirty()` excludes untracked by design — a dirty-tree merge refusal is always about
  TRACKED modifications.
- Still true: `allowedTools` is a FLOOR (grants, never withholds); no write-time writeScope enforcement;
  NuGet flat-container index publishes before the search index.
