REPO: C:\Dev AI\Guardrails
TOPIC: Guardrails/Build

CONTEXT: What we were working on
  Building and iterating on the complete Guardrails agentic-engineering harness:
  - A `plan-breakdown` Claude skill converting reviewed markdown plans into DAG task folders
  - A cross-platform C#/.NET dotnet tool (`guardrails` CLI) executing the DAG with retries, state passing, and honest needs-human halts
  - A full `.claude/` ecosystem (agents, knowledge skills, guardrail-review, uber-report)
  - A golden example (`hello-guardrails`) exercising every moving part
  - Dogfood plan (04-dogfood-cost-cap) executed via harness — harness implemented itself
  - All 20 uber-report v1 findings completed; Reality Gate 3/3 PASS; v1.0.0-preview.10 published

COMPLETED:
  - M1: Foundations — plan committed, .gitignore, schemas (02), roadmap (03), domain-knowledge skill, architect agent, hello-guardrails golden example
  - M2: Walking skeleton — PlanLoader/PlanValidator, ProcessRunner + InterpreterMap, serial executor, exit-code guardrails; run + validate commands; CI matrix
  - M3: State + journal + resume — StateManager, JsonMerger, AtomicFile, RunJournal, status/reset commands, per-attempt logs; kill-and-resume proven
  - M4: DAG + parallelism + retry — Channel-based Kahn scheduler, WorkspaceLock, retry+feedback, needsHuman/Blocked, Ctrl+C tree-kill, plan command, Spectre live UI
  - M5: Prompts — PromptComposer, ClaudePromptRunner (claude -p stream-json), IPromptRunner/PromptRunnerRegistry, guardrailOverrides, fake-CLI integration tests
  - M6: Skills — plan-breakdown + references, guardrail-review, all 5 agents, guardrails-dev/domain-knowledge, uber-report, README
  - M7: Polish + packaging — validate depth, cost reporting, --dry-run, --no-ui, NuGet packaging (ServantSoftware.Guardrails), CI + release workflows, dogfood plan (04-dogfood-cost-cap) validate-clean
  - Post-M7: compile-coupled-tests note + workspace-depth note added to skill references
  - Uber-report v1 run — 20 findings identified, slug-keyed, prioritized
  - Dogfood guardrail blocker fixed — 03-tests-untouched.ps1 converted to content-hash (git hash-object)
  - IConsoleIo injectable console seam (issue #4) — IConsoleIo interface, SystemConsoleIo.Instance, StringConsoleIo; all commands adopt
  - guardrails graph command (PR #2) — Mermaid flowchart TD; --check exit 0/2/1; GraphSourceHash SHA-256; diagram.md provenance anchor
  - NuGet Trusted Publishing (OIDC) configured (DavidMaltby profile, no stored key)
  - v1.0.0-preview.1 published (first release)
  - v1.0.0-preview.2 published — graph command + IConsoleIo seam
  - guardrails skills install command + optional <folder> arg on folder-taking commands
  - docs/DEPLOYMENT.md — work-machine runbook
  - 4 library repos (FileBased.DataProviders, SettingsOnADO, SqlBuildingBlocks) refactored to tag-triggered OIDC release; v1.0.1 published
  - Node 20 GitHub Actions bump; NU1903/NU1904 vulnerability fixes
  - PR #21 (LogServer fixes) — three-agent review (harness-dev, test-author, devils-advocate); fixed xUnit1031 (async Task + await using), 127.0.0.1 binding, X-Content-Type-Options + X-Frame-Options security headers, TempPlan.Dispose catch(Exception), FormatDurationTests boundary case; merged → v1.0.0-preview.6 published
  - Lunch & Learn PPTX — 13-slide deck (docs/presentations/guardrails-lunch-and-learn-2026-06-15.pptx) with Tricentis blue/orange palette (#01499B / #F36F21); generated from Node.js build script via html2pptx.js; committed to repo
  - PR #25 (deterministic restore + Directory.Build.props centralisation) — three-agent review; fixed BLOCKER (release.yml missing --locked-mode on test-job restore + publish job had no restore step at all); added RestoreLockedMode conditioned on $(CI); clarified AnalysisLevel comment (doesn't cover NuGet-shipped analyzers); added MSBuild traversal note; fixed trailing newlines in all 4 packages.lock.json files; 242+117 tests green; merged → v1.0.0-preview.8 published
  - P1/P2 uber-report remediation (PR #29) — ISchedulerJournal.CurrentCostUsd default impl + multiple P1/P2 findings resolved
  - P3 batch findings (PR #30) — 7 slugs fixed: logs-status-mapping-untested-e2e, readme-exit2-and-command-table-drift, transcript-truncate-splits-surrogates, transcript-determinism-overclaim, logserver-perf-rereads-file-per-poll, logserver-port-probe-toctou, p3-task-executor-split
  - Reality Gate #3 PASS — plan-breakdown round-trip of hello-guardrails.md passes `guardrails validate`; Reality Gate now 3/3 PASS
  - Dogfood execute (docs/plans/04-dogfood-cost-cap, PR #31) — harness implemented maxCostUsd cost-cap: GR2012 validator, Scheduler halt before lock, RunJournal.CurrentCostUsd (lock-guarded), 10 new tests; honest-halt + human-resume demonstrated; dogfood cost $12.66
  - v1.0.0-preview.10 published (tag v1.0.0-preview.10, 3-OS CI green)
  - Uber-report finalized (PR #32) — all 20 findings "completed"; findings.json head "8ff6dee", realityGate 3/3, planBreakdownVerifiedOn 2026-06-15, dogfoodCostUsd 12.6606
  - All temp worktrees torn down; main checkout clean

IN PROGRESS:
  - Nothing. All open threads closed. Release pipeline for v1.0.0-preview.8 was in_progress when context was saved (triggered by tag push); should have completed by now.

NEXT STEPS:
  1. Deploy to Tricentis work computer — install `ServantSoftware.Guardrails --prerelease` + `guardrails skills install`; run on a real Tricentis work plan
  2. Run uber-report v2 — M1-M7 complete + Reality Gate 3/3; clean-slate assessment of what's next
  3. guardrail-review skill gap: did not catch tests-untouched hidden-state anti-pattern during M7; needs stronger hidden-state check or battle-testing
  4. WorkspaceLock worktree-per-task parallelism — v2 scope
  5. p2-ssot-guardrail-artifacts (per-guardrail stream/composed-prompt doc in SSOT §8; fix §2 {script} token) — carried forward from P2 findings

KEY FILES:
  - .claude/tasks/2026-06-15-uber-report-v1.md — finalized status report (all 20 findings completed, Reality Gate 3/3, v1.0.0-preview.10)
  - .claude/tasks/2026-06-15-uber-report-v1.findings.json — findings index (head: 8ff6dee, all completed)
  - .claude/contexts/_registry.md — context topic registry
  - docs/plans/02-schemas-and-contracts.md — SSOT (§2 maxCostUsd cost-cap, §7 exit codes, §8 per-guardrail streams, §10 graph)
  - docs/plans/03-roadmap.md — M1–M7 exit criteria, Reality Gate, v2 bets
  - docs/plans/04-dogfood-cost-cap/ — executed 4-task dogfood plan folder (all tasks green)
  - docs/DEPLOYMENT.md — work-machine runbook
  - docs/presentations/guardrails-lunch-and-learn-2026-06-15.pptx — 13-slide Lunch & Learn deck (Tricentis palette)
  - docs/presentations/lunch-and-learn-2026-06-15.md — source markdown for the PPTX
  - Directory.Build.props — repo-wide build policy (ImplicitUsings, Nullable, TreatWarningsAsErrors, AnalysisLevel=8.0, RestorePackagesWithLockFile, RestoreLockedMode on CI)
  - .github/workflows/release.yml — OIDC Trusted Publishing; tag v* → 3-OS test → locked-mode restore → pack → push (fixed in PR #25)
  - .github/workflows/ci.yml — 3-OS matrix; --locked-mode restore
  - src/Guardrails.Core/Model/RunConfig.cs — MaxCostUsd decimal? property
  - src/Guardrails.Core/Execution/Scheduler.cs — CostCapHaltFor check before lock
  - src/Guardrails.Core/Journal/RunJournal.cs — CurrentCostUsd() lock-guarded
  - tests/Guardrails.Core.Tests/CostCapSchedulerTests.cs — FakeJournal; cap/no-cap/exceeded scenarios
  - tests/Guardrails.Integration.Tests/LogServerTests.cs — fixed xUnit1031 + TempPlan.Dispose (PR #21)
  - src/Guardrails.Cli/Ui/LogServer.cs — 127.0.0.1 binding + security headers (PR #21)
  - .claude/skills/plan-breakdown/SKILL.md + references/ — 8-step procedure
  - .claude/skills/guardrails-domain-knowledge/SKILL.md — SELF-UPDATING; maxCostUsd cost-cap bullet added
  - .claude/agents/ — 5 agents

CROSS-REPO:
  - C:\Dev\FileBased.DataProviders — tag-triggered release.yml (OIDC); v1.0.1 published
  - C:\Dev\SettingsOnADO — tag-triggered release.yml (OIDC); v1.0.1 published; DataProtection → 10.0.9
  - C:\Dev\SqlBuildingBlocks — tag-triggered release.yml (OIDC); v1.0.1 published
  - C:\Dev\MockDB — tag-triggered release.yml BLOCKED (no NuGet publish — no obfuscation/licensing; see memory mockdb-do-not-publish-nuget.md)

BLOCKERS/NOTES:
  - Latest published NuGet: v1.0.0-preview.10 (from PR #32 uber-report finalization). v1.0.0-preview.8 (PR #25) and v1.0.0-preview.6 (PR #21) are superseded. Tag sequence: .6 (LogServer fixes) → .7 (parallel session) → .8 (lock files) → .9/.10 (uber-report/dogfood).
  - Reality Gate: 3/3 PASS (build+test, walking skeleton, plan-breakdown round-trip all verified).
  - Dogfood: fully executed and merged. Honest-halt → human → resume loop demonstrated: `.claude/` write boundary caused task 03 to halt needs-human; human applied it; guardrail verified on resume. Memory saved (dogfood-claude-dir-write-boundary.md).
  - guardrail-review skill gap persists: did not catch tests-untouched hidden-state anti-pattern during M7; stronger check needed.
  - WorkspaceLock: prompt actions exclusive by default; worktree-per-task parallelism is v2. No validator warning for `exclusive: false` on prompt action (minor foot-gun).
  - Key design decisions locked: flat dependsOn, snapshot-in/fragment-out, claude -p for prompts, dotnet tool distribution.
  - Dev note: never run installed `guardrails` tool while testing — always use `dotnet run --project src/Guardrails.Cli`.
  - Worktree workflow: always create a dedicated worktree per session; tear down on merge. See memory use-dedicated-worktree.md.
  - Release-build self-lock: dogfood harness run must launch via `dotnet run` (Debug), not the Release binary (which locks Core.dll and breaks the build guardrail).
  - PR #25 locked-mode note for contributors: to update a package, bump the reference then run `dotnet restore --force-evaluate`, commit the updated lock files.
