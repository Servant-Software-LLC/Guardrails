REPO: C:\Dev AI\Guardrails
TOPIC: Guardrails/Build

CONTEXT: What we were working on
  The Guardrails agentic-engineering harness is a mature, released dotnet tool
  (`ServantSoftware.Guardrails`). This session (2026-07-08) was a full DOGFOOD-BACKLOG
  REMEDIATION + RELEASE cycle:
  - Dogfooded the `diagram-live-status-and-search` plan (#219/#220 diagram features) via
    /plan-breakdown → /guardrails-review → `guardrails run`. The run surfaced a cascade of real
    harness/skill bugs on Windows/Git-Bash.
  - Fixed the ENTIRE resulting dogfood backlog — 20 PRs merged — then cut NuGet
    v1.0.0-preview.36 and authored a `cut-release` skill.
  - Prior Build context was preview.18-era (2026-06-17). The intervening releases (.19–.35:
    four-folder model, worktree-per-task parallelism, preflights/terminal-gate, model-tiering
    scaffolding, diagram features) are captured in the sibling contexts
    Guardrails/Model-Tiering-Epic (2026-07-07) and Guardrails/Preflights-Two-Scope (2026-07-03),
    not re-derived here.

COMPLETED:
  --- Historical (M1–M7 → v1.0.0-preview.18, prior Build threads) ---
  - M1–M7 harness build: PlanLoader/PlanValidator, ProcessRunner+InterpreterMap, Channel-based Kahn
    scheduler, WorkspaceLock, retry+feedback, needsHuman/Blocked, StateManager/JsonMerger/AtomicFile/
    RunJournal, PromptComposer+ClaudePromptRunner, guardrails graph (Mermaid), NuGet packaging
    (ServantSoftware.Guardrails), CI + OIDC-Trusted-Publishing release workflows, hello-guardrails golden
    example, all 5 agents + knowledge skills. Uber-report v1: 20 findings completed, Reality Gate 3/3.
  - Post-M7 through preview.18: TDD default (#36), hash-in-state (#38), framework-selection (#43),
    streaming logs (#44), harness-computed captureHashes (#47), single-writer-per-key (#50), opt-in
    restoreOnRetry (#53). NuGet Trusted Publishing (OIDC, DavidMaltby profile, no stored key).
  - (Intervening .19–.35 work: see Model-Tiering-Epic + Preflights-Two-Scope contexts.)

  --- 2026-07-08 session → v1.0.0-preview.36 (20 PRs merged) ---
  Trigger: dogfooded `docs/plans/diagram-live-status-and-search` (untracked draft plan for #219/#220
  diagram live-status + search). Ran /plan-breakdown, /guardrails-review (applied 2 fixes: maxTurns +
  a dependsOn parallelism edge, marked reviewed), then `guardrails run` on Windows/Git-Bash — which
  surfaced the backlog below. The run itself never fully completed #219/#220 (it was the bug-finding
  vehicle); those features remain open.

  Dogfood-surfaced fixes (all merged, each 3-OS-CI-green + independently re-verified):
  - #253 — write-scope check surfaces A/M/D git status + forensic preview for new offending files (the
    `git add -A` false-attribution investigation; root cause never confirmed, diagnostics added).
  - #250 — guardrails-review: the #125 union-safe decision test must consider ALL plan-wide unions, not
    just unions feeding the guardrail's own task (this was a real mistake made during THIS session's own
    review; also fixed the plan-breakdown generation-time root cause).
  - #248 — guardrails-review: new probe to verify a pattern-matching guardrail against REAL tool output
    (found live: a `tests-fail-on-stubs` regex assumed xUnit "Passed: N, Failed: M"; it's the reverse →
    the guardrail could never match; 4 identical scripts affected).
  - #221 — plan-breakdown + guardrails-review + catalogue: an explicit prose "do NOT …" prohibition needs
    a matching structural guardrail (perverse-incentive angle where an empirical guardrail REWARDS the
    forbidden shortcut).
  - #249 — `guardrails graph` emits the diagram.html link itself (OSC 8 via RunCommand.Hyperlink); fixes
    the MSYS `/f/…` vs native `F:/…` path form.
  - #251 — catalogue: absolute-path resolution for tools that self-canonicalize (bats relative-path fails
    on Windows/Git-Bash) + bats TDD-red must exclude synthetic `bats-gather-tests` markers.
  - #252 — plan-breakdown default `allowedTools` includes read-only git (log/diff/show/status) for
    multi-task plans; Part 2 (misleading "multiple operations" rejection message) confirmed NOT this repo's
    code (Claude Code CLI's own message).
  - #266 — HARNESS: prompt-output staging. A plan under `.claude/` couldn't run prompt tasks (Claude
    Code sensitive-path block on the state-out fragment). Fix: stage STATE_OUT/VERDICT_OUT under
    `<workspace>/.guardrails-agent-io/…` (never `.claude/`, inside the worktree-containment root), harness
    promotes to the final path after the sub-agent exits. Architect-designed; the "just grant permission"
    option was empirically DISPROVEN (5 live experiments) before landing this.
  - #263 — HARNESS: on Windows, bash-invoked script guardrails get GUARDRAILS_* paths in forward-slash
    form (backslash corrupts when interpolated into node -e/regex/sed). Gated to Windows + bash-resolved
    interpreter only (PowerShell keeps native backslash).
  - #262 — HARNESS: write-scope matcher now claims dotfile literals (`.gitignore` was misclassified as a
    directory by the file-vs-dir heuristic → out-of-scope → needs-human).
  - #264 — HARNESS: deterministic script-action retries short-circuit to needs-human on byte-identical
    reproduction (sibling of #174/#182 no-op short-circuit) + script-appropriate retry feedback.
  - #258 — HARNESS: scaffold a plan-root `.gitignore` (denylist of the RunReset.Fresh transient set:
    /logs/, /state/run.json, /state/state.json, /state/merge-conflicts.log, /state/captured/) at
    StateManager.Initialize; non-clobbering. Whitelists committed state/seed.json + guardrails-review.json.
  - #256 — plan-breakdown Step 7.4 delivers the interactive-diagram link as a Markdown link (markdown
    hosts don't linkify OSC 8); the CLI redirected-fallback now emits a `file://` URI (not a bare path) so
    the skill can wrap it. Distinct from #249 (which fixed the URI form).
  - #272 — HARNESS: plan-gate failure `reason` carries the failure TAIL (the #179 treatment for plan-level
    gates, via new GuardrailFailureReason.Tail) instead of an early stdout line (npm ci noise); + the
    overlapping-writeScope collision hint is now HEDGED (overlap is expected for TDD stub+impl; failure
    detail is the primary signal).
  - #260 — HARNESS: review attestation keys on a NEW `PlanDefinitionHash` (covers guardrail/preflight/
    action BODIES + sidecars), NOT the narrow `PlanHash` (widening PlanHash would false-halt its
    load-bearing pre-DAG PlanPreflightPhase SKIP). Introduced the shared `TaskDefinitionFiles` enumerator
    (reused by #274 Part A). Consolidated newline-normalization into `HashText`. Architect-designed +
    SSOT-first draft (#286, superseded by impl #288).
  - #274 Part B — HARNESS: `--fresh`/`reset` now tear down the plan branch (`guardrails/<plan>`) via
    GitWorktreeProvider.TeardownPlanBranch. Root cause: the durable resume record is the plan-branch
    commit trailers, which the old --fresh never cleared (→ the "nuke the whole run 3×" pain).
  - #274 Part A — HARNESS: definition-drift halt. On resume, recompute each pre-settled-green task's
    `TaskDefinitionHash` (over TaskDefinitionFiles) vs the hash recorded at settle (journal +
    `Guardrails-Task-Hash:` trailer); mismatch → RunReport.DefinitionDrift, exit 2, with a RICH per-file
    "what changed" report (added/removed/modified, git diff command, dependent closure). HALT-ONLY.
    Architect-designed; went through an ADVERSARIAL REVIEW (guardrails-devils-advocate) — no blocker; one
    WEAK finding fixed (unguarded pre-pass File.ReadAllText outside the #150 honest-halt capture → a
    transient Windows file-lock would crash a healthy resume; now degrades to an honest abort). Also fixed
    the dry-run trailer-parity NIT.
  - 2 skill-consistency syncs: (#289) hedged-collision-hint wording + #264 short-circuit note + #260
    PlanDefinitionHash correction across guardrails-domain-knowledge / guardrails-review / catalogue;
    (#292) the #274 Part A drift-halt note in guardrails-domain-knowledge Resume coverage.

  Release + tooling:
  - Cut v1.0.0-preview.36 (annotated tag at master a5acd87 → release.yml → 3-OS test + packaged-tool-
    smoke + publish via OIDC, all green). Published to NuGet.
  - Added `cut-release` skill (.claude/skills/cut-release/SKILL.md, PR #293) — maintainer-only, NOT in
    the packed set. Documents the tag-triggered release procedure + gotchas.

  Filed for the team (NOT implemented this session):
  - #273 — SSOT §9.3's documented `.claude/` write-permission remedy (#104) no longer works vs current
    Claude Code (empirically re-tested).
  - #275 — adopt a `.guardrails/` folder as the default home for plan folders + runtime state (spun off
    from #266's design; overlaps #258). The maintainer is ALREADY using `.guardrails/` for Wave 2.
  - #277 — flaky LogServerTests.CallerChosenPort_BindsToExactlyThatPort (port-binding contention).
  - #290 — skill terminology drift: retired `integrationGate` term + stale "next-free GR code" line in
    guardrails-domain-knowledge / guardrails-dev-knowledge.
  - Deleted 2 spam comments (on #255, #263) — brand-new accounts posting zip "patch" bait that parroted
    the issue text; user authorized each deletion; reported via GitHub UI (no API for report).

IN PROGRESS:
  - Wave 2 (work laptop, Tricentis): pending install of v1.0.0-preview.36 + a fresh /plan-breakdown from a
    `.guardrails/`-located plan (the DFD/threagile-substrate Wave 2). The `.guardrails/` location sidesteps
    the #266 `.claude/` block entirely.
  - #274 Part C (approved, deferred, NOT started): the safety-check + DESTRUCTIVE plan-branch rewind
    primitive, exposed as (i) a manual scoped `guardrails reset <folder> <taskId>` AND (ii) an opt-in /
    interactive-confirmed run-time AUTO-RESOLVE of the provably-safe (trailing-suffix) drift case; the
    unsafe fan-in case always halts. Gets its OWN design→draft-PR review because the rewind is destructive.
  - Two untracked draft plan folders present all session: docs/plans/diagram-live-status-and-search/ (the
    #219/#220 dogfood vehicle — partially run, 04/06 hit needs-human) and docs/plans/harden-flaky-worktree-test/.

NEXT STEPS:
  1. Work laptop: install `ServantSoftware.Guardrails --version 1.0.0-preview.36`, run Wave 2's
     /plan-breakdown (under `.guardrails/`) → /guardrails-review → run. Expect Wave 2 to be the real-world
     dogfood that validates #274 Part A (and likely surfaces the next backlog, as Wave 1 did).
  2. #274 Part C: dispatch its dedicated design pass → SSOT-first draft PR → implement (reuses the
     TaskDefinitionFiles primitive + the trailing-suffix safety check; the architect's full Part C design
     is already specified in the refinement pass this session).
  3. Address the filed cleanups when convenient: #290 (terminology/GR-code — confirm true next-free GR
     first), #275 (.guardrails/ convention design), #273 (stale #104 guidance), #277 (flaky test).
  4. #219/#220 diagram features themselves are still unimplemented (the plan run was the bug vehicle) —
     re-run diagram-live-status-and-search once the harness is stable on Wave 2, or fold into a wave.

KEY FILES:
  - .github/workflows/release.yml — tag `v*` → 3-OS test + packaged-tool-smoke → OIDC Trusted-Publishing
    pack+push. Version derived from tag (strip `v`).
  - .claude/skills/cut-release/SKILL.md — NEW this session; the release procedure (maintainer-only, not packed).
  - docs/plans/02-schemas-and-contracts.md — SSOT. This session added: §9.5 (prompt-output staging, #266),
    §5.1 write-scope A/M/D (#253), §3.4 dotfile write-scope (#262) + hedged collision hint (#272), §7 plan-gate
    reason tail (#272), §7.3 PlanDefinitionHash (#260), §7.2 definition-drift halt (#274 Part A), §5.3
    Guardrails-Task-Hash trailer, §6.1 plan-branch teardown (#274 Part B), §1 scaffolded .gitignore (#258).
  - docs/plans/03-roadmap.md — Part A shipped / Part C deferred line (#274).
  - src/Guardrails.Core/Journal/ — PlanDefinitionHash.cs, TaskDefinitionFiles.cs (shared #260/#274 enumerator),
    TaskDefinitionHash.cs; Hashing/HashText.cs (consolidated newline-normalize + framing).
  - src/Guardrails.Core/Execution/ — PromptOutputStaging.cs (#266), WriteScope.cs (#262 dotfile + #272 hedged
    hint), WriteScopeCheck.cs (#253 A/M/D), WindowsBashPaths.cs (#263), GuardrailFailureReason.cs (#272),
    DefinitionDriftReporter.cs (#274 A), Scheduler.cs (drift pre-pass + honest-abort guard),
    GitWorktreeProvider.cs (TeardownPlanBranch #274B + trailer read/write #274A), TaskExecutor.cs, RunReset.cs.
  - src/Guardrails.Core/Review/ReviewMarker.cs — keys on PlanDefinitionHash (#260).
  - src/Guardrails.Core/State/StateManager.cs + PlanGitignore.cs — .gitignore scaffold (#258).
  - src/Guardrails.Cli/Commands/GraphCommand.cs (#249/#256), RunCommand.cs + DryRun.cs (#274 A rendering).
  - .claude/skills/{plan-breakdown,guardrails-review}/SKILL.md + references/guardrail-catalogue.md — doctrine
    updates (#250, #248, #221, #251, #252, #256, #260, #272 hedge, #264 note, #274 A note).
  - .claude/contexts/_registry.md — topic registry (unchanged this session).

CROSS-REPO:
  - Work laptop (Tricentis): now on the DFD/threagile-substrate Wave 2 (fresh plan under `.guardrails/`).
    The prior preview.18-era 0003-commander-desktop-pivot migration is long superseded.
  - C:\Dev\FileBased.DataProviders / SettingsOnADO / SqlBuildingBlocks — tag-triggered OIDC release; v1.0.1.
  - C:\Dev\MockDB — NuGet publish BLOCKED (no obfuscation/licensing; memory mockdb-do-not-publish-nuget.md).
    NOTE: that block is MockDB-specific; Guardrails publishes previews freely.

BLOCKERS/NOTES:
  - Latest published NuGet: v1.0.0-preview.36 (this session; the whole dogfood backlog). Cutting a release =
    push a `v1.0.0-preview.<N+1>` tag at a pushed, CI-green master HEAD; see the cut-release skill.
  - Release gotcha (learned this session): poll the release run's TOP-LEVEL `.status`, not a nested job — the
    packaged-tool-smoke job shows "completed" while the run is still in_progress.
  - #274 staging rationale: shipped Part A (halt, non-destructive) now; Part C (auto-resolve, DESTRUCTIVE git
    rewind) deferred to its own review. Released after Part A — Part B provides the escape hatch, and Wave 2
    is a better validator of Part C than rushing it. The drift halt's per-file "what changed" report was
    added specifically so a human authorizing a rebuild sees WHICH files drifted, not just that a hash moved.
  - #260 rationale: a SEPARATE PlanDefinitionHash (not widening PlanHash) — widening would false-halt the
    load-bearing pre-DAG PlanPreflightPhase SKIP. The three hashes nest: PlanHash (structure/config, resume) ⊂
    PlanDefinitionHash inputs (review marker) ⊇ each TaskDefinitionHash (per-task drift). They share the
    TaskDefinitionFiles enumerator so they can't drift.
  - #266 rationale: the "grant a scoped permission" workaround is empirically DEAD vs current Claude Code
    (the `.claude/` block sits above permission-rule evaluation). Fix stages I/O outside `.claude/`. This also
    stales the #104 guidance (→ #273).
  - Windows file-lock class is real and recurring here (worktree-cleanup permission-denied all session;
    Release-build self-lock memory). #274 Part A's WEAK finding (unguarded pre-pass IO) was fixed precisely
    because of it — a transient lock now degrades to an honest abort, not a crash.
  - Machine had transient resource pressure this session (fork/OOM errors; a couple of background agents
    exited mid-task and auto-restarted from transcript — recovered cleanly each time).
  - Process discipline that worked well (per user directives / memory): substantial work → delegated agents
    (harness-developer / skill-author / architect / devils-advocate), each in its own worktree; lead
    independently re-verifies (own build+test + read the diff) before push; PR + 3-OS CI poll + squash-merge
    only when green; ff-only sync of main; the release headline (#274 Part A) additionally got an adversarial
    review. Big/contract changes go SSOT-first as a design→draft-PR before implementation (#106 loop).
  - Dev notes (still valid): never run the installed `guardrails` tool while testing — use
    `dotnet run --project src/Guardrails.Cli`. Dogfood runs launch via Debug (Release binary self-locks
    Core.dll). Throwaway worktrees go under %TEMP% clarifying root, never C:\Dev. Use `git -C <repo>`, never
    `cd && git`.
  - Reality Gate historically 3/3 PASS. Key design decisions locked: flat dependsOn, snapshot-in/fragment-out,
    claude -p for prompts, dotnet-tool distribution, four-folder preflights/guardrails model, OIDC releases.
