REPO: C:\Dev AI\Guardrails
TOPIC: Guardrails/Build

CONTEXT: What we were working on
  Building and iterating on the complete Guardrails agentic-engineering harness:
  - A `plan-breakdown` Claude skill converting reviewed markdown plans into DAG task folders
  - A cross-platform C#/.NET dotnet tool (`guardrails` CLI) executing the DAG with retries, state passing, and honest needs-human halts
  - A full `.claude/` ecosystem (agents, knowledge skills, guardrail-review, uber-report)
  - A golden example (`hello-guardrails`) exercising every moving part
  - A `guardrails graph` command emitting Mermaid DAG diagrams with `--check` staleness detection
  - NuGet Trusted Publishing (OIDC) release pipeline; two preview packages shipped

COMPLETED:
  - M1: Foundations — plan committed, .gitignore, schemas (02), roadmap (03), domain-knowledge skill, architect agent, hello-guardrails golden example
  - M2: Walking skeleton — PlanLoader/PlanValidator, ProcessRunner + InterpreterMap, serial executor, exit-code guardrails; run + validate commands; CI matrix
  - M3: State + journal + resume — StateManager, JsonMerger, AtomicFile, RunJournal, status/reset commands, per-attempt logs; kill-and-resume proven
  - M4: DAG + parallelism + retry — Channel-based Kahn scheduler, WorkspaceLock, retry+feedback, needsHuman/Blocked, Ctrl+C tree-kill, plan command, Spectre live UI
  - M5: Prompts — PromptComposer, ClaudePromptRunner (claude -p stream-json), IPromptRunner/PromptRunnerRegistry, guardrailOverrides, fake-CLI integration tests
  - M6: Skills — plan-breakdown + references, guardrail-review, all 5 agents, guardrails-dev/domain-knowledge, uber-report, README
  - M7: Polish + packaging — validate depth, cost reporting, --dry-run, --no-ui, NuGet packaging (ServantSoftware.Guardrails), CI + release workflows, dogfood plan (04-dogfood-cost-cap) validate-clean
  - Post-M7: compile-coupled-tests note + workspace-depth note added to skill references
  - Uber-report v1 run (ee73bbc) — 10 findings, 7 open slug-keyed next actions, Reality Gate partially evaluated
  - Dogfood guardrail blocker fixed (3307283) — 03-tests-untouched.ps1 converted from git-diff-HEAD to content-hash (git hash-object) comparison; immune to fresh-clone baseline + commit-timing attacks
  - IConsoleIo injectable console seam (issue #4) — IConsoleIo interface (Out/Error TextWriters), SystemConsoleIo.Instance, StringConsoleIo (tests), CommandFactory.BuildRootCommand(io), all commands adopt XCommand.Create(io); merged to master
  - ConsoleCaptureCollection deleted; CliExitCodeTests + StatusResetCliTests migrated to StringConsoleIo
  - guardrails graph command (PR #2, merged) — Mermaid flowchart TD diagram emitted to diagram.md; MermaidRenderer.Render + SemanticContent share AppendNodesAndEdges; GraphSourceHash = SHA-256 of SemanticContent; AllocateNodeIdBases injective; Quote escapes & < > " # and collapses newlines; ComposeDocument anchors provenance comment to line 1 (no generated= timestamp); --check exit codes 0/2/1 (fresh/stale/error; SSOT §7); structure-only caption on diagram.md
  - GH issue created for Console.SetOut → injectable TextWriter long-term fix
  - Dedicated git worktree workflow established (worktree per session, tear down on merge); memory saved to C:\Users\David\.claude\projects\C--Dev-AI-Guardrails\memory\use-dedicated-worktree.md
  - MacOS BSD wc -l padding fixed (tr -d '[:space:]' in RetryFlowTests fixture)
  - NuGet Trusted Publishing (OIDC) configured for Guardrails repo (DavidMaltby profile, no stored key)
  - v1.0.0-preview.1 published (first release — M7 harness)
  - v1.0.0-preview.2 published (tag 2026-06-14) — includes graph command, IConsoleIo seam, all PR #2 + issue #4 work; 3-OS tests green
  - guardrails skills install command + install-alias; optional <folder> arg on all folder-taking commands; error detection for misshapen folders
  - docs/DEPLOYMENT.md — work-machine runbook (bootstrap subcommand, Trusted Publishing, Upgrading section)
  - 4 library repos (FileBased.DataProviders, SettingsOnADO, SqlBuildingBlocks) refactored to tag-triggered release.yml + OIDC Trusted Publishing; v1.0.1 packages published for all three. MockDB explicitly NOT published (no obfuscation/licensing — memory saved).
  - Node 20 GitHub Actions bump (ahead of 2026-06-16 deadline); NU1903/NU1904 vulnerability fixes (SettingsOnADO DataProtection → 10.0.9)

IN PROGRESS:
  - Nothing actively in-flight. Last task (v1.0.0-preview.2 publish) completed; indexing lag on NuGet flat-container (expected, push confirmed).

NEXT STEPS:
  1. Dogfood execution (p1-dogfood-execute) — `guardrails run docs/plans/04-dogfood-cost-cap --fresh` to implement the maxCostUsd feature via the harness itself. Completes M7 exit criterion + flips Reality Gate items 2+3 toward proven. Costs real Claude tokens.
  2. Reality Gate item 2 verify (p2-reality-gate-verify) — run `guardrails run examples/hello-guardrails/hello-guardrails --fresh --no-ui` on real Claude (~$1) to prove end-to-end golden-example path.
  3. Deploy to Tricentis work computer — install `ServantSoftware.Guardrails --prerelease` + `guardrails skills install`; try on a real Tricentis work plan (the planned dogfood scenario).
  4. P2/P3 uber-report cleanup: p2-ssot-guardrail-artifacts (per-guardrail stream/composed-prompt doc in SSOT §8; fix §2 {script} token), p3-task-executor-split (TaskExecutor.cs is 845 lines), p3-prompt-state-integration-test (multi-task FakeClaudePlanBuilder state-passing fixture).
  5. Run a fresh uber-report to re-evaluate Reality Gate and updated action status.

KEY FILES:
  - .claude/tasks/2026-06-10-uber-report-v1.md — status report (Reality Gate, findings, risk register, next actions)
  - .claude/tasks/2026-06-10-uber-report-v1.tasks.json — slug-keyed task index (7 open actions)
  - .claude/contexts/_registry.md — context topic registry
  - docs/plans/02-schemas-and-contracts.md — SSOT (§7 exit codes, §8 per-guardrail streams, §10 graph)
  - docs/plans/03-roadmap.md — M1–M7 exit criteria, Reality Gate, v2 bets
  - docs/plans/04-dogfood-cost-cap.md — dogfood plan narrative
  - docs/plans/04-dogfood-cost-cap/ — validate-clean 4-task plan folder
  - docs/DEPLOYMENT.md — work-machine runbook (bootstrap, skills install, upgrading)
  - src/Guardrails.Core/ — Scheduler, TaskExecutor (845 lines — split candidate), StateManager, RunJournal, ClaudePromptRunner, PlanLoader, PlanValidator
  - src/Guardrails.Core/Graph/MermaidRenderer.cs — Render/SemanticContent share AppendNodesAndEdges
  - src/Guardrails.Core/Graph/GraphSourceHash.cs — SHA-256 of SemanticContent
  - src/Guardrails.Cli/Commands/GraphCommand.cs — graph/--check; StaleExitCode=2; DiagramCaption
  - src/Guardrails.Cli/Commands/SkillsCommand.cs — skills install + install alias
  - src/Guardrails.Cli/CommandFactory.cs — BuildRootCommand(io); graph + skills + install-alias wired
  - src/Guardrails.Cli/IConsoleIo.cs + SystemConsoleIo.cs — injectable console seam
  - tests/Guardrails.Integration.Tests/GraphCliTests.cs + GraphValidateToleranceTests.cs — StringConsoleIo-based
  - .github/workflows/release.yml — OIDC Trusted Publishing; tag v* → 3-OS test → pack → push
  - .github/workflows/ci.yml — 3-OS matrix (windows/ubuntu/macos-latest)
  - .claude/skills/plan-breakdown/SKILL.md + references/ — 8-step procedure, guardrail catalogue, schemas, example
  - .claude/skills/guardrails-domain-knowledge/SKILL.md — SELF-UPDATING; Status M1–M7 complete + graph
  - .claude/agents/ — 5 agents (architect, harness-developer, skill-author, test-author, devils-advocate)

CROSS-REPO:
  - C:\Dev\FileBased.DataProviders — tag-triggered release.yml (OIDC); v1.0.1 published (9 packages)
  - C:\Dev\SettingsOnADO — tag-triggered release.yml (OIDC); v1.0.1 published (2 packages); DataProtection → 10.0.9
  - C:\Dev\SqlBuildingBlocks — tag-triggered release.yml (OIDC); v1.0.1 published (5 packages)
  - C:\Dev\MockDB — tag-triggered release.yml BLOCKED (no NuGet publish — no obfuscation/licensing); see memory mockdb-do-not-publish-nuget.md

BLOCKERS/NOTES:
  - Reality Gate item 2 (hello-guardrails end-to-end on real Claude): STILL UNVERIFIED — costs ~$1; not yet run. Uber-report verdict still capped until this and dogfood execute are demonstrated.
  - Dogfood plan has NOT been executed yet — the harness implementing itself (maxCostUsd feature) is the flagship demo; it's ready but not run.
  - guardrail-review skill gap persists: did not catch tests-untouched hidden-state anti-pattern during M7; stronger hidden-state check or battle-testing still needed.
  - WorkspaceLock: prompt actions exclusive by default; worktree-per-task parallelism is v2. No validator warning for `exclusive: false` on prompt action (minor foot-gun).
  - Key design decisions locked: flat dependsOn, snapshot-in/fragment-out, claude -p for prompts, dotnet tool distribution.
  - Dev note: never run installed `guardrails` tool while testing — always use `dotnet run --project src/Guardrails.Cli`.
  - Worktree workflow: always create a dedicated worktree per session that edits code; tear down on merge. See memory use-dedicated-worktree.md.
  - v1.0.0-preview.2 NuGet indexing lag: push confirmed (OIDC key minted, "Your package was pushed" at 23:11 UTC 2026-06-14); flat-container may still show only preview.1 for a few minutes — normal.
