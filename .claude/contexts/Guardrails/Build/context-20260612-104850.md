REPO: C:\Dev AI\Guardrails
TOPIC: Guardrails/Build

CONTEXT: What we were working on
  Building the complete Guardrails agentic-engineering harness from scratch:
  - A `plan-breakdown` Claude skill that converts a reviewed markdown plan into a file-system task DAG
  - A cross-platform C#/.NET dotnet tool (`guardrails` CLI) that executes the DAG with retries, state passing, and honest needs-human halts
  - A full `.claude/` ecosystem (agents, knowledge skills, guardrail-review, uber-report)
  - A golden example (`hello-guardrails`) exercising every moving part
  - A first dogfood plan (`04-dogfood-cost-cap`) — guardrail blocker now fixed, ready to execute under the harness itself

COMPLETED:
  - M1: Foundations — plan committed as docs/plans/00-initial-plan.md, .gitignore, schemas (02), roadmap (03), domain-knowledge skill, architect agent, hello-guardrails golden example (hand-built)
  - M2: Walking skeleton — solution/project structure, PlanLoader/PlanValidator (GR10xx/GR20xx diagnostic codes), ProcessRunner + InterpreterMap, serial executor, exit-code guardrails, no retry; run + validate commands; CI matrix (windows/ubuntu/macos-latest)
  - M3: State + journal + resume — StateManager (snapshot-in/fragment-out, deep merge, atomic writes), JsonMerger, AtomicFile, RunJournal (state/run.json, merge-sequence protocol), status/reset commands, per-attempt logs; kill-and-resume proven
  - M4: DAG + parallelism + retry — Channel-based Kahn scheduler with maxParallelism workers, cycle detection (GR2007), WorkspaceLock (FIFO shared/exclusive), retry+feedback (RetryPolicy composing feedback.md), needsHuman short-circuit, Blocked transitive closure (independent branches continue), Ctrl+C tree-kill, plan command, Spectre live UI (thread-safe), --fresh flag
  - M5: Prompts — PromptComposer (state inline ≤16KB, output contract, verdict contract, feedback injection), ClaudePromptRunner (claude -p --output-format stream-json, stream-json parsing, cost aggregation), IPromptRunner/PromptRunnerRegistry, guardrailOverrides profile, fake-CLI integration tests
  - M6: Skills — plan-breakdown (SKILL.md + guardrail-catalogue.md + schemas.md + example-breakdown.md), guardrail-review (SKILL.md), all 5 agents (guardrails-architect, -harness-developer, -skill-author, -test-author, -devils-advocate), guardrails-dev-knowledge skill, uber-report skill, README.md, guardrails-domain-knowledge SELF-UPDATING with Status M1–M6 complete
  - M7: Polish + packaging — validate depth improvements (GR2008 promptRunners check, GR2009 runner-not-on-PATH warning), cost reporting (Total prompt cost on run/status), run --dry-run (waves + per-task resolution + resume-skip preview), --no-ui flag, NuGet packaging (ServantSoftware.Guardrails, v1.0.0-preview.1, MIT), CI workflow, release workflow (tag v* → test matrix → pack → NuGet push), dogfood plan (docs/plans/04-dogfood-cost-cap.md + validate-clean task folder), domain-knowledge updated M1–M7 complete + Reality Gate met
  - Post-M7 fixes: two skill reference files updated (compile-coupled tests note added to guardrail-catalogue.md, workspace depth note added to schemas.md)
  - Context save (session-start) — committed 68be0c2, first save for Guardrails/Build topic; _registry.md created
  - Uber-report v1 RUN (commit ee73bbc) — .claude/tasks/2026-06-10-uber-report-v1.md + .tasks.json; build+tests verified PASS this session (131 Core + 36 Integration passing, 1 RealClaude smoke skipped); golden folder validate-clean confirmed; four read-only agent dispatches produced 10 findings + risk register; 7 slug-keyed next actions
  - DOGFOOD GUARDRAIL BLOCKER FIXED (commit 3307283) — see IN PROGRESS for detail; p1-dogfood-fix-guardrail closed
  - Test suite: 167 tests passing (131 Core + 36 Integration), 1 skipped (RealClaude smoke, intentional/opt-in); all green
  - 12 commits on master, all pushed to origin (HEAD: 3307283)

IN PROGRESS:
  - p1-dogfood-execute is the next active task. The dogfood guardrail fix is DONE:
    - PROBLEM (found by uber-report devils-advocate): tasks/02-implement-cost-cap/guardrails/03-tests-untouched.ps1 used `git diff --name-only HEAD` — passed trivially on a fresh clone (no diff vs HEAD) AND was gameable because the action has Bash(git *) and could `git commit` mid-run to empty the diff.
    - FIX (commit 3307283): task 01 now runs `git hash-object` on each authored test file and writes the blob hashes to its state fragment under key "01-author-cost-cap-tests".testFileHashes. Task 02's guardrail reads those hashes from GUARDRAILS_STATE_IN (script guardrails inherit it — BuildGuardrailEnvironment copies action env, removes only STATE_OUT) and compares against current `git hash-object` output. git hash-object is purely content-based → immune to commit timing and fresh-clone baselines.
    - Plan re-validated clean after the fix. Ready to run.

NEXT STEPS:
  1. Dogfood execution (p1-dogfood-execute) — `guardrails run docs/plans/04-dogfood-cost-cap --fresh` to implement the maxCostUsd feature via the harness itself. This completes M7's exit criterion ("a phase plan broken down, reviewed, executed by the harness") and flips Reality Gate items toward proven. Costs real Claude tokens (3 prompt-action tasks). Optionally human-review guardrails once more first.
  2. First NuGet release (p1-nuget-release) — configure NUGET_API_KEY secret on Servant-Software-LLC/Guardrails (Settings → Secrets → Actions), then `git tag v1.0.0-preview.1 && git push --tags` to trigger the release pipeline.
  3. Reality Gate item 2 verify (p2-reality-gate-verify) — run `guardrails run examples/hello-guardrails/hello-guardrails --fresh --no-ui` on real Claude (~$1) to flip the end-to-end boolean to PASS. Independent of the above.
  4. P2/P3 cleanup from uber-report: p2-ssot-guardrail-artifacts (document per-guardrail stream/composed-prompt files in SSOT §8; fix §2 interpreter {script} token), p3-task-executor-split (TaskExecutor.cs is 845 lines), p3-prompt-state-integration-test (multi-task FakeClaudePlanBuilder state-passing fixture).

KEY FILES:
  - .claude/tasks/2026-06-10-uber-report-v1.md — the status report (Reality Gate, findings, risk register, next actions)
  - .claude/tasks/2026-06-10-uber-report-v1.tasks.json — slug-keyed task index (7 open actions); carry-forward joins on slug
  - .claude/contexts/_registry.md — context topic registry (Guardrails/Build entry)
  - docs/plans/02-schemas-and-contracts.md — SSOT for every schema and contract
  - docs/plans/03-roadmap.md — M1–M7 exit criteria, Reality Gate, v2 bets, seeded risk register
  - docs/plans/04-dogfood-cost-cap.md — the dogfood plan-of-record narrative
  - docs/plans/04-dogfood-cost-cap/ — validate-clean 4-task plan folder; tasks/01 writes test-file hashes to state, tasks/02 guardrail 03 verifies them
  - src/Guardrails.Core/ — domain: Scheduler, TaskExecutor (845 lines — split candidate), WorkspaceLock, RetryPolicy, StateManager, RunJournal, ClaudePromptRunner, PromptComposer, PlanLoader, PlanValidator
  - src/Guardrails.Cli/ — dotnet tool (PackageId: ServantSoftware.Guardrails, Version: 1.0.0-preview.1)
  - examples/hello-guardrails/hello-guardrails/ — golden example: 3 tasks, script + 2 prompt actions, state passing, prompt-judge guardrail
  - .claude/skills/plan-breakdown/SKILL.md — 8-step breakdown procedure
  - .claude/skills/plan-breakdown/references/guardrail-catalogue.md — 9 archetypes, decision tree, anti-patterns
  - .claude/skills/plan-breakdown/references/schemas.md — schema quick-reference (cites SSOT)
  - .claude/skills/plan-breakdown/references/example-breakdown.md — full worked breakdown + negative example
  - .claude/skills/guardrail-review/SKILL.md — adversarial BLOCKER/WEAK/NIT pass (NOTE: missed the tests-untouched hidden-state anti-pattern — battle-testing gap)
  - .claude/skills/uber-report/SKILL.md — Reality Gate-first status report procedure
  - .claude/skills/guardrails-domain-knowledge/SKILL.md — SELF-UPDATING; Status: M1–M7 complete + Reality Gate met
  - .claude/skills/guardrails-dev-knowledge/SKILL.md — layout, commands, conventions, dogfood safety
  - .claude/agents/ — guardrails-architect.md, -harness-developer.md, -skill-author.md, -test-author.md, -devils-advocate.md
  - .github/workflows/ci.yml — 3-OS matrix (windows/ubuntu/macos-latest)
  - .github/workflows/release.yml — tag v* → test matrix → pack → NuGet push (needs NUGET_API_KEY secret)

CROSS-REPO:
  (none)

BLOCKERS/NOTES:
  - Reality Gate item 2 (end-to-end example run on real Claude): STILL UNVERIFIED — running hello-guardrails costs ~$1 in tokens; not yet run; mark UNVERIFIED, never pass-by-assumption. Overall uber-report verdict is capped at "walking skeleton not proven" until items 2+3 are demonstrated this-session.
  - NuGet release: NUGET_API_KEY secret must be configured before pushing tag v1.0.0-preview.1; release pipeline otherwise untested.
  - guardrail-review skill gap: it reviewed the dogfood plan in M7 but did NOT catch the tests-untouched hidden-state anti-pattern that the uber-report devils-advocate found. The skill needs a stronger hidden-state check or real-plan battle-testing.
  - Dogfood friction documented in skill references: (1) compile-coupled tests can't split build/fail-on-stub guardrails; (2) nested plan folders need deeper workspace path — both noted in skill reference files.
  - WorkspaceLock design: prompt actions exclusive by default; worktree-per-task parallelism is v2. No validator warning exists for `exclusive: false` on a prompt action (minor foot-gun).
  - Key design decisions locked: flat dependsOn manifest, snapshot-in/fragment-out state, Claude Code CLI headless for prompts, dotnet tool distribution.
  - Dev note: never run `guardrails` installed tool while testing — always use `dotnet run --project src/Guardrails.Cli` to avoid testing the wrong binary.
  - Process note: model was switched to Sonnet 4.6 for this session (uber-report + guardrail fix). Earlier milestone work used Opus subagents for M2/M3/M5/M7 and lead for M4/M6.
