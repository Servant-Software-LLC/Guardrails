REPO: C:\Dev AI\Guardrails
TOPIC: Guardrails/Build

CONTEXT: What we were working on
  Building and iterating on the complete Guardrails agentic-engineering harness:
  - A `plan-breakdown` Claude skill converting reviewed markdown plans into DAG task folders
  - A cross-platform C#/.NET dotnet tool (`guardrails` CLI) executing the DAG with retries, state passing, and honest needs-human halts
  - A full `.claude/` ecosystem (agents, knowledge skills, guardrail-review, uber-report)
  - A golden example (`hello-guardrails`) exercising every moving part
  - Dogfood plan (04-dogfood-cost-cap) executed via harness — harness implemented itself
  - All 20 uber-report v1 findings completed; Reality Gate 3/3 PASS; v1.0.0-preview.10 published
  - Post-M7 PRs: TDD default (#36), hash-in-state pattern (#38), framework-selection (#43), streaming logs (#44),
    harness-computed captureHashes (#47), single-writer-per-key (#50), dogfood alignment (#52),
    opt-in restoreOnRetry (#53) — all merged; latest release v1.0.0-preview.18

COMPLETED:
  - M1: Foundations — plan committed, .gitignore, schemas (02), roadmap (03), domain-knowledge skill, architect agent, hello-guardrails golden example
  - M2: Walking skeleton — PlanLoader/PlanValidator, ProcessRunner + InterpreterMap, serial executor, exit-code guardrails; run + validate commands; CI matrix
  - M3: State + journal + resume — StateManager, JsonMerger, AtomicFile, RunJournal, status/reset commands, per-attempt logs; kill-and-resume proven
  - M4: DAG + parallelism + retry — Channel-based Kahn scheduler, WorkspaceLock, retry+feedback, needsHuman/Blocked, Ctrl+C tree-kill, plan command, Spectre live UI
  - M5: Prompts — PromptComposer, ClaudePromptRunner (claude -p stream-json), IPromptRunner/PromptRunnerRegistry, guardrailOverrides, fake-CLI integration tests
  - M6: Skills — plan-breakdown + references, guardrail-review, all 5 agents, guardrails-dev/domain-knowledge, uber-report, README
  - M7: Polish + packaging — validate depth, cost reporting, --dry-run, --no-ui, NuGet packaging (ServantSoftware.Guardrails), CI + release workflows, dogfood plan (04-dogfood-cost-cap) validate-clean
  - Post-M7: compile-coupled-tests note + workspace-depth note added to skill references
  - Uber-report v1 run — 20 findings identified, slug-keyed, prioritized
  - Dogfood guardrail blocker fixed — 03-tests-untouched.ps1 converted to content-hash (git hash-object)
  - IConsoleIo injectable console seam (issue #4) — IConsoleIo interface, SystemConsoleIo.Instance, StringConsoleIo; all commands adopt
  - guardrails graph command (PR #2) — Mermaid flowchart TD; --check exit 0/2/1; GraphSourceHash SHA-256; diagram.md provenance anchor
  - NuGet Trusted Publishing (OIDC) configured (DavidMaltby profile, no stored key)
  - v1.0.0-preview.1 published (first release)
  - v1.0.0-preview.2 published — graph command + IConsoleIo seam
  - guardrails skills install command + optional <folder> arg on folder-taking commands
  - docs/DEPLOYMENT.md — work-machine runbook
  - 4 library repos (FileBased.DataProviders, SettingsOnADO, SqlBuildingBlocks) refactored to tag-triggered OIDC release; v1.0.1 published
  - Node 20 GitHub Actions bump; NU1903/NU1904 vulnerability fixes
  - PR #21 (LogServer fixes) — three-agent review; fixed xUnit1031, 127.0.0.1 binding, security headers, TempPlan.Dispose catch, FormatDurationTests boundary; merged → v1.0.0-preview.6
  - Lunch & Learn PPTX — 13-slide deck (docs/presentations/guardrails-lunch-and-learn-2026-06-15.pptx) with Tricentis blue/orange palette; committed to repo
  - PR #25 (deterministic restore + Directory.Build.props centralisation) — three-agent review; BLOCKER fixed (release.yml missing --locked-mode on test + publish jobs); RestoreLockedMode on CI; 242+117 tests green → v1.0.0-preview.8
  - P1/P2 uber-report remediation (PR #29) — ISchedulerJournal.CurrentCostUsd default impl + multiple P1/P2 findings resolved
  - P3 batch findings (PR #30) — 7 slugs fixed: logs-status-mapping-untested-e2e, readme-exit2-and-command-table-drift, transcript-truncate-splits-surrogates, transcript-determinism-overclaim, logserver-perf-rereads-file-per-poll, logserver-port-probe-toctou, p3-task-executor-split
  - Reality Gate #3 PASS — plan-breakdown round-trip of hello-guardrails.md passes `guardrails validate`
  - Dogfood execute (docs/plans/04-dogfood-cost-cap, PR #31) — harness implemented maxCostUsd cost-cap: GR2012 validator, Scheduler halt, RunJournal.CurrentCostUsd; dogfood cost $12.66
  - v1.0.0-preview.10 published (tag v1.0.0-preview.10, 3-OS CI green)
  - Uber-report finalized (PR #32) — all 20 findings "completed"; findings.json head "8ff6dee", realityGate 3/3
  - PR #36 (TDD default) → v1.0.0-preview.12: plan-breakdown skill now generates author-tests tasks before implementation tasks by default; tests-first is the doctrine
  - PR #38 (hash-in-state 3-part pattern) → v1.0.0-preview.13: test-author task runs git hash-object + writes testFileHashes fragment; state-fragment-written guardrail; tests-untouched reads .testFileHashes
  - PR #43 (framework selection not silent) → v1.0.0-preview.14: plan-breakdown prompts user for test framework when ambiguous; no silent default
  - PR #44 (streaming log writes) → v1.0.0-preview.15: harness streams action/guardrail output to log files in real time (no buffering)
  - PR #47 (harness-computed captureHashes) → v1.0.0-preview.16: test-author task declares captureHashes in task.json; harness computes SHA-256 after action (no agent git command); GR2013 validation; agent no longer needs allowedTools:git to hash files
  - PR #50 (single-writer-per-key) → v1.0.0-preview.17: enforces a task may only write state keys equal to its own task id (SSOT §6.2); runtime error on violation
  - PR #52 (dogfood alignment, docs-only) → no new tag: migrated 04-dogfood-cost-cap to new captureHashes pattern; deleted state-fragment-written guardrail; tests-untouched now reads .fileHashes
  - PR #53 (opt-in restoreOnRetry) → v1.0.0-preview.18: harness snapshots captureHashes file bytes and restores them before each downstream retry; opt-in via restoreOnRetry:true in task.json; GR2014 (restoreOnRetry without captureHashes is error); --fresh/reset wipe state/captured/; WorkspaceContainment assert; audit log on restore failure
  - Work-laptop migration prompt authored: 7-step prompt for upgrading an existing plan folder (0003-commander-desktop-pivot) from old git hash-object/testFileHashes/state-fragment-written pattern to new captureHashes+restoreOnRetry; includes critical note that test-author task must also be reset so harness re-captures hashes under .fileHashes before the new tests-untouched guardrail can read them

IN PROGRESS:
  - Work laptop deployment pending: install v1.0.0-preview.18 + run migration prompt for 0003-commander-desktop-pivot (07-implement-wizard-navigation is stuck at needs-human under old pattern)

NEXT STEPS:
  1. Work laptop: run migration prompt — upgrade 0003-commander-desktop-pivot from old pattern; reset test-author task AND 07-implement-wizard-navigation; resume run
  2. Run uber-report v2 — assess what post-preview-18 issues exist; new findings may include the guardrail-review hidden-state gap
  3. guardrail-review skill gap: did not catch tests-untouched hidden-state anti-pattern during M7; needs stronger "consumed state key vs. produced state key" cross-check
  4. WorkspaceLock worktree-per-task parallelism — v2 scope (currently prompt actions exclusive by default)
  5. p2-ssot-guardrail-artifacts (per-guardrail stream/composed-prompt doc in SSOT §8; fix §2 {script} token) — carried forward from P2 findings

KEY FILES:
  - .claude/tasks/2026-06-15-uber-report-v1.md — finalized status report (all 20 findings completed, Reality Gate 3/3, v1.0.0-preview.10)
  - .claude/tasks/2026-06-15-uber-report-v1.findings.json — findings index (head: 8ff6dee, all completed)
  - .claude/contexts/_registry.md — context topic registry
  - docs/plans/02-schemas-and-contracts.md — SSOT (§2 maxCostUsd cost-cap, §3.1.1 captureHashes+restoreOnRetry+resume, §5/§6 harness-writes-workspace exception, §6.1 --fresh/reset wipe captured/, §6.2 single-writer-per-key, §7 exit codes, §8 per-guardrail streams, §10 graph)
  - docs/plans/03-roadmap.md — M1–M7 exit criteria, Reality Gate, v2 bets
  - docs/plans/04-dogfood-cost-cap/ — executed dogfood plan (all tasks green; migrated to captureHashes in PR #52)
  - docs/DEPLOYMENT.md — work-machine runbook
  - docs/presentations/guardrails-lunch-and-learn-2026-06-15.pptx — 13-slide Lunch & Learn deck (Tricentis palette)
  - Directory.Build.props — repo-wide build policy (ImplicitUsings, Nullable, TreatWarningsAsErrors, RestoreLockedMode on CI)
  - .github/workflows/release.yml — OIDC Trusted Publishing; tag v* → 3-OS test → locked-mode restore → pack → push
  - .github/workflows/ci.yml — 3-OS matrix; --locked-mode restore
  - src/Guardrails.Core/Model/TaskNode.cs — captureHashes + restoreOnRetry fields
  - src/Guardrails.Core/Execution/CapturedFileStore.cs — byte snapshot + restore + WorkspaceContainment assert
  - src/Guardrails.Core/Execution/Scheduler.cs — CostCapHaltFor; restoreOnRetry restore-before-retry hook
  - src/Guardrails.Core/Journal/RunJournal.cs — CurrentCostUsd() lock-guarded; wipes state/captured/ on --fresh/reset
  - src/Guardrails.Core/Validation/PlanValidator.cs — GR2013 (captureHashes paths), GR2014 (restoreOnRetry without captureHashes)
  - .claude/skills/plan-breakdown/SKILL.md + references/ — 8-step procedure; example-breakdown now uses captureHashes+restoreOnRetry pattern
  - .claude/skills/guardrails-domain-knowledge/SKILL.md — SELF-UPDATING; captureHashes+restoreOnRetry+single-writer bullets added
  - .claude/agents/ — 5 agents (guardrails-architect, -harness-developer, -skill-author, -test-author, -devils-advocate)

CROSS-REPO:
  - C:\Dev\FileBased.DataProviders — tag-triggered release.yml (OIDC); v1.0.1 published
  - C:\Dev\SettingsOnADO — tag-triggered release.yml (OIDC); v1.0.1 published; DataProtection → 10.0.9
  - C:\Dev\SqlBuildingBlocks — tag-triggered release.yml (OIDC); v1.0.1 published
  - C:\Dev\MockDB — tag-triggered release.yml BLOCKED (no NuGet publish — no obfuscation/licensing; see memory mockdb-do-not-publish-nuget.md)
  - Work laptop (Tricentis): plan folder 0003-commander-desktop-pivot has task 07-implement-wizard-navigation at needs-human; migration prompt ready to apply

BLOCKERS/NOTES:
  - Latest published NuGet: v1.0.0-preview.18 (PR #53 restoreOnRetry). Tag sequence through preview.18: .10 (uber-report/dogfood) → .11 (unknown) → .12 (#36 TDD) → .13 (#38 hash-in-state) → .14 (#43 framework) → .15 (#44 streaming) → .16 (#47 captureHashes) → .17 (#50 single-writer) → .18 (#53 restoreOnRetry). No tag for PR #52 (docs-only).
  - Old pattern (pre-#47) DEPRECATED: test-author runs git hash-object + writes testFileHashes; state-fragment-written guardrail; tests-untouched reads .testFileHashes. Any existing plan folder using this pattern must be migrated before running on v1.0.0-preview.16+.
  - Migration path for old folders: (1) update CLI to ≥preview.18, (2) add captureHashes+restoreOnRetry to test-author task.json, (3) remove agent hashing instruction from action.prompt.md, (4) delete state-fragment-written guardrail, (5) rewrite tests-untouched to use Get-FileHash + .fileHashes, (6) guardrails validate, (7) reset test-author task (not just implementation task) + resume.
  - Critical subtlety: if the test-author task already succeeded under the old run, its fileHashes won't be in state (the harness only captures on a fresh run). Must reset the test-author task too, not just the stuck implementation task.
  - Reality Gate: 3/3 PASS (build+test, walking skeleton, plan-breakdown round-trip all verified).
  - Dogfood: fully executed and merged. Honest-halt → human → resume loop demonstrated.
  - guardrail-review skill gap persists: did not catch tests-untouched hidden-state anti-pattern during M7; stronger hidden-state check needed.
  - WorkspaceLock: prompt actions exclusive by default; worktree-per-task parallelism is v2.
  - Key design decisions locked: flat dependsOn, snapshot-in/fragment-out, claude -p for prompts, dotnet tool distribution.
  - Dev note: never run installed `guardrails` tool while testing — always use `dotnet run --project src/Guardrails.Cli`.
  - Worktree workflow: always create a dedicated worktree per session; tear down on merge. See memory use-dedicated-worktree.md.
  - Release-build self-lock: dogfood harness run must launch via `dotnet run` (Debug), not the Release binary (locks Core.dll → breaks build guardrail). See memory dogfood-release-build-self-lock.md.
  - PR #25 locked-mode note: to update a package, bump the reference then run `dotnet restore --force-evaluate`, commit updated lock files.
