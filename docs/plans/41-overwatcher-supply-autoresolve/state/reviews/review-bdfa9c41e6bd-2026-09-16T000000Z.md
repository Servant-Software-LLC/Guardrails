# Guardrail review — 41-overwatcher-supply-autoresolve

Plan-Definition-Hash: sha256:bdfa9c41e6bd0ed733beb65f7576a0b33b4adb98b8bd31258f187aa971d6e04b

Reviewed and remediated 2026-09-16 (UTC). Flat plan (no wave folders). **20 tasks / 8 tiers /
maxParallelism 3**, unchanged by this pass — every finding was remediated in place rather than by
adding tasks. `validate` exit 0 (GR2025 only, cleared by this stamp; GR2042 x2 and GR2069 x4
dispositioned below). `samples verify` exit 0, 14 pairs, 0 findings.

**VERDICT: 2 BLOCKERs and 3 WEAKs FIXED and re-verified; 2 proposed remedies OVERTURNED by
measurement; 6 findings recorded as accepted residuals. The plan is marked reviewed.**

A green `validate` here proves the plan folder is internally CONSISTENT — schema, DAG, writeScope
reachability, clause shape. It proves nothing about whether the plan delivers design 41. That
claim rests on the per-clause baselines and the sample pairs below, and on the reader.

## How this pass was run (#467)

This session AUTHORED the folder, so the adversarial pass was DELEGATED to two independent agents
handed the folder cold: `guardrails-devils-advocate` (adversarial lens) and `guardrails-architect`
(contract lens). Their disagreements were the signal, and in two cases the delegates were wrong
and measurement settled it — recorded here because a review that only lists what it accepted is
indistinguishable from one that measured nothing.

## Findings and resolution

| # | Task | Sev | What wrong implementation slipped through | Resolution |
|---|---|---|---|---|
| A1 | 14 | BLOCKER | The composition root could wire the whole feature and leave §2.1's shared threshold rule with TWO live spellings: `Scheduler.EffectiveThresholdToken` and the open-coded `(perGate ?? cfg.EscalationThreshold)` at its call site. Every behavioural test passes — both spellings render identical text for every input the suite supplies. | NEW `tasks/14/guardrails/03-scheduler-uses-the-shared-rule.ps1`: REQUIRES `GateThreshold\s*\.\s*Effective\s*\(` (baseline 0) and BANS `perGate\s*\?\?\s*cfg\.EscalationThreshold` (baseline 1 — a removal deliverable, red on arrival). Task 14's prompt gained a section pointing at the other half, phrased as a grep rather than a line citation so it cannot rot. |
| D1 | 16 | BLOCKER | A guardrail clause that **could never fire**. It hunted the literal `'proceeded-best-guess / proceeded-unreviewed'`, but the #561 literal-neutralizing pass rewrites `/` to `#` inside string literals — and the banned text IS a string literal. Measured in `$code`: **0**. The script exited 1 the whole time on its siblings, so a per-SCRIPT red could never have exposed it; only the per-CLAUSE census (#478) did. | Re-anchored on `proceeded-best-guess\s*.\s*proceeded-unreviewed`, which matches the neutralized `#` and a raw `/` alike. Re-measured: **1**. Proven with a mutant that kept the enumeration verbatim and previously exited 0. |
| D2 | 16 | WEAK | The `ResourcePathToken` ban names an IDENTIFIER, and an identifier is free to vary. Keeping the private regex and renaming it `PathTokenPattern`, beside a genuine `MissingResourceSignal` call, satisfied every other clause in the file. | Added a rename-proof CONSTRUCT ban on `private static readonly Regex`, read from `$raw` (the pattern lives in a string literal that both derived levels blank). Baseline **1**, and the over-match check (#470) shows that single occurrence IS the member being deleted, at `RunCommand.cs:3339` — so it cannot false-red a survivor. |
| D3 | 02, 03 | WEAK | Nothing in the plan discriminated the DECIDED reading of `d41-candidate-scope` from the reading the review explicitly rejected. Every census row was green under BOTH readings, including the wiring proof — so the plan could have shipped the rejected semantics with 20/20 green. | Pinned `Compute_ReturnsACandidate_WhenTheHaltedTasksOwnWriteScopeCoversThePath` into task 02's red census, task 03's forward census, and task 02's prompt, with the prompt stating why that one row exists. |
| D4 | 01, 08, 15, 17 | WEAK | A DECLARED EXEMPTION (`Expect='Executed'`) rejected only `NotExecuted`/empty. An exempt row that came back **Failed** therefore PASSED the census — so a fixture broken outright could leave every row red and still certify the task, billing the NEXT task with a misattributed halt. Task 17's header claimed the opposite in writing. | All four now require outcome `Passed`. Task 17's false header claim corrected in place. |
| — | 08, 09 | WEAK (authoring) | `Overwatch.FixKindToken` maps `ResourceSupply` to today's `_ => "unknown"`; NO task pinned it (measured: zero hits for `FixKindToken` across every prompt and guardrail). It would ship recording `"unknown"` in `overwatch.jsonl` forever, failing nothing. | Row `TheResourceSupplyFixKindTokenIsResourceSupply` added to task 08's census and task 09's forward census, asserting the observable `fixes[].Kind` (the method is private). |

### Two proposed remedies OVERTURNED by measurement

1. **D2's suggested anchor was `new Regex(` / `[GeneratedRegex`.** Both measure **0** on the real
   tree — the declaration is target-typed `new(@"...")`. Applying it as proposed would have created
   a clause that can never fire, which is precisely the D1 defect being removed three rows above.
   The construct ban was substituted after measuring three candidate anchors.
2. **The architect's `./` baseline claim** for task 20 was re-measured (confirmed 0, with a
   positive control proving the probe could see a planted hit).

### Dispositioned diagnostics

- **GR2074 x2 (task 13/05)** — CLEARED, honestly. The clauses require `RunJournal.RecordSupplied`
  / `RecordRefreshed` without a trailing `\(`. The subject is a DOC COMMENT and design §8 requires
  a `<see cref="..."/>` cross-reference, so a mention is exactly what belongs; demanding `\(` would
  make both clauses unsatisfiable by any correct edit (#479). Reading the rule's source
  (`PlanValidator.cs:3025-3057`) showed `ClaimsAnInvocation` scans the whole leading `#` block for a
  word-anchored vocabulary — and this guardrail's own DISPOSITION PARAGRAPH tripped it, by restating
  the vocabulary it was arguing about. The explanation set off the rule it explained. Header
  reworded, clauses byte-for-byte unchanged, no `# guardrails: ignore`. Re-measured: GR2074 x2 -> x0.
- **GR2042 x2 (tasks 12, 13)** — ACCEPTED, deliberate. Design §11 row 7 is one behaviour spread
  across 11 files; splitting it further would put the observer contract and its eight forwarding
  sites in different tasks, where neither half can be proven. Task 13 carries writeScope weight 9
  and is the widest blast radius in the plan; it is named as under-guarded below.
- **GR2069 x4 (rows 3, 5, 7, 10)** — CONFIRMs, expected. Each is a deliberately split row; the
  validator's own output shows every path reachable by the task owning that half.

### A break this pass created and caught

Committing the plan folder turned the repo's own `ProducerCoverageCorpusTests` RED
(`TheExpectationTableCoversEveryPlanFolder`: a 19th plan folder with no `Corpus` row). This was not
cosmetic: the plan's baseline preflights filter `Category!=OverwatchSupply`, and that meta-test
carries no Category trait, so `!=` SELECTS it — the plan would have halted on its own baseline
preflight before task 01 ran, reported as "the baseline is red". Row added, pre-run commit derived
the way the test derives it (`519a9640`, 20 task.json files). Re-run: **71/71 green**.

**Consequence for delivery: the PR carrying this folder must NOT be squash-merged**, or `519a9640`
is rewritten and `ThePreRunCommitsAreTheBreakdownCommits` goes red on a sha that no longer resolves.

## Accepted residuals (recorded, NOT fixed)

| # | Task | Why it was left |
|---|---|---|
| D5 | 18 | The clause anchors on a `nameof`-satisfiable mention rather than the call. Same class as GR2074; the task's paired census already requires the four rows observed Failed, so a `nameof`-only implementation cannot reach green through the pair. |
| D7 | 14 | One `Overwatch` reference in `SchedulerFactory` is unbound by any clause. |
| D8 | — | The positive half of `d41-supply-source` is unbound; its negative half is pinned. |
| A4 | 07 | No clause names the `applied:{supplied,commit}` writer specifically. |
| A5 | 20 | A `./`-normalization clause would have baseline 0 and no removal deliverable — it would be unfirable by construction, so it was NOT added. |
| A6 | — | `wave: task.WaveDir` is inapplicable: this plan is flat. |

## Under-guarded tasks, named

- **19 (`update-ssot-and-design-docs`) and 20 (`update-domain-knowledge-skill`)** — the weakest in
  the plan. Their guardrails assert that prose MENTIONS the right tokens. A task that pastes the
  vocabulary into a heading and changes nothing substantive passes. There is no mechanism that can
  compare shipped prose to intent, which is the honest limit; treat their green as "the words are
  present", never as "the docs are correct".
- **13 (`implement-observer-by-field`)** — widest writeScope in the plan (weight 9, eight forwarding
  sites). Its guardrails prove each named site was touched; they cannot prove a NINTH observer added
  later is forwarded. This is the #712 defect class reappearing one level up.
- **18 (`implement-terminal-gate-names-supply`)** — see D5. Guarded by its census, not by its own
  source-shape clause.
- **12** — GR2042 over-scope with `maxTurns` 75; likeliest needs-human in the run.
