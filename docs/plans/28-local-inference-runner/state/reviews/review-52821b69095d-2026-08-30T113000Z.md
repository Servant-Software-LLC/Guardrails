# Guardrail review — plan 28 (local-inference `openai-compat` runner, #223)

Plan-Definition-Hash: sha256:52821b69095d2dbeac3637684327c9140452487a9b12cf4b3e18a480303b8129

Reviewed 2026-08-30. `guardrails validate` OK · `graph --check` exit 0.

## Reviewer independence — read this first

**This pass was run by the agent that authored the plan, roughly an hour earlier.** That is the weak
configuration this repo has already been burned by, and it is recorded here rather than left implicit
so a later reader can weigh the findings accordingly. The mitigation applied was to lean on
**execution over reading**: 9 guardrails executed, 2 gaming mutations applied, 7 filter-scope probes
resolved with `--list-tests`, 1 orphaned-pin sweep run against the real test tree. **Both BLOCKERs
came out of execution. Neither came from reading the folder.** A genuinely independent pass on the
source-shape layer is still owed.

## Findings

| # | Task | Guardrail | Sev | What slipped through | Resolution |
|---|---|---|---|---|---|
| 1 | 23 (was 19) | `02-preflight-tests-pass` | **BLOCKER** | The prompt instructs adding `OpenAiCompat` to `ModelEnumerable`. `ProvidersInitAnnotationTests:276` asserts `Assert.Empty(PromptRunnerKinds.ModelEnumerable)` and `Assert.Equal(["claude"], UnenumerableKinds)`. That file was in **no task's `writeScope`** and the task's filter never ran it — so the task went green and the suite failed at the terminal gate, owned by nobody (#193). | **FIXED.** `ProvidersInitAnnotationTests.cs` added to task 23's `writeScope`; the prompt instructs the re-baseline **and forbids weakening the never-invents-a-model guarantee**; a third guardrail (`03-providers-init-pin-rebaselined`) runs that suite. |
| 2 | — | breakdown report | **BLOCKER** | **No `Seam ledger (#382)` heading was emitted** — evidence the Step 4 seam analysis never ran. Concretely: task 01 fakes `IPromptRunner` to capture the argument, and the follow-on task's only proof re-ran those same fakes. Nothing showed the role travelling the production path. | **FIXED.** Analysis run, ledger emitted (below). Its one owed row now has a proof: new task **03-prove-role-reaches-real-runner**, driving the real `GuardrailRunner` + real `ClaudePromptRunner` over a stub binary. |
| 3 | 28, 30 | SSOT / domain-knowledge records | WEAK | **Executed:** three lines of prose (`### 9.8` + one sentence naming all eight tokens) took the guardrail to **exit 0** — a vocabulary check, zero contract recorded (Probe B operator 18). | **FIXED.** Split into a **structural-position** check (the five block keys must sit INSIDE the canonical `promptRunners` region), a **`SchemaDriftTests`** run (the block must still parse and validate), and a **distribution** clause (tokens across ≥5 distinct lines). Re-running the same gaming edit now exits 1. |
| 4 | 29 (was 25) | `01-canonical-block-mirrors-ssot` | WEAK (#468) | Hand-written PowerShell re-implemented a comparison `SchemaDriftTests.PromptRunnersSchema_SkillCopyMatchesSsot` **already performs** — two extractors on one property, and mine had already produced one false-red during authoring. | **FIXED (demoted).** Replaced by `dotnet test --filter ~SchemaDriftTests`. |
| 5 | 06 (was 05) | fake-server self-test | WEAK (op 21) | The task authors both fixture and self-test with no TDD split and no census; `Assert.True(true)` passed. Three downstream pairs build every assertion on this fixture. | **FIXED.** Replaced by a **per-test census** over the runner's own TRX: five pinned scenario method names must have executed and passed. The residual (assertion discriminating-ness) is named in the guardrail. |
| 6 | 09 (was 08) | — | WEAK (GR2042) | 7 paths + `maxTurns 75` — the structural over-scope fingerprint. | **FIXED by split.** The config surface (`PromptRunnerConfig`, `RawManifests`, `PromptFailureKind`, the runner stub, a round-trip shape test) became its own task 09; the runner test-authoring split into three paired tasks (10/12/14), which also removed a 5-way fan-in warning and restored proper TDD pairing. |
| 7 | 02, 28 | — | NIT (#229) | Tier opinions: `hard` for seven one-line edits; `easy` for ten SSOT edits requiring reading real code. | **FIXED.** Both set to `medium`. |

## Seam ledger (#382)

| seam (component → declared dependency) | bucket | production type | faked underneath | T* | proof |
|---|---|---|---|---|---|
| the seven §3.4 producers → `IPromptRunner` | E | `ClaudePromptRunner` | the `claude` child process (stub binary) | `03-prove-role-reaches-real-runner` | `tasks/03-prove-role-reaches-real-runner/guardrails/02-role-reaches-the-real-runner.ps1` |

**Rows the ledger deliberately does NOT carry.** `OpenAiCompatPromptRunner` → the OpenAI endpoint is a
**process seam** (a real loopback socket), which rule 1 permits faking and which gets no row — that is
the design working, not an omission. The same holds for tasks 16, 22, 24 and 26, whose tests drive the
real harness against that same socket.

T\* was recomputed from the DAG, not accepted from the cell: `PromptRole` exists at task 01 and
`ClaudePromptRunner` exists today, so the earliest task at which both production types exist is 02 —
and the proof sits at 03, one task later, **named here with its reason**: task 02's deliverable is the
seven assignments and task 03's is the proof, split during this review because they are separately
verifiable (finding 6's sibling).

## Verdict

**Both BLOCKERs addressed; all WEAK findings fixed; the NIT applied.** No finding was declined.

Plan grew 26 → **30 tasks** (two splits), 56 → **66 guardrails**.

## What this pass could NOT check

- **9 of 66 guardrails executed.** The rest invoke `dotnet build`/`dotnet test` (minutes each).
- **Probe B operator 20: INAPPLICABLE** — greenfield, the real-seam tests do not exist yet. Reported
  not-run, never as passed.
- **Operator 21 applied to task 28 only** (green → finding → fixed). Not manufactured for the
  run-authored test files of tasks 10/12/14/16/22/24/26, which are pinned by `tests-fail-on-stubs`
  rather than a `covers-*` floor.
- **A₂ clause census** done by hand for the three doc guardrails; the dotnet-invoking guardrails parse
  exit codes and summary counts, so they carry no token clauses to census.
- **Ancestor-tree residual:** the censused tree is the PLAN's baseline, not each task's. A clause
  measured 0 today can be pre-satisfied by an ancestor's output, and that tree does not exist yet.
- **§4 missing-insertion pointed at the gate folders:** yes. `<plan>/preflights/` (producer set =
  nobody; both run existing test projects) and `<plan>/guardrails/` (producer set = all 30). Clean.
- **Probe C:** the plan carries **no forbidden-present clauses at all**, so the #470 collision axis is
  vacuous. Checked, not assumed.
- **Positive controls:** the filter-scope probes used `--list-tests` and returned non-zero counts
  (self-proving); the orphaned-pin sweep was a `grep` that returned hits, not a zero.

## Accepted, documented, not fixed

**GR2042 on tasks 01 and 02** — both are the same seven `PromptInvocation` construction sites. Task 01
carries 9 paths + `maxTurns 75`; task 02 carries 7 paths on breadth alone. **Neither is splittable:**
adding a `required` member is a source break at every construction site, so there is no intermediate
state in which the solution compiles, and assigning seven one-line values is one deliverable checked by
one test class. Splitting into seven tasks would buy seven agent invocations, seven worktrees and seven
merges for seven lines, with six intermediate states where the pinning test class is partially red.
The retry cost the warning exists to prevent does not apply here — a failed guardrail re-runs seven
one-line edits.
