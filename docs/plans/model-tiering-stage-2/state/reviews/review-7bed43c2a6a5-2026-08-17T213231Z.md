# Guardrail review — model-tiering-stage-2 · wave-02-attempt-launch-wiring

Plan-Definition-Hash: sha256:7bed43c2a6a521885f38344dfc12e567a724e61e610307a7b571afbbe2cc6db6

Reviewed: 2026-08-17T21:32:31Z · Scope: **wave 2** (13 tasks), the JIT wave recovered from the
quarantined breakdown. Supersedes `review-2e800d076e41-2026-08-17T045706Z.md` for the plan-level
marker; that report's wave-1 findings stand unchanged.

## Verdict

**Reviewed. One BLOCKER found and fixed, two NITs fixed, one warning resolved-as-accepted.**

`guardrails validate` (plan-level): OK — GR2042 on task 07 (resolved below) and GR2025 (this stamp
clears it). `graph --check`: exit 0. All 13 tasks' guardrails are **deterministic**; the wave has
**zero prompt-judges**, so there is no demotion gate to apply and no #223 JIT-judge model deferred
as unchecked.

## Findings

| Task | Guardrail | Sev | Defect | Resolution |
|---|---|---|---|---|
| 06 | `01-covers-required-behaviors.ps1` | **BLOCKER** | **Unsatisfiable.** Line 25 REQUIRES `[Trait("Category", "TierResolution")]`; line 66 FORBADE `TierResolver\|TierResolution` in the raw content. The required attribute's own string literal contains the banned token, so both clauses fire on the same characters and **no file could pass**. Task 06 authors the wave's conformance suite and 07→08→09 all depend on it — the whole wiring chain would have dead-ended after paying 06's full retry budget | **FIXED.** The ban now scans comment- and string-literal-stripped source and anchors on a USE (`TierResolver\s*\.` / `TierResolution` as a type), per #76 + #97/#98. Filed as **#470** with a proposed `validate` lint |
| preflight | `01-wave-01-artifacts-materialized.ps1` | NIT | `(?m)^\s*Task,\s*$` / `^\s*PlanDefault\s*$` encoded that `Task` carries a trailing comma and `PlanDefault` is last — a correct enum would false-RED on member ORDER, which no rule constrains | **FIXED** — `[,}]?` on both |
| 04 | `02-unavailability-tests-pass.ps1` | NIT | `$ran -lt 2` is cleared by the pre-existing `ClaudeSignalClassifierTests` alone (~32 rows), so it could not detect the NEW class contributing zero — the case worth catching | **FIXED** — per-class `--list-tests` assertion added |
| 07 | — (GR2042) | WEAK | `maxTurns 80` + 4-path `writeScope` trips the over-scope fingerprint | **Resolved as accepted, not waved through.** The 4th path is `PromptExecutionSupportModelTests.cs`, a #193-recommended re-baseline that **cannot** be split off — a test must be re-based in the change that breaks it. The other three are one call path; the follow-on wiring is already split into 08/09; the prompt uses durable grep markers, not line numbers (#203 clean); and `02-two-level-precedence-coverage-survives` guards against gutting the re-based test. No split improves it |

## Verified by EXECUTION, not by reading

Twenty-one two-sided cases across five scripts. The BLOCKER was invisible to reading — its two clauses
are 40 lines apart and each is individually correct — and surfaced only when a **complete** valid
sample was executed. (My first sample was incomplete and produced a different failure that nearly
masked it; that near-miss is recorded in #470.)

- **Wave-2 entry gate**, run against **wave 1's real merged output** on `guardrails/model-tiering-stage-2`:
  all 12 declaration patterns match → **passes**; correctly REDs on a removed member, on a member
  present only in a comment, and (after the NIT fix) still passes a reordered enum while still
  catching a genuinely removed one.
- **Task 06** after the fix, 8 cases: correct suite GREEN · token in a comment GREEN · token in a
  string GREEN · real `TierResolver.Resolve(…)` call RED · `TierResolution` as a type RED ·
  eight-of-nine names RED · missing trait RED · file absent RED.
- **Task 05** harness-shape, 5 cases: real seams + fake runner + LogDir GREEN; Scheduler undriven,
  no LogDir, harness calling the resolver, file absent all RED.
- **Tasks 12 and 13** (hand-authored during recovery), 11 cases including two comment-only evasions.

## Cleared — checked, and recorded rather than silently omitted

- **#193 orphaned golden (task 04).** Its filter deliberately includes the PRE-EXISTING
  `ClaudeSignalClassifierTests`. Resolved by reading that file's actual `InlineData` rows: the only
  non-transient pins are `Bash not permitted`, `Syntax error`, `file already exists` — **no
  pre-existing pin asserts a connection-level shape**, so DoR §6.3's additive change cannot be forced
  to flip one. Not merely "uncertain"; cleared.
- **#455.** All 12 task-level filters resolve to their own pair's class. Both directions asked. The
  conformance ratchet is monotone and deadlock-free: 06 authors 9 names → 07 owes 6 → 08 owes 7 →
  09 owes all 9, and 09's two additions are exactly its own deliverables (the D28 ceiling warning and
  the climb warning).
- **#462.** No `-v q` on any `dotnet test`. The seven scripts lacking `DOTNET_CLI_UI_LANGUAGE` are all
  `build-passes` checks that read only an exit code.
- **#164/#254.** All 13 prompts key their state fragment on the wave-qualified id.
- **#203/#204.** No line-number pointers anywhere in the wave; task 07's prompt says "durable markers —
  grep, do not trust a line number".
- **#382.** Task 05's harness-shape check REQUIRES the real `PlanLoader`/`TaskExecutor`/`Scheduler`,
  permits faking only `IPromptRunner` (the process boundary), and FORBIDS the harness consulting the
  resolver — with the negative assertion verified by execution.
- **TDD write-scope**, **cross-wave `dependsOn`** (none), **#157/GR2026** (silent; spot-checked
  against prompts): clean.
- **#224 model availability.** No `action.model` / `action.runner` overrides anywhere in the wave and
  no prompt-judges; every prompt resolves to the single declared `claude` block, whose `kind` has a
  concrete runner. Nothing deferred as unchecked.

## Process notes for whoever reads this next

- The BLOCKER was authored by the **JIT auto-breakdown**, unattended, and nothing executed it before
  the run. The attended review caught it; a JIT wave under autonomous `proceed-unreviewed` has no
  equivalent gate (**#375**).
- **The per-wave stamp flow in the `/guardrails-review` skill is unexecutable in the nested layout.**
  It prescribes `guardrails plan-hash <plan>/wave-NN-<slug>` and
  `mark-reviewed <plan>/wave-NN-<slug>`, but a nested wave has no own `guardrails.json`, so both fail
  GR1001. This review is therefore stamped at PLAN level with the report here under
  `<plan>/state/reviews/`. Filed as **#472**.

## Issues filed from this review

- **#470** — a guardrail can REQUIRE a token it also FORBIDS (this review's BLOCKER); proposes a
  `validate` lint as the GR2026 analogue.
- **#471** — the failed-breakdown quarantine keeps the wave gates while reporting "reverted to its
  empty stub", and silently stales the plan's review attestation.
- **#472** — the per-wave review-stamp flow cannot run on a nested wave.
- **#402** — measured evidence added: the truncated breakdown's recoverable prefix was 92%.
