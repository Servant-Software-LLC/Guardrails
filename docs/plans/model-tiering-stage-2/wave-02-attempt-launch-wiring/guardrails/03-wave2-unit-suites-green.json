{
  "description": "Wave 2 exit gate — the four Core.Tests classes this wave authors (journal-tiering schema, connection-unavailability classification, per-tier spend, attempt usage tokens) all pass on the merged wave HEAD. LOCAL: a wave terminal postcondition (#165). Filtered, so it does not credit GR2028."
}
