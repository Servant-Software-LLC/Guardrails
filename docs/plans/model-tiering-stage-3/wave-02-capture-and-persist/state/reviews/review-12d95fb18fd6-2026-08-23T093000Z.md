# Guardrail review — `wave-02-capture-and-persist`

Plan-Definition-Hash: sha256:12d95fb18fd69e1745008a49af6c1260a440978411fd497a766326d3fa68668c

Reviewed 2026-08-23 (UTC) against the wave-2 entry tree (merged wave-1 HEAD, `7d567db`), tool v1.9.0.
Wave authored just-in-time at the wave-2 checkpoint; the breakdown session was cut off at 30:01 during
self-validation, after authoring completed (see the verdict's note on the missing Step 7.4 report).

## Verdict

**No BLOCKERs. Two WEAK findings, both FIXED in this pass; one NIT, declined.** Wave is stamped.

## Findings

| Task | Guardrail | Sev | What slips through | Disposition |
|---|---|---|---|---|
| wave gate | `guardrails/03-wave-deliverables-present.ps1` | WEAK | **Measured: 7 of 12 required clauses satisfied by a comment alone.** The script stripped C# comments before its *forbidden* scans (citing #470) but not before its *required* ones, so `// TODO: ObservedModel = result.ObservedModel` matched even the dotted-assignment anchor. Fatal to this gate specifically: its stated job is catching a hunk lost in the wave merge, and every task prompt *mandates an XML doc comment* on the member it checks — a member dropped by an AI-merge whose doc comment survived would read as delivered. | **FIXED** — comment strip added for `*.cs` required clauses (comments only, never literals: `"system"` is a required token living in a literal, so the two-level rule applies); three bare-token clauses re-anchored on the declaration/assignment the prompts mandate. Re-measured: 0 of 12 clearable by comment, valid half still exit 0, baseline still 12/12 red. |
| — | breakdown report | WEAK | No Step 7.4 report exists — the truncation killed it — so the obligations discharged only there are absent: the #382 seam ledger and the #468 per-check "why no test could carry it" lines. | **FIXED here** — seam analysis re-derived from the folder and source-shape justifications recorded below. |
| 04 | `01-provenance-tests-pass.ps1` | NIT | `$namedRan -lt 5` is a count floor where the three behaviour names are already pinned in task 03's census; also re-runs the whole Integration project a second time. | **DECLINED** — not exploitable (task 04's `writeScope` excludes the test file, so it cannot add theory rows), and the script documents it as a merge-integrity check, which is what it is. |

### Considered and not flagged

- **#193 orphaned golden.** Task 04 runs the whole pre-existing `Stage2ConformanceTests` class, which it
  does not own. Not certain to collide: the fold is a no-op when the runner reported no observed model —
  every pre-existing test — and `ProvenanceModel_StaysTheResolvedRoute_WhenTheRunnerReportedNoModel` pins
  exactly that. The guardrail routes a pre-existing failure to `needs-human` rather than an out-of-scope
  edit. Deliberate and reasoned.
- **#455 trait-only filter on task 03.** `Category=ObservedModelProvenance` carries no class term, which
  the rule normally requires. Correct here and for the documented reason: the new tests live inside the
  shipped 2,100-line `Stage2ConformanceTests`, so a class filter would sweep pre-existing green tests into
  an inverse-polarity check. The trait is task-pair-scoped, not plan-wide.

## Source-shape justifications (#468) — the lines the missing report owed

Every surviving source-shape check, and why no test carries it:

- `preflights/01-stage2-anchors-materialized.ps1` — asserts 13 *structural* anchors (a type is declared,
  a method exists) on the tree this wave is authored against. No runtime proxy: the claim is "the code
  this wave names still exists at this path", which is precisely a build-graph fact. Legitimate.
- `guardrails/03-wave-deliverables-present.ps1` — the merge-drop detector. Its claims *are* carried by
  tests for the code half (wave gate 02 runs both suites unfiltered), and it exists because those suites
  are green on a tree with none of this work in it. Its irreplaceable half is the three **documentation**
  clauses, which no test can carry. Legitimate, and the code clauses are deliberate redundancy at the one
  point where a union can silently drop a hunk.
- `tasks/05/01-contract-delta-present.ps1` — documentation deliverable. Exempt from the sample pair (no
  meaningful invalid sample of a design doc); the PRECEDENT check holds — each demanded token has a
  sibling precedent in the same document.

## Seam ledger (#382) — re-derived from the folder

The Step 7.4 ledger was unavailable. Re-derived per check 7: the only substitution this wave's tests make
is a **fake prompt runner** (`Stage2PlanHarness.Success() with { ObservedModel = ... }`). `IPromptRunner`
resolves to `ClaudePromptRunner`, which spawns the `claude` **child process** — a process seam, which
#382 explicitly excludes from the ledger. **No rows owed.** The real capture path is proven separately by
task 01's `ClaudePromptRunner_CarriesTheObservedModel_OffARealStream` on a real stream.

## What was EXECUTED

- **Preflight, both halves** — valid green; each of 13 anchors scrubbed in turn, all 13 exit 1. Confirms
  the single-element-unwrap defect found during authoring is fixed. Committed as
  `samples/01-stage2-anchors-materialized.probe.ps1`.
- **Gate 03, both halves** — committed as `samples/03-wave-deliverables-present.probe.ps1`.
- **Probe A** — gate 03 (12/12 red), task 05 (3/3 red). Both correctly red on the baseline.
- **Probe A₂** — fast path; every required clause fired, so none pre-satisfied. Bans correctly green and
  not censused. The preflight declares its wave-entry exception (#478) and it holds.
- **Probe B** — operator 2 (token in a comment) against gate 03: 7 of 12 cleared → the WEAK above.
- **Probe C** — required × forbidden reconciled across all three shared files; no collision. The ban is
  anchored on the JSON-key form and the C# declaration so the prose recording the refusal cannot trip it.
- **#248** — `(?:Passed|Failed):\s*(\d+)` verified against real `dotnet test` output
  (`Failed: 0, Passed: 14`); order-independent, unlike the motivating bug.
- **#455** — `--list-tests` enumerated task 02's filter: exactly `ClaudeStreamParserTests` and
  `ClaudePromptRunnerArgsTests`, both pre-existing and green. Discriminating.
- **#474** — datum clauses traced to carriers; `ActionRun` (`ActionRunner.cs`) and `AttemptProvenance`
  (`JournalModel.cs`) are both in the writing task's or an ancestor's scope.
- **§4 missing-insertion, pointed at the GATE folders** — all 10 paths gate 03 requires are claimed by a
  task's `writeScope`. Plan is **closed** for waves 1–2; waves 3–5 are declared stubs, so no coverage
  verdict was withheld for this wave's own gates.

## What was NOT checked — gaps, not omissions

- **Probe B operator 20: NOT RUN.** Greenfield first review — the real-seam test does not exist yet.
- **Probe B operator 21: reasoned, not manufactured.** Its target (a `covers-*` naming floor) does not
  exist here; both test-author tasks already use the per-test TRX red census that *is* operator 21's fix.
  A hollow `Assert.True(true)` fails the census; a hollow `Assert.Fail()` passes it but then deadlocks the
  implement task (which cannot edit the test file) rather than false-greening. The residual — an
  invoking-then-hollow test — is named by the guardrails themselves.
- **Not executed** (toolchain-invoking, or dependent on work that has not happened): `01-tests-build` on
  tasks 01/03, both census guardrails, both `tests-pass` guardrails, wave gates 01 and 02.
- **A₂ ancestor residual**: the censused tree is the PLAN baseline, not each task's. The
  ancestor-prompt/`writeScope` textual check was run for the required tokens (`ObservedModel`,
  `RequestedModel`) and no ancestor pre-writes them.

## Harness issues filed from this wave's run

#507 (`lock` hashes `logs/`), #508 (halt says INCOMPLETE on a 5-of-5 wave), #509 (no `samples/` home above
task level; plan-root is GR2033), #510 (nothing executes a committed sample pair).
