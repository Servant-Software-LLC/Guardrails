# Guardrail review — `wave-05-review-net`

Plan-Definition-Hash: sha256:3f4b23340f8b9a8447453ecd4242d8883bfc2c57b97bdbae5c12caf29da05d7d

Reviewed 2026-08-24 (UTC) against the wave-5 entry tree, tool v1.10.0. Wave authored JIT at the wave-5
barrier over **38+ minutes** — past the retired 30-minute ceiling, and the extra time went into writing
and executing its own sample probes. This is the last wave of Stage 3.

## Verdict

**No BLOCKERs. No WEAK findings.** Nothing applied; stamped as authored.

## Findings

| Task | Guardrail | Sev | Note |
|---|---|---|---|
| — | — | — | **No findings.** The three hazards this wave's shape invites were each already closed by the author — see below. |

### The three risks this wave's shape invites, and how each was already closed

**1. `.claude/` deliverable (#313 — a certain dead-end, not a risk).** Task 04's whole job is writing
`.claude/skills/guardrails-review/SKILL.md`, and the tool-permission layer refuses `.claude/` writes
unconditionally. The straight-to-hatch `needsHarnessWrite` instruction is present (6 mentions) with the
"Do NOT attempt a direct Write/Edit" form.

**2. The graceful-skip silence facts (the wave-1 trap, repeated).** #229's charter makes silence on a
legacy plan an absolute requirement, and a silence assertion **cannot be red before the feature exists** —
in wave 1 a census demanding exactly that coached an agent toward destroying the Invariant-7 test. Here the
two graceful-skip tests are **deliberately excluded** from the red census, and the script says so in its
own failure text: *"a silence assertion cannot be red before the feature exists … Do not add them here to
make this pass."* The census is split `$mustFail` / `$mustPass`, which is the remedy wave 1 needed.

**3. Fourteen verbatim doctrine clauses spanning two tasks (#157, cross-task).** Task 03 pins 14 clauses
that only task 04 can satisfy; a single divergence dead-ends task 04 with a clause no correct
implementation can meet. **Cross-checked mechanically: all 14 appear verbatim in task 04's prompt.** The
anchor comparison is also whitespace-normalised, which the prompt justifies rather than assumes — *"an
anchor set that breaks on re-wrapping is an anchor set someone deletes for being annoying."*

### Considered and not flagged

**No docs task.** Every prior wave ended with an SSOT/domain-knowledge sink; this one does not, and that
is correct — the deliverable is skill text, no documented contract moves, and the wave gate demands no
docs delta (verified: it names neither `docs/plans/02-…` nor the domain-knowledge skill). The brief's
instruction was to emit no docs task rather than a vacuous one, and it was followed.

**The audit lives in the TEST project**, not `src/`. Deliberate, and enforced: `TierClassificationAudit.cs`
sits under `tests/Guardrails.Core.Tests/`, and task 02's guardrail bans `DiagnosticCodes.`,
`new PlanValidator(` and `new Diagnostic(` — the structural backing (#221) for the charter's ruling that
this finding gets no GR code because a code is a thing that can fail a build.

## What was EXECUTED

- **Five committed sample probes, 93 mutation cases total, all behaving as specified**: entry gate 39 (25
  clause mutants, 11 missing-file, 2 missing-directory), wave exit gate 20, task-preflights 11, the
  no-diagnostic-code ban 7, the review-skill-intact regression set 16.
- **Probe A** on every pure-script check. Correctly red where red is right; the wave-5 ENTRY gate is
  **green on the plan baseline** — correct here, unlike waves 3 and 4, because its anchors are Stage-1/2
  tier vocabulary already on master. Task 04's `01-review-skill-intact` is green on arrival and **declares
  it**: *"EVERY CLAUSE IS A REGRESSION CLAUSE, and its nonzero baseline is EXPECTED and NAMED (#478)."*
- **Probe B operator 21 — EXECUTED** against task 01's split census by shadowing `dotnet` with a function
  writing a synthetic TRX, leaving every clause the real code. Four mutants: hollow-but-passing
  **rejected**; honest red **accepted**; a `$mustPass` test going red **rejected** (with a message
  correctly diagnosing the FIXTURES rather than the tests); empty **caught**.
- **§4 missing-insertion, pointed at the gate folders.** Every uncovered path resolves to a pre-existing
  repo file the entry gate anchors on (all verified present) or a bare project/directory argument.
- **Probe C** — the one negative-assertion guardrail reconciles its own #470 axis explicitly and correctly:
  anchored on a USE (`DiagnosticCodes.`, `new PlanValidator(`) so the ordinary English the prompt uses to
  STATE the prohibition cannot trip it. One documented exception: the GR-code-in-a-message ban is
  deliberately not narrowed to code positions, because there a mention **is** the defect.
- `guardrails validate` clean; `graph --check` exit 0.

## What was NOT checked — gaps, not omissions

- **Probe B operator 20: NOT RUN.** Greenfield first review — the real-seam test does not exist yet.
- **Operator 21 was NOT run against task 03's anchors census.** Its shape differs (clause rows plus
  placement assertions) and it would have needed a second shim. The substantive risk that census carries is
  the cross-task clause divergence, which was verified directly instead — all 14 clauses matched verbatim
  against task 04's prompt. Recorded as an unrun probe rather than folded into "operator 21 applied".
- **Seam ledger not produced** to this pass. Re-derived from the folder: this wave substitutes no
  in-process seam — the audit is a pure function over fixture files and the anchors read the skill file
  from disk. No rows owed.
- **Not executed**: both build gates, the suites gate, and the four test-running guardrails — all depend on
  work that has not happened.

## Stage 3 closes here

Five waves, four reviews with findings (waves 1 and 3 carried BLOCKERs), one clean (wave 4), one clean
(this). The conventions established in the wave-2 and wave-3 reviews are now visible in the artifact
without being asked for: the committed sample-pair convention has gone from one hand-written pair to
**93 executed mutation cases**, the comment-strip before a required scan, the `-Repo` wrong-tree hint, and
the `$mustFail`/`$mustPass` census split are all present here and were all corrections made upstream.
