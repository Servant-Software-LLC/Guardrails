{
  "description": "Bucket B (negative/assert-absent baseline, for attribution): the RequestId field is ABSENT from the charge result NOW, so a later 'it is present' gate is provably this plan's doing. A no-op-root doctrine task; ONE-SHOT at run start; inverted polarity.",
  "stableId": "pf-neg",
  "dependsOn": []
}
