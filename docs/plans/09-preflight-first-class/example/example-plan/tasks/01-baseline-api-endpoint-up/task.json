{
  "description": "Bucket A (positive NON-TEST baseline): Acme.Payments.Api GET /health already returns 200 before any work runs — the generalization beyond unit tests. A no-op-root doctrine task; every API modifier dependsOn it (deduped one-per-area).",
  "stableId": "pf-api",
  "dependsOn": []
}
