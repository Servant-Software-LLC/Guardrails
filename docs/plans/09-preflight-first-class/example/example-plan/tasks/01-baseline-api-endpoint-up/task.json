{
  "description": "PREFLIGHT (positive NON-TEST baseline): Acme.Payments.Api GET /health already returns 200 before any work runs. The generalization the design is about — a baseline that is not a unit-test run. No-op action.",
  "stableId": "pf-api",
  "dependsOn": []
}
