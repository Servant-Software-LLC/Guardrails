{
  "description": "Anti-tautology: the new RequestId tests are RED against current code (the local polarity that the 02 negative preflight generalizes). scope:local — never re-run at a union.",
  "scope": "local"
}
