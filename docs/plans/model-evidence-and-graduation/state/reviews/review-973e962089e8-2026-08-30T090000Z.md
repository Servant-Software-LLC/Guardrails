# Guardrail review — model-evidence-and-graduation — AMENDMENT

Plan-Definition-Hash: sha256:973e962089e8ac6970cdf1167b66afa21355807e7ad48e5b4660131444e9a3bf

Amends `review-ab5cd5c1063a-2026-08-30T080000Z.md`, which remains the record of the review pass itself.
**This is an amendment, not a second review** — no new adversarial pass was run. It records a
post-review edit, why it was made, and why it does not disturb the earlier findings.

## What changed after the stamp, and why

A cross-cutting compatibility question — *"does this arc collide with #223 (local inference)?"* — turned
up one field-level forward-compatibility defect that had survived the review pass.

**Task 01 (`TelemetryRow`): `kind` was specified as a field with no stated type.** An implementer who
reached for `PromptRunnerKind` would have produced a corpus that rejects, or silently drops, the first
row from a provider registered after this code was written — which is exactly the provider the corpus
exists to evaluate (`openai-compat` arrives with #223, and local inference is the reason for the arc
this plan belongs to). The corpus is an **archive**: its job is to record what the journal said, not to
have an opinion about it. `JournalTierSpend` already set that precedent one level up, reporting an
unrecognized rung rather than discarding it.

Edits, both in task 01:
1. The prompt now requires `kind`, `model`, `tier`, `tierSource` and `runner` to be recorded as
   **verbatim strings, never a re-validated enum**, with the reasoning and the `JournalTierSpend`
   precedent named.
2. A **seventh pinned behaviour** — `Row_UnrecognizedKind_RoundTripsVerbatim` — added to the prompt's
   behaviour table **and to the red census**.

Point 2 is load-bearing and was not optional: point 1 alone would have been a prose requirement with no
guardrail behind it, which is the #221 prose-only-prohibition shape the review pass had just finished
checking for. Adding the instruction without the census row would have introduced the exact finding
class the pass exists to catch.

## Effect on the earlier findings — none

- The seventh behaviour is red on the stub tree by the same mechanism as the other six (the store stub
  throws), so the census's falsifiability is unchanged. This is **not** the vacuously-true class that
  produced finding 1 against task 12: an unrecognized `kind` round-trip fails against a throwing stub,
  where "nothing was written" did not fail against unwired code.
- No `writeScope`, no `dependsOn`, no DAG shape changed. Task 01 already owned both edited files.
- Findings 2–4 (default corpus root, opt-out surface, task 13's unreachable instruction) are untouched
  and remain fixed. Finding 5 remains an accepted lower bound; 6–7 remain declined NITs.

## Unchecked — carried forward, plus one new

Everything in the original report's unchecked list still stands: the **self-review limitation (#467)**
above all, the unexecuted toolchain guardrails, the `IConsoleIo` process-seam classification, and the
baseline-tree caveat on the A₂ census.

New, and worth recording because it happened during this amendment: a parse check over the edited
scripts first reported **"0 parse failures" having scanned zero files** — it was run from a working
directory where its relative path resolved to nothing. That is #500's zero-match trap reproduced inside
the review's own tooling, and it was caught only by adding a positive control (counting the `ok` lines
and confirming 40 files were actually reached). The re-run from the repo root is the one that counts.
Read as evidence about the review process rather than about this plan: a probe whose pass condition is
"no findings" says nothing until the search is proven to have reached its subject.
