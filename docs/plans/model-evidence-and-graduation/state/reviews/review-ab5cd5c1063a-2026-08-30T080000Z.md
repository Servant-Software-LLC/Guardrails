# Guardrail review — model-evidence-and-graduation (Phase 0 of #533, issue #535)

Plan-Definition-Hash: sha256:ab5cd5c1063aa03c0d83e7993f8ec002e507e313ffd8b970114ae379a80e81c2

Reviewed 2026-08-30. 14 tasks, flat, linear. Tiering configured (haiku/sonnet/opus, all `kind: claude`).

## Reviewer independence — read this before trusting the empty half

**The reviewing agent AUTHORED this breakdown.** #467's ruling is that a self-review inherits the
author's assumptions and the adversarial pass belongs to a non-authoring agent; agent delegation was
not available in this session, so the pass ran solo. The findings below are real. The *absence* of
findings in any area is materially weaker evidence than it would be from an independent pass, and the
areas most likely to be under-audited are the ones the author decided rather than derived — the seam
ledger's zero-row claim and the `IConsoleIo` process-seam classification chief among them.

## Findings

| # | Task | Guardrail | Severity | What slipped through | Disposition |
|---|---|---|---|---|---|
| 1 | 12 | `02-tests-fail-on-stubs` | **BLOCKER** | Two of four pinned behaviours were vacuously TRUE before the wiring existed. `Run_TelemetryWriteFailure_DoesNotChangeTheExitCode` and `Run_WhenCollectionDisabled_IngestsNothing` both PASS on an unwired tree — nothing ingests, so nothing can fail and "nothing written" already holds. The census requires all four observed `Failed`, so task 12 could never go green and would burn its full retry budget to `needsHuman` **with correct tests**. | **FIXED** — both reshaped to assert a CONTRAST that is false until task 13 lands: `Run_TelemetryWriteFailure_IsReported_AndExitCodeUnchanged` (adds: the console reports the failed ingest) and `Run_CollectionDisabled_SuppressesIngestThatOtherwiseHappens` (runs the plan twice — enabled writes rows, opted out writes nothing). Prompt and census updated together. |
| 2 | 09/10 | `02-telemetry-command-tests-pass` | WEAK | Task 10's prompt gave it sole ownership of resolving `~/.guardrails/telemetry/`, but all four behaviours supplied a temp root. An implementation that never resolves a default passed every guardrail while shipping a verb unusable by a real operator. | **FIXED** — fifth behaviour `DefaultCorpusRoot_ResolvesUnderTheUserProfile` added to task 09's prompt and census, explicitly the one test that takes no override and performs no I/O (asserts the resolved path string). |
| 3 | 02/10/13 | (prompts) | WEAK | The opt-out surface was never specified — config key, env var and CLI flag were all consistent with the prompts. Three tasks had to agree on a mechanism none of them named, so task 02 would invent it and 10/13 reverse-engineer it. Two mechanisms for one decision opts a machine out of one path and not the other, which is worse than no opt-out: the operator believes collection is off. | **FIXED** — defined once in task 02 as `GUARDRAILS_TELEMETRY=off`, checked inside the store; tasks 10 and 13 cite it and are told to call the store rather than re-read the environment. |
| 4 | 13 | (prompt) | WEAK | The prompt required sharing `TelemetryCommand`'s corpus-root resolution rather than duplicating it, but task 13's `writeScope` is `RunCommand.cs` alone — it cannot refactor `TelemetryCommand.cs` to expose one. Its only in-scope move was the duplicate the prompt forbade. | **FIXED** — task 10 now exposes the resolution as `internal static` (same assembly, callable from `RunCommand.cs`); task 13 calls it and is told to escalate rather than inline. No `writeScope` change needed. |
| 5 | 14 | `01-surfaces-recorded` | WEAK | A token floor over PROSE: Probe B operator 18 defeats it — one HTML comment listing all five tokens, with `never reported` beside `TelemetryRow`, satisfies every clause while documenting nothing. | **ACCEPTED as a stated lower bound.** Tightening a prose check costs more than it buys; a doc change is human-reviewed on the PR. Recorded here so the next reader does not mistake green for "the SSOT is written well". |
| 6 | gate | `01-solution-builds` | NIT | Green on arrival without declaring that exception, which #478 requires of a green-on-arrival check. | Declined for now — terminal postconditions are green at t=0 by construction. |
| 7 | 12 | `task.json` | NIT | Tiered `medium` while carrying `maxTurns: 75` for the integration-harness archetype; `hard` is defensible. An opinion about difficulty, phrased as a rung. | Declined — one rung on a task that could sit either side. |

**Verdict: the BLOCKER is fixed; findings 2–4 are fixed; 5 is accepted and recorded; 6–7 declined as
NITs.** No open BLOCKER at stamp time.

## Probes run

- **A (baseline red):** task 14's guardrail executed against the real tree — exit 1, all 9 clauses
  firing. **Toolchain guardrails NOT executed** (each invokes `dotnet build`/`dotnet test`, minutes
  apiece): every `01-build-passes`, every `02-*-tests-pass`, every census, and both baseline preflights.
  Named as a gap, not assumed covered.
- **A₂ (clause census):** every required-present token measured against its own subject on the baseline —
  `guardrails/telemetry` 0/0, `telemetry ingest` 0/0, `TelemetryRow` 0, `undifferentiated` 0,
  `run-end telemetry` 0/0, null-rule pair 0. One clause **was** pre-satisfied and was fixed during
  breakdown: a bare `never reported` scan measures **1** in the SSOT (JournalTierSpend's own rule) and
  was hiding behind its failing siblings; it is now a proximity clause requiring the rule within 600
  characters of `TelemetryRow`. Censused tree is the PLAN baseline, not each task's — the ancestor-token
  check was not run per task.
- **B (minimal-gaming mutation):** operators 1–19 applied by reading. **Operator 20 INAPPLICABLE** —
  greenfield, no real-seam test exists to mutate; recorded not-run, never as passed. **Operator 21
  applied to task 14** (the hollow-prose mutant, finding 5); not applicable elsewhere, as no `covers-*`
  token floor exists in this plan.
- **C (pair reconciliation):** no forbidden-present clause exists anywhere in the folder, so no #470
  collision is constructible. Every "do NOT" in a prompt is backed by a `writeScope` exclusion the
  harness enforces — except finding 4, which was the inverse case (a prohibition the scope made
  impossible to obey).
- **#248 (tool-output pattern):** the censuses parse TRX via `$doc.TestRun.Results.UnitTestResult`, and
  TRX carries a default XML namespace. **Verified by execution** against a real TRX generated from this
  repo: the expression resolves, `testName`/`outcome` are readable, theory-row suffixes are handled by
  the substring match, and `foreach` over a null `Results` iterates zero times. Not a defect.
- **#455 (filter scope × DAG edges):** every task-level filter names its own pair's class;
  `~TelemetryCommandTests` verified discriminating against `TelemetryCommandWiringTests`. No forward
  deadlock (task 10's filter excludes the wiring class), no inverse tautology.
- **§4 missing-insertion, pointed at the GATE folders:** `<plan>/preflights/` producer set is nobody and
  it requires only the two existing test projects; `<plan>/guardrails/` requires `Guardrails.sln` and
  both test projects. Satisfied by the repo today.

## Unchecked — stated, not omitted

- The self-review limitation above (#467).
- The toolchain guardrails listed under Probe A.
- `IConsoleIo` classified as a **process seam** (the adapter over process stdout) and therefore given no
  seam-ledger row. Defensible; decided by the author, not independently audited.
- The A₂ census ran against the plan's baseline tree. A non-root task's clause could be pre-satisfied by
  an ancestor's output, and that tree does not exist at review time.
- Zero-match probes carried no positive control (#500); the token counts were read from `grep -c`
  against explicit paths, which reached their subjects, but no control literal was searched to prove it.
