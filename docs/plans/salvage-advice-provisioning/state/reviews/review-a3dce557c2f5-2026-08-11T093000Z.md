# Guardrail review — salvage-advice-provisioning

Plan-Definition-Hash: sha256:a3dce557c2f5a715c498f77233dc60b2cd23621926e55e3d79d52af9aae8d3d5

Reviewed: 2026-08-11T09:30:00Z · Reviewer: authoring agent (self-reported, non-authoritative)
Shape: WAVED — 4 waves, 14 tasks, reviewed wave-by-wave.

## Tooling

- `guardrails validate` → exit 0 (only the advisory GR2025 nudge, cleared by this stamp).
- `guardrails graph --check` → exit 0 (diagram fresh; regenerated after fixes).
- `guardrails plan` → 4 waves in strict order; intra-wave tiers as intended.

## Findings

| Task | Guardrail | Sev | What slips through / breaks | Fix | Outcome |
|---|---|---|---|---|---|
| `wave-04/02-implement-salvage-advice` | `01-advice-tests-pass` | **BLOCKER** | **Orphaned golden (#193).** The `--filter` sweeps in `RetryPolicyTests`, which pins the exact salvage text this task is instructed to rewrite (`git show "<ref>:<path>"` at lines 81/122/238/610; `allowedTools` / `only if` at 135–136). That file was **not** in the task's `writeScope`, so every attempt would fail a test it cannot edit → `needsHuman`. Certain, not speculative. | Add `tests/Guardrails.Core.Tests/RetryPolicyTests.cs` to `writeScope`; instruct the task to re-baseline the pinned strings **while preserving the #374 intent** (no runnable ungranted command), never by deleting the assertion. | **FIXED** |
| `wave-03/01-author-tests-grant-injection` | `01-tests-build` | **BLOCKER** | **#155 behavioral red with no stub.** Assertion 4 requires reading what the runner injected — a surface that does not exist yet — so the test could not compile and `01-tests-build` would fail on every attempt. The task could never go green. | Test-author owns the minimal stub: add `src/Guardrails.Core/Prompts/ClaudePromptRunner.cs` to `writeScope` (stub only; task 02 fills it). Red is now the #155 behavioural pair — `01-tests-build` + `02-tests-fail-on-stubs`. | **FIXED** |
| `wave-01/03,04,05` | `01-floor-not-ceiling-wording` | WEAK | **Gameable by deletion.** The check asserted only that the *stale* wording was absent, never that the corrected claim was present — deleting the whole passage passed. | Added a POSITIVE half: the skill must state the corrected floor/cannot-withhold claim alongside `allowedTools`. | **FIXED** |
| `wave-02/preflights/01` | entry gate | WEAK | **Negative-polarity wave-≥2 entry gate (#254).** Asserted *absence* of the removed claim; doctrine wants entry gates positive-monotone-safe. Also redundant with `wave-01/01`'s own `01-false-claims-removed`. | Dropped the negative half; the gate is now the positive SSOT `git show <ref>:<path>` check only. | **FIXED** |
| build/test guardrails (7 files) | — | WEAK | **Not author-time-executed (#302)** — they need the not-yet-authored tests / built solution. | None; recorded as an honest deferral. | **ACCEPTED** |

## Author-time smoke tests executed (#302)

Two-sided (invalid → non-zero, valid → 0), run against the real repo and hand-synthesized samples:

- `wave-01/01/01-false-claims-removed` — **caught a real false-pass during authoring**: the target claim
  is emitted across two `AppendLine` calls, so the whole-sentence regex never matched. Fixed to match the
  distinctive single-line fragment, then re-verified both ways. (Filed as #428.)
- `wave-01/02/01-ssot-names-granted-route`
- `wave-01/03/01-floor-not-ceiling-wording` — three-sided: stale wording → fail; passage **deleted** →
  fail (the newly-added positive half); corrected wording → pass.
- `wave-02/preflights/01-wave1-corrections-materialized` — both sides.
- `wave-04/03/01-advisory-has-no-unusable-recipe`, `wave-04/04/01-hook-message-reconciled`

Deferred with reason (not runnable at author time): the `dotnet build` / `dotnet test` guardrails in
`wave-01/01`, `wave-02`, `wave-03`, `wave-04` and both wave-1 baseline preflights.

## Clean on

No cross-wave `dependsOn` (GR2034). Wave-qualified state keys agree across harness header, `## Task`
example, and guardrail index. All three `.claude/` prompt tasks carry the straight-to-hatch
`needsHarnessWrite` instruction (#191/#313/#321). Every task declares a real `writeScope`; no `**`, none
absent. Every `tests-pass` guardrail uses the #179 failure-detail re-emit form. Whole-suite/whole-build
checks are LOCAL and only in the LAST wave's exit gate (#125/#165). GR2028 satisfied per wave. No
`integrationGate` remnant (GR2029). No seam-injecting wiring guardrail (#120). Brownfield baseline
preflights are `--filter`-scoped and deduped per area (#181).

## Known process finding

The first BLOCKER is the same class of defect this plan exists to fix — a check demanding something the
actor structurally cannot deliver. The breakdown reproduced it while authoring the correction for it.
Related doctrine gaps filed during this pass: **#428** (grep spanning multiple source-line literals) and
**#429** (the #179 re-emit form defeats GR2028's integration-re-run recognizer).

## Verdict

All BLOCKERs addressed; both WEAKs fixed; one WEAK explicitly accepted (author-time-undeferrable
build/test guardrails). Re-validated to exit 0, diagram regenerated, baseline re-locked after the fixes.
**Approved to stamp.**
