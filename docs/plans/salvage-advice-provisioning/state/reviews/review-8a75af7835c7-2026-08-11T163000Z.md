# Guardrail review (addendum 2) — salvage-advice-provisioning

Plan-Definition-Hash: sha256:8a75af7835c7709b1dc95d5497b284252b9801bab7ad526d29c7021db384ef42

Reviewed: 2026-08-11T16:30:00Z · Supersedes review-df9db1264998 (which supersedes review-a3dce557c2f5).
Prior passes remain valid for everything they covered; this addendum records one change forced by a
harness release.

## Why this addendum exists

The three `.claude/` skill-wording tasks (03/04/05) were authored against the pre-v1.3.0
`needsHarnessWrite` contract, which required FULL file content. That made them impossible:

- 204 KB `plan-breakdown/SKILL.md` — over the runner's output cap; the agent halted honestly.
- 104 KB `guardrails-review/SKILL.md` — the agent tried a `.claude/settings.json` permission
  workaround and hit the wall.
- 82 KB `guardrails-domain-knowledge/SKILL.md` — passed, but only against the WEAK pre-fix guardrail.

v1.3.0 (#437) added an anchored-edit form AND now REFUSES a full-content write to an existing file over
64 KiB. So after the release all three tasks would fail — including 05, which previously passed.

## Change

| Task | Change | Outcome |
|---|---|---|
| `wave-01/03`, `04`, `05` | `action.prompt.md` harness-write header rewritten from the full-content form to the **anchored `edits`** form, matching the shipped Step 5b guidance verbatim: `old` must occur exactly once, matched VERBATIM (only line endings tolerated), edits apply in order and ATOMICALLY, empty `new` deletes. Adds an explicit "do NOT use `content` here" with the reason (>64 KiB refusal, plus the silent-corruption risk of re-emitting thousands of lines of normative text). | **APPLIED** |

Structure verified intact on all three after the edit: one harness-contract header, one `## Task`, one
Scope-boundary paragraph, wave-qualified state key unchanged. `guardrails validate` exits 0.

## Not re-reviewed

No guardrail, `writeScope`, `dependsOn`, or gate changed in this addendum — only the three prompt
bodies. The findings and verdict of the two prior reports stand unchanged.

## Verdict

No open BLOCKERs. Plan re-validated to exit 0, diagram regenerated, baseline re-locked.
**Approved to stamp.**

Note the run must be started with `--fresh`: the plan definition has moved twice since the halted run
(guardrail fixes, then these prompts), and `wave-01/05`'s recorded success was a false green that a
resume would skip rather than re-test.
