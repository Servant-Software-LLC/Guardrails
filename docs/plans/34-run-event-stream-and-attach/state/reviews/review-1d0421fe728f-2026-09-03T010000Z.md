# /guardrails-review — plan 34, pass 2 (non-authoring adversarial pass)

Plan-Definition-Hash: sha256:1d0421fe728f8a0fe57705271ef49eede0e59584bc0290b99a2d10f56a89809c

Supersedes `review-c1941ad00c1d-*.md` (pass 1, self-review by the folder's author). Pass 2 was run by
`guardrails-devils-advocate`, a NON-authoring reviewer, against the post-merge tree. Every finding below
was independently re-verified before being acted on.

## Pass 2 findings — four BLOCKERs the authoring pass missed

| Task | Guardrail | Severity | What it meant | Fix | Status |
|---|---|---|---|---|---|
| 13 | `02-composition-seam-exists.ps1` | **BLOCKER — the plan could not START** | `SampleVerifier` supplies the sample BOTH positionally and as `GR_SUBJECT` (`SampleVerifier.cs:67,336`); this guardrail hardcoded `$file`, so BOTH halves scanned the real `RunCommand.cs` and both exited 1. The same verifier is a **pre-DAG halt** (`PlanPreflightPhase.cs:220`), so the run died at exit 2 before task 01. `samples verify` → `FAILED: 1 finding(s)`. The committed pair was a decoy: present, plausible, inert. | Honour `param([string]$Subject)` → `$env:GR_SUBJECT` → the real default. | **FIXED** — `samples verify` now `OK: 1 pair(s), 0 findings`; still exits 1 on the real tree |
| 13 / 14 | seam accessibility | **BLOCKER** | The prompt said `internal`, both samples said `private`, and the declaration regex accepted either. Task 14's tests live in `Guardrails.Integration.Tests` — a different assembly — and **`Guardrails.Cli` ships no `InternalsVisibleTo`**, so a non-public seam is uncompilable from the tests that must drive it. Fixing the sample wiring alone would have machine-certified `private` as correct. | Seam is now **public** in the prompt, both samples, and the regex — matching the house pattern (`LiveRunObserver`, `OnTheFlyLogSiteObserver` are public for this reason). | **FIXED** |
| 10 | `writeScope` | **BLOCKER** (#474) | The prompt said "register the verb wherever the CLI's other verbs are registered". That site is `CommandFactory.BuildRootCommand` (`CommandFactory.cs:19-36`) and was **not in the task's `writeScope`** — so the agent's only in-scope moves were an honest halt or a command nobody can invoke. | `writeScope` += `CommandFactory.cs`; the prompt now names the file and the `rootCommand.Add` marker. | **FIXED** |
| 04 | discovery instruction + `writeScope` | **BLOCKER** (#474) | The prompt told the agent to find completion paths by grepping `Outcome = AttemptOutcome` — **2** matches, against **18** `AttemptOutcome.` references and `AttemptRecord` constructed in **two** files, the second (`AttemptJournaler.cs`) outside the scope. A fix covering only the reachable paths reproduces the very survivorship defect the plan exists to close. | `writeScope` += `AttemptJournaler.cs`; the prompt now ENUMERATES, states the measured counts, and routes an out-of-scope path to `needsHuman`. | **FIXED** |

## Why pass 1 missed all four

Pass 1's "Verified clean" list cited `validate`, `graph --check`, the #455 table, guard ordering, `-v q`,
Probe C and "all 36 scripts parse". **It never ran `guardrails samples verify`** — the one mechanical check
that exists specifically to catch a guardrail that can never PASS, against the folder's only sample pair.
This is **issue #559** landing on us exactly as filed: *"the samples-verify contract (GR_SUBJECT …) is absent
from the SSOT and the skills — following plan-breakdown exactly produces pairs that all fail at the pre-DAG
gate."* The breakdown followed `stacks/dotnet.md §22` faithfully and produced a pair that fails the gate.
**This is dogfood evidence for #559, and a SKILL defect rather than a plan defect.**

## Verified after the fixes

`samples verify` OK (1 pair, 0 findings) · `validate` OK under the local GR2071-aware build · 36 scripts
parse, positive control confirms the checker detects breakage · task 13's guardrail still exits 1 on the
real tree · the union gate still exits 0 on the valid intermediate union and 1 on conflict markers.

## Residual — unchanged and still honest

No `dotnet build`/`dotnet test` guardrail and neither preflight has been executed end-to-end; the census
TRX logic was proven against hand-built TRX files, never behind a real `dotnet test`. Probe A₂ censused the
plan's baseline tree, not each task's.

## Verdict

Eight findings across two passes, all addressed; no BLOCKER open. The decisive four came from the
non-authoring pass, and the most severe of them — a plan that could not start — was invisible to every
check the authoring pass ran.
