# /guardrails-review — plan 34 (run event stream + attach)

Plan-Definition-Hash: sha256:c1941ad00c1d97643fc39c1f19fde3f0ffe46c2e1d5d386b67182881d309db23

Reviewed 2026-09-03. Base: `design/34-run-event-stream-and-attach` after merging `origin/master`
(plan 33, GR2071, the #586/#588 fixes). Validated with a LOCAL build, because the published tool
(1.14.0) predates GR2071.

## Findings

| Task | Guardrail | Severity | Wrong implementation that slipped through | Fix | Status |
|---|---|---|---|---|---|
| `<plan>/guardrails/` | `03-union-intact.ps1` | **BLOCKER** | Nothing — the inverse. The contribution-present block was gated on task 13's seam while asserting task 15's projections, and the file is `scope:"integration"`, so it re-ran at every union and **red-halted the valid post-13/pre-15 partial merge** (#125). Reproduced: exit 1. | Removed the block; conflict-marker + non-empty union-soundness retained (still credits GR2028). The assertion it reached for is a terminal postcondition already covered LOCAL by `tasks/15/02-wiring-tests-pass`. | **FIXED** — same intermediate union now exit 0; conflict markers still exit 1 |
| 01,03,05,07,09,11,14 | `02-tests-fail-on-stubs.ps1` | **BLOCKER** (01,14) / WEAK (rest) | A hollow test — named for the behaviour, body `Assert.True(true)` — passes on the stub tree and hides behind its genuinely-failing siblings, because the red check was a **suite exit code**. Each prompt enumerates 4–6 behaviours; one real failure satisfied all of them (#75/#375). Tests are run-authored, so this review cannot see them. | Replaced with the **per-test red census**: each behaviour bound to its prompt-pinned method name and observed `Failed` in the runner's own TRX. Two **declared exemptions** (`LogServer_StillServesItsExistingRoutes`, `BuildObserverChain_StillWiresTheExistingObservers`) carry `Expect='Executed'` — a correct implementation leaves both green on the stub tree. | **FIXED** — hollow TRX → named finding; `<Results>`-less TRX → precondition fires |
| 14 / 15 | seam ledger | **BLOCKER** | The ledger declared 1 row; re-deriving from the prompts (#382 check 7) yields 4. The missing one that mattered: `TaskExecutor → IRunObserver`. Every hop was proven against a fake and task 15 proved only *assembly* — **nothing proved an event reaches `events.jsonl` through the real composed chain**. | Added behaviour `AttemptFinished_ThroughTheRealComposedChain_WritesEventsJsonl` to task 14's prompt and its census manifest; task 15's forward `tests-pass` carries it. Asserts an effect only production emits (the file on disk). | **FIXED** |
| 13 | (none existed) | WEAK | A gutted `BuildObserverChain` that returns `inner` unchanged satisfies all three source-shape clauses. Existing tests would catch it, but not at task 13 — it went green and misattributed downstream. Also the #468 demotion: "behaviour unchanged" is behaviour, and only a test carries it. | Added `03-existing-observer-tests-pass.ps1` — LOCAL, `FullyQualifiedName~LogSite` over three pre-existing classes, with the zero-match guard and the #179 re-emit. | **FIXED** |

## Verified clean

`validate` OK (local build, GR2071-aware) · `graph --check` fresh · **#455 scope×edges table clean**
(every filter selects exactly its own pair's class; pairwise substring check found no non-discriminating
pair; no forward deadlock, no inverse tautology) · guard ordering correct in both polarities · no `-v q`
on any `dotnet test` · Probe C: no require/forbid collisions · model resolves (single `claude` runner) ·
tiering unconfigured, so the #229 probe correctly emits nothing · all 36 scripts parse (positive control
confirmed the checker detects breakage).

## What this pass could NOT check

- **Probe B operator 20** — INAPPLICABLE (greenfield: the real-seam tests do not exist yet). Not run.
- **Operator 21** — reasoned by construction rather than manufactured: the target `covers-*` floor did not
  exist at all, so its absence WAS the finding. The census now closes it.
- **Every `dotnet build`/`dotnet test` guardrail and both preflights — NOT executed** (minutes each).
  The census's TRX logic was proven against three hand-built TRX shapes, but no census has been run
  end-to-end behind a real `dotnet test`.
- **A₂ censused the plan's baseline tree, not each task's.** A non-root task's clause could be
  pre-satisfied by an ancestor's output; the ancestor-prompt textual check was run in its place.
- **The reviewer authored the folder.** Untracked, so no human edit to distinguish. Three BLOCKERs were
  found in the author's own work, but a non-authoring adversarial pass has NOT been run and remains the
  single most valuable thing outstanding.

## Verdict

Four findings, all addressed; no BLOCKER open. The plan is a reviewed draft, not proven — the
un-run guardrails above are the honest residual.
