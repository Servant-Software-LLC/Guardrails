{
  "description": "Implement the pre-DAG plan-preflight phase (evaluate <plan>/preflights/ once before scheduling via IReVerifier; record the B1 planPreflights marker; halt exit 2 on red; resume SKIP on a matching passed marker; re-run on --fresh / planHash mismatch) and land the SSOT §7 resume rule in the same change",
  "stableId": "p2pd07",
  "dependsOn": ["06-author-tests-pre-dag-phase", "02-implement-reverifier-wiring"],
  "writeScope": [
    "src/Guardrails.Core/Execution/",
    "src/Guardrails.Cli/",
    "docs/plans/02-schemas-and-contracts.md"
  ],
  "action": { "maxTurns": 75 }
}
