# Guardrail review — 39-incremental-delivery

Plan-Definition-Hash: sha256:075a478b1b7e3803bbc3a311df8ef58fcd01142807eb1886a25dead62e6ce2b4

Reviewed and remediated 2026-09-11 (UTC). Flat plan. **21 tasks / 8 tiers as reviewed → 23 tasks /
12 tiers as remediated.** `guardrails validate` exit 0, `graph --check` exit 0 after regeneration,
48/48 scripts parse.

**VERDICT: all 10 BLOCKERs and all 14 WEAK findings FIXED and re-verified. 4 NITs recorded. The
plan is marked reviewed.**

## How this pass was run (#467)

This session AUTHORED the folder, so the adversarial pass was DELEGATED to two independent agents
handed the folder cold, with this session's own findings WITHHELD: `guardrails-devils-advocate`
(adversarial) and `guardrails-architect` (contract). The four maintainer-ruling artifacts were
NAMED to both as things to attack.

**Every one of the three passes found a BLOCKER the other two missed** — the strongest evidence
for #467 this project has produced:

| pass | BLOCKERs only it found |
|---|---|
| contract lens | 5, 6, 7, 8 — the four missing or FALSE schema surfaces |
| adversarial lens | 1, 2, 10 — the strip defect, the census-vs-negative-control dead-end, the ungated numbering |
| authoring pass | none unique; it reproduced 3 and 4 |

## Findings and resolution

| # | Task | Sev | What dead-ends or slips through | Fix APPLIED |
|---|---|---|---|---|
| 1 | 21 | BLOCKER | **A certain false RED, measured.** The `(?s)<!--.*?-->` strip removed **169,334 bytes — 44.8% of `plan-breakdown/SKILL.md`** in one 167,393-byte span, because line 636 is the skill's own prose teaching this idiom (*"fail on a residual unterminated `` `<!--` ``"*) and that bare mention opened a match running to line 2579. `$baselineArea` (line 1313), the exact anchor the prompt directs `GR2078` to, was inside it. The unterminated-`<!--` precondition reported HEALTHY throughout. | Mask fenced blocks and inline code spans, strip, then UNMASK — so a `<!--` a reader SEES cannot be an opener, and fence content still counts (the counter-rule). **Measured: 169,334 → 1,355 bytes.** Two-sided: a correct implementation with `GR2078` at Step 5 now exits **0**; both tokens hidden in a real HTML comment still exit **1**. Applied to plan 40's three doc guardrails too (latent there, not live). |
| 2 | 01, 03, 05, 07, 14, 16, 18 | BLOCKER | **The red census demanded `Failed` from rows a CORRECT test leaves GREEN.** Measured on task 03 against a hand-written correct TDD-red file: exit 1 with **four misattributed findings**, each telling the agent its correct negative control was "hollow or already implemented". A diagnostic that does not exist is silent, so every `_IsSilent` row and the severity row pass by construction. Task 14's `AFastForwardDelivery_DoesNotRefresh` — the maintainer's own gating ruling — likewise, with no stub to force it red. Positive control: all tests failing → exit 0, so the instrument is sound. | ONE RULE across 7 tasks: drop from the RED set, DECLARE the exemption inline with its structural reason, assert the row still EXISTS (`$mustExist`), and pair it with a new **forward census** on the implement task requiring each observed `Passed`. 7 declarations + 7 forward censuses written. |
| 3 | 11, 13 | BLOCKER | **The CLI test in a project that cannot see the CLI.** `WaveDeliveredCliForwardingTests.cs` in `Guardrails.Core.Tests`, which references `Guardrails.Core` alone (0 `using Guardrails.Cli` vs 156 in Integration.Tests; stated verbatim at `PlanSource/PlanSourceWiringTests.cs:21`). No task may edit a `.csproj`. Identical to plan 40's finding 4 — task 11's prompt shows why, citing plan 40's draft as its model. | Moved to `tests/Guardrails.Integration.Tests/WaveDelivery/`; task 13's guardrail repointed at the Integration project. |
| 4 | 11 → 13 | BLOCKER | **Task 13 passable by doing literally nothing, measured.** `EveryCliDecorator_ForwardsTheEvent` was pinned in the prompt and dropped from the census, and the filter `~WaveDeliveredEventTests` never selected the CLI class. A one-line `Placeholder() => Assert.True(true)` compiled, was invisible to the census, and gave task 13 **`Failed: 0, Passed: 1`, exit 0** with `src/Guardrails.Cli/` untouched. | New `03-cli-forwarding-tests-fail-on-stubs.ps1` on task 11 over the Integration project, plus a forward census on task 13. |
| 5 | 01, 02, 20 | BLOCKER (design) | **There is no wave manifest.** §1b/§5 said *"the wave manifest gains `delivers`"*; SSOT §14.1 says v1 has *"no per-wave config"*, and `WaveFolder.TryResolveWaveTarget` treats a directory with its own `guardrails.json` as **a plan, never a wave** — the obvious guess silently un-waves the plan. | **DECIDED: `delivers: true` in the existing optional `brief.md` YAML front matter.** Design §1b rewritten; tasks 01/02/20/21 prompts and task 20's token retargeted from the JSON form to `delivers: true`. No loader-detection change needed. |
| 6 | 05, 06, 20 | BLOCKER (design) | **`DecisionEntry` has no wave member** — §1a's premise, on which `d39-interlock-scope` rests, is false. 18 public members, none a wave; `Subject` is free text and only the `wave` boundary puts a wave dir there, while the `task`/`drift` boundaries that produce suppressing decisions put a task id. | Design §1a corrected to say the attribution must be ADDED. `DecisionEntry.cs` added to task 05's `writeScope` as the stub; `TheWaveAttributionIsRecordedOnTheDecision_NotParsedFromSubject` pinned; task 06's prompt requires recording at each creation site; task 20 records the surface change and now depends on 06. |
| 7 | 01 | BLOCKER (design) | **`WaveDefinitionHash` would not move when `delivers` moved** — so flipping it on a completed wave produced no drift and the review marker kept reading `passed`. | **Discharged by the `brief.md` choice**: `Compute` → `GateDefinitionOf` already folds `brief.md` and says so in its own comment. Collapsed from a hasher change to one pinned test, `WaveDefinitionHash_ChangesWhenDeliversChanges`. |
| 8 | 07, 08 | BLOCKER (design) | **Self-contradictory merge order.** §1 requires the gate to see the delivered tree; §3 gates delivery on the exit. Both prompts said *"Merge first, then gate"* while pinning `AWaveWhoseExitGateFails_DoesNotDeliver`; neither said "scratch" or "trial". The cheapest passing implementation merges onto the user's branch and hard-resets on red — contradicting task 16's own `TheUsersCheckoutIsNotModified` and the un-merge §1a forbids. | **DECIDED: a TRIAL MERGE on `refs/guardrails/trial/<waveDir>`.** Design §1 rewritten with the four-step order; tasks 07/08 prompts rewritten; `AFailedExitGateAfterTheTrialMerge_LeavesTheUsersBranchUnmoved` pinned; the ref recorded as an SSOT token. |
| 9 | (none) | BLOCKER (missing) | **The design's one named required harness change had no task.** §1c: *"a positive baseline re-evaluates; a negative one keeps skip-once… the only place in this design where the harness must grow a new distinction."* `RunWaveEntryGateAsync` documents skip-once for BOTH kinds. A grep over the whole folder returned zero. Tasks 14/15 shipped the refresh, which the design says is defensible ONLY because the next wave re-verifies. | **NEW pair 22/23**, ordered `08 → 22 → 23 → {14, 16}`, pinning `APositiveBaselineEntryCheck_ReEvaluatesAfterADelivery`, `ANegativeBaselineEntryCheck_KeepsSkipOnce`, `AnEntryGateAfterARefresh_RunsAgainstTheRefreshedTree`. The prompt names why there is no stub (the type exists; the DISTINCTION is missing) and warns against fixing one direction by breaking the other. |
| 10 | 03 | BLOCKER | **Two of three deliverables verified by nothing.** The constants and the next-free marker were checked only by a whole-solution build and a census over test NAMES; the cheapest implementation wrote the tests against string literals and never opened `DiagnosticCodes.cs`, leaving the marker at GR2078 for the next plan to re-take. | Ported plan 31's check as **`03-codes-and-marker.ps1`**: both constants DECLARED (anchored on `= "GR20xx"`), GR2077 still unallocated, exactly one live marker reading GR2080. Verified three ways — today's file exits 1 naming all three problems, a correct implementation exits 0, a GR2077-taking mutant exits 1 — and the prose mention at line 567 correctly does not count as a second marker. Committed `.valid`/`.invalid` pair in `samples/`. |
| 11 | 07, 08 | WEAK | **#578 CORRECTED: `DeliverAndCleanup` does not exist.** `grep -rn DeliverAndCleanup src/ tests/` exits 1; the name entered at charter review and propagated into the design, the task description and the prompt. | Rewritten as an executable pointer at `Scheduler.Finalize` → `DeliverToUserBranch` → `MergePlanBranchIntoUserBranch`, in the prompt, the task description, the design's §1 table and its §5. |
| 12 | 11, 12, 13 | WEAK | **#578, repeating plan 40.** `grep -rln "IRunObserver"` returns 17 files in Core; the declaration set is 3, including the nested `NullObserver` whose contract is to SWALLOW. A reflection-derived sweep would demand it forward. | Grep changed to `: IRunObserver`; task 11 pointed at `ObserverForwardingSweepTests` as the precedent, naming the `NullObserver` trap and its declared exemption. |
| 13 | 21 | WEAK | **#578: "Step 0.8" does not exist.** The fork is substep 8 of `## Step 0`; a different `## Step 8 — Regeneration merge` DOES exist at line 2496, so shortening the pointer lands the edit in an unrelated section. | Replaced with the durable marker: *"grep for `Decide FLAT vs WAVED`"*. |
| 14 | 11, 12, 13 | WEAK | **Orphaned golden, repeating plan 40's finding 16.** `ObserverForwardingSweepTests` requires four named decorators to DECLARE every `IRunObserver` member; task 11 adds one with a no-op default, so it reds immediately. Named only in prose; tasks 12/13, which close it, never mentioned it. | Named in both 12 and 13's prompts with its four types and an instruction not to edit it, plus a `LogSiteHaltBannerTests` caution on 13 (keep the page byte-identical when nothing was delivered). |
| 15 | 06, 08, 11, 12, 13, 15, 17, 19 | WEAK | **Nine unordered `writeScope` overlaps**, sharpest 15 ↔ 17 (same tier, both whole-`Execution/`, both editing the delivery call site). The union gate was a conflict-marker scan only, and its header argued the inverse of what that scan does. | Every scope narrowed to its real files; the Scheduler.cs writers serialized into `06 → 08 → 23 → 15 → 17`. **Re-measured: 9 → 0 unordered overlapping pairs.** Duplicate-definition count added to the union gate, and its header corrected. |
| 16 | 07, 18 | WEAK | Undeclared census exemptions — the right call taken silently, so a future edit cannot tell a decision from an oversight, and nothing required those tests to exist. | Folded into finding 2's one rule: declared + `$mustExist` + forward census. |
| 17 | 03, 04 | WEAK | **#221 prose-only prohibition on the human's ruling** — *"Both are WARNINGS… Do not strengthen them to errors"* was pinned only by a run-authored test body. | `BothAreWarnings_AndDoNotMoveTheExitCode` now sits in task 04's forward census (must be observed `Passed`), and the pre-existing `DiagnosticCatalogueTests.EverySeverityMarkerMatchesWhatTheSourceTreeActuallyDoes` independently reads the real `Warning(...)` call sites. |
| 18 | 14, 16 | WEAK | **The house fake cannot express either scenario.** `FakeWorktreeProvider.MergePlanBranchIntoUserBranch` HARDCODES `MergeOnSuccessResult.FastForwarded`, making `AFastForwardDelivery_DoesNotRefresh` trivially green and `ANonFastForwardDelivery_…` plus every BranchMoved case inexpressible. | Both prompts now require the real `GitWorktreeProvider` over temp repos, with the measured house precedent (26 files vs 3) quoted. |
| 19 | 20 | WEAK | Eight contract changes never reached the SSOT, and task 20 depended on only 3 of the tasks whose contracts it records. Separately, `02-schemas-and-contracts.md` says *"an unrelated new code should take `GR2078`"*, which this plan makes false. | Prompt extended to seven contracts; `dependsOn` extended to 04, 06, 10, 13, 15, 17; tokens `GR2079` and `refs/guardrails/trial/` added (each measured 0) plus a **NEGATIVE clause** requiring the stale `should take **`GR2078`**` sentence to be GONE — the only clause that proves a correction rather than an append. |
| 20 | 21 | WEAK | The `d39-wave-doctrine` ruling — the only decision here that changes what `plan-breakdown` tells every future author — had no gate, nor did the serialization cost, the Step 7 report, or GR2079. | Tokens `for parallelism`, `delivery point`, `GR2079` added (each measured 0) plus a **NEGATIVE clause** on the old unqualified `**Do NOT wave a flat plan** —`. The prompt now demands a REWORD, not an append. |
| 21 | 03 | WEAK | GR2077's reservation had no gate; the catalogue tests cannot catch taking it (reserved in a comment, and taking 2077+2078 still leaves the high-water mark at 2078). | `GR2077_RemainsReservedAndUnallocated` pinned, and `03-codes-and-marker.ps1` asserts no constant equals `"GR2077"` — verified against a mutant that takes it. |
| 22 | 13 scripts | WEAK | **#608 allowlist re-emit.** Measured on plan 40 against real xunit.v3 output, it drops `System.NotImplementedException : …` — the dominant first-attempt failure here — and the `Failed <TestName>` header. | Block capture applied to all 13; task 23's new one written with it. **14/14 carrying the block capture, 0 allowlist-shaped.** |
| 23 | 04, 06, 12, 13, 15, 17, 19 | WEAK | Over-broad whole-directory and whole-project scopes manufacturing the finding-15 overlaps. | Folded into finding 15's narrowing. |
| 24 | 20, 21 | WEAK | Documentation lower bound: one appended prose line carrying the tokens takes both to exit 0. | Accepted and NAMED rather than taken silently (see the residuals below). The two negative clauses raise the bar materially — an append cannot satisfy them. |
| 25 | 20, 21 | NIT | The earlier toothless-clause repair bisected both headers mid-sentence. | **NOT FIXED** — cosmetic, recorded. |
| 26 | 20 | NIT | Latent twin of finding 1 on the SSOT (measured: one stray inline `` `<!--` `` would blank 166,702 bytes). | **FIXED** by the same bounded strip. |
| 27 | design §5 | NIT | *"New diagnostics — one, not four"* contradicted §1c's second code. | **FIXED** — §5 now reads TWO and names both. |
| 28 | 01, 02 | NIT | `Delivers` conflates the declared flag with the effective delivery-point predicate. | **NOT FIXED** — recorded; both readings are separately pinned by name, so the ambiguity is in the naming rather than the coverage. |

## Measured clearances (attempts that FAILED, not areas skipped)

- **The bounded strip, four ways.** Old vs new on all four subjects (169,334 → 1,355 on the skill;
  2,615 → 2,516 on the SSOT; 0 → 0 on the README; 95 → 0 on the domain-knowledge skill). Three
  literals unique to the previously-blanked span (lines 1300/1350/1400, each occurring exactly once)
  go from invisible to visible. A token inside a REAL comment is still killed. A token in a FENCE
  still counts. And the end-to-end two-sided test on the real subject: valid → 0, invalid → 1.
- **The earlier toothless-clause repair, verified not trusted.** The original bare tokens really do
  measure 7 / 28 / 4, and all replacements measure 0 — declared counts re-measure exactly.
- **`03-codes-and-marker.ps1` three ways** — today's file (exit 1, three problems), a correct
  implementation (exit 0), a GR2077-taking mutant (exit 1). Committed sample pair in `samples/`,
  re-run both halves.
- **Every token added was measured first**, present-clauses at 0 and absent-clauses at >0, with
  positive controls (SSOT `PlanDefinition`=39, skill `writeScope`=79). The script REFUSES to add a
  clause that cannot bite; `GR2078` was rejected as an SSOT token because it already appears once —
  which is itself finding 19.
- **#493** — 9 unordered overlapping pairs before, **0 after**, same script.
- **Parse** — 48/48 scripts, negative control bit.
- **Probe A** — the three clause-bearing guardrails exit 1 with every clause firing (6, 6, 3); the
  union gate exits 0, correctly union-safe.
- **#455** — `Category!=WaveDelivery` measured non-vacuous; the plan-wide trait appears only in the
  preflights' `!=` exclusion.
- **#439** — VSTest measured (no `MTP0001`), so `--filter` is honoured.
- **Four-folder model** — whole-build and whole-suite correctly LOCAL; the one `scope:"integration"`
  guardrail is conditional and union-safe.

## What this pass could NOT check

- **#229 model-appropriateness: NOT RUN, correctly silent.** No `routing` or `tiering` block.
- **Probe B operator 20: NOT RUN — inapplicable.** Greenfield first review.
- **Probe B operator 21: RUN** against task 01 and the documentation guardrails; NOT against every
  census.
- **The #382 seam ledger was NOT PRODUCED to this pass** — an unchecked gap, not an absent heading.
  Checks 6 and 7 were run from the folder alone and produced finding 18.
- **Guardrails NOT executed end-to-end:** every `dotnet build`/`dotnet test` guardrail — 10
  `01-build-passes.ps1`, both preflights, the two plan-root gates, 10 `01-tests-pass.ps1`, and all
  the censuses (their filters select classes that do not exist yet). Their decisive components were
  executed against real tool output during plan 40's review, on the identical templates.
- **The correct-negative case (finding 2) was MANUFACTURED for tasks 03, 13 and 14 only.** The same
  defect in 01, 05, 07, 16 and 18 was argued from the manifest text. The fix was applied uniformly;
  the *diagnosis* was executed on three of seven.
- **Probe A₂'s deepest residual** — every clause was censused against the PLAN baseline, not each
  task's. Tasks 02–23 run against trees their ancestors wrote; the ancestor-prompt grep was not run
  for every non-root task.
- **The documentation token floors remain LOWER BOUNDS.** One dense sentence can satisfy several
  positive tokens at once. The two NEGATIVE clauses are the part that cannot be satisfied by
  appending, which is why they were added; the positive half still relies on a human reading it.

## Verdict

All 10 BLOCKERs and all 14 WEAK findings fixed and re-verified; NITs 25 and 28 recorded and left.
`validate` clean, `graph --check` fresh, 0 unordered scope overlaps, 48/48 scripts parse. Plan
marked reviewed at this hash.
