<Project Sdk="Microsoft.NET.Sdk">

  <!-- Build policy (ImplicitUsings, Nullable, TreatWarningsAsErrors, analyzer/restore
       determinism) is centralised in the repo-root Directory.Build.props. -->
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="YamlDotNet" Version="16.3.0" />
  </ItemGroup>

  <ItemGroup>
    <!-- The banned-guardrail-pattern registry (SSOT §4.6, issue #346) is authored ONCE beside the
         catalogue and embedded here so PlanValidator loads it with zero runtime path discovery. The
         LogicalName pins the manifest resource id (BannedPatternRegistry.ResourceName) regardless of
         the source file's on-disk path. Forward slashes keep the Include cross-platform. One authored
         file, no drift. -->
    <EmbeddedResource Include="../../.claude/skills/plan-breakdown/references/banned-guardrail-patterns.json">
      <Link>Resources/banned-guardrail-patterns.json</Link>
      <LogicalName>Guardrails.Core.Resources.banned-guardrail-patterns.json</LogicalName>
    </EmbeddedResource>

    <!-- The guardrail-abort shim (design 38 S3.2, issue #608) wraps every .ps1 guardrail invocation
         so an uncaught engine error can never reach the harness as a false exit-0 PASS. Embedded here
         (same mechanism as the registry above) so InterpreterMap materializes it with zero runtime
         path discovery. It lives inside this project's own directory tree, so it must be pulled out of
         the SDK's implicit None glob first, otherwise it is both a None and an EmbeddedResource item. -->
    <!-- The DIAGNOSTIC CATALOGUE (issue #558). `guardrails validate` emits codes and nothing in the
         shipped tool could tell an operator what one meant: the only authoritative catalogue was the XML
         doc comments in this source file, which a consumer of the packaged dotnet tool does not have.
         Embedding the SOURCE — rather than generating a parallel table — is the whole point: a second copy
         is what produced the drift the issue documents (16 defined codes the SSOT never mentioned, plus two
         it named that no constant defined). Same mechanism as the two resources above. -->
    <EmbeddedResource Include="Loading/DiagnosticCodes.cs">
      <LogicalName>Guardrails.Core.Resources.DiagnosticCodes.cs</LogicalName>
    </EmbeddedResource>

    <None Remove="Execution/guardrail-shim.ps1" />
    <EmbeddedResource Include="Execution/guardrail-shim.ps1">
      <LogicalName>Guardrails.Core.Resources.guardrail-shim.ps1</LogicalName>
    </EmbeddedResource>
  </ItemGroup>

  <ItemGroup>
    <!-- Tests pin internal contracts (e.g. ClaudePromptRunner flag spelling). -->
    <InternalsVisibleTo Include="Guardrails.Core.Tests" />
    <InternalsVisibleTo Include="Guardrails.Integration.Tests" />
  </ItemGroup>

</Project>
