Read `out/recipient.txt` (relative to your working directory). It contains a single word:
the recipient.

Write `out/greeting.txt` containing exactly `Hello, <recipient>!` on one line, with
`<recipient>` replaced by that word. Write nothing else, and change no other file.

Then stop.
