Read `out/greeting.txt` (relative to your working directory) — the raw file, not a summary
of it.

Write `out/review.md` containing exactly these three parts:

- a `# Greeting review` heading;
- a `## Greeting` section quoting the greeting VERBATIM, on its own line;
- a `## Verdict` section of at least one full sentence saying whether the greeting reads
  warmly, and why.

Be honest in the verdict. A greeting that reads flat should be described as flat — a
review that praises everything tells the reader nothing.

Change no other file. Then stop.
